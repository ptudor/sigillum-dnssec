# Fix log — REVIEW_ASTRA6_XHIGH.md

Worked in the suggested fix order (REVIEW_ASTRA6_XHIGH.md §"Suggested fix order and dependencies").
Each entry: finding ID, what changed, files touched, verification result. SKIPPED entries record why the spec was ambiguous or conflicted with actual code behavior. Paths are relative to the module named in the entry.

Baseline before any changes (review baseline `0e12ebc`, source baseline `a22bfe8`): both modules `go vet ./...` clean and `go test -race -count=1 ./...` green. Toolchain: go1.27.0 darwin/arm64 (modules pin `toolchain go1.26.5`).

---

## Phase 1 — Regression boundaries and containment of false Secure verdicts

### RA6X-007 — Unsigned Additional DS records can authenticate an attacker-controlled child — FIXED
- **Change (Golang-dnssec-validator):** Every parsed record now carries `Owner`, `Class` and `Section` (`answer`/`authority`/`additional`; additive `omitempty` JSON fields), produced by new per-section converters in `internal/dns/records.go` that `parseResponse` uses section by section instead of merging first. Added an authenticated-data layer (`internal/validator/authenticated.go`): `VerifyRRsetFromResponse` evaluates every covering RRSIG as an indivisible candidate (owner, signer, validity window, crypto under each eligible same-tag key) and returns a `VerifiedRRset` holding the exact wire records and the exact signature that verified; `VerifyDNSKEYRRsetFromResponse` verifies the zone's Answer-section DNSKEY RRset over the wire against the authenticated keys; `VerifyDenialRRsetsFromResponse` verifies Answer/Authority NSEC/NSEC3 RRsets and returns only those that verified (Additional is never a candidate; unverifiable RRsets are reported and excluded, not promoted and not allowed to poison a genuine proof). The DS path now returns a `parentDSResult` separating the served Answer-section child-owned DS RRset (diagnostic `ZoneResult.DS`) from the exact authenticated DS RRset; only the authenticated set enters `ValidateChainLink`, `CollectDSMatchedKeys`, the RDAP comparison and `ChainLink.ParentDS`, and the DS signature must name the actual parent zone as signer. The parent for the DS query is the zone preceding the child in the walked hierarchy (`parentZoneFromHierarchy`) so the signer requirement is correct for label-skipping delegations. `ZoneResult.DNSKEY` is the Answer-section RRset owned by the zone; DNSKEY RRSIG verification runs over the raw response rather than a reconstruction of the merged slice. Leaf verification selects RRSIG/CNAME candidates from the Answer section at the queried owner only; the NSEC/NSEC3 `WithRRSIG` wrappers, `verifyDSAbsence` and `verifyWildcard` run their semantic checks on the authenticated records only. The old `VerifyRRsetRRSIGFromResponse`/`AnyKey` helpers remain as thin wrappers over the new core. Two test seams were added for the hermetic harness (RA6X-050): `Resolver.SetDefaultPort` and an unexported `Validator.rootServers` override.
- **Files:** `internal/dns/records.go` (new), `internal/dns/types.go`, `internal/dns/query.go`, `internal/dns/resolver.go`, `internal/dns/dialaddr_test.go`, `internal/dns/sections_ra6x007_test.go` (new), `internal/validator/authenticated.go` (new), `internal/validator/dnssec.go`, `internal/validator/validator.go`, `internal/validator/nsec.go`, `internal/validator/verification_fixes_test.go`, `internal/validator/harness_test.go` (new), `internal/validator/ds_section_ra6x007_test.go` (new)
- **Verification:** The review's probe is reproduced through the real `validateZone` path with the hermetic harness: a genuinely signed `child.test. DS` for key A in Answer plus an unsigned DS for attacker key B (same owner in Additional, unrelated owner in Additional, and same owner in Authority) with a child DNSKEY RRset signed only by B is now **Bogus** in all three placements and the injected DS never appears in `ZoneResult.DS` or the chain link; the identical response with only A's chain (and legitimate glue in Additional) is **Secure** with `ChainLink.ParentDS` equal to the exact authenticated DS. Also covered: an Additional-section DNSKEY is not part of the RRset (genuine RRset still Secure, injected key not promoted; RRset signed only by a non-DS-matched key is Bogus), the denial boundary (Additional NSEC ignored, Additional-only NSEC is no evidence, an unrelated unverifiable NSEC RRset is excluded without failing a genuine proof), a DS RRSIG naming another zone as signer is rejected, the leaf RRSIG must sit in the Answer section, and the parser's per-section metadata. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RA6X-008 — A mixed anchor document bypasses the root-anchor pinning boundary — FIXED
- **Change (Golang-dnssec-validator):** Added `dns.GetActivePinnedAnchors`, the only anchor set a root authentication path may consume (currently valid AND exactly pinned). `validateZone`'s root branch now uses it for both the existential root check and the DNSKEY-signing key set, and `CollectAnchorMatchedKeys` itself skips every non-pinned anchor, so the filter is applied at the final trust decision and not only at ingestion. Loading still retains unpinned entries as diagnostic data (`/api/anchors`, `GetActiveAnchors`), but they can no longer contribute a key. Readiness (`/healthz`, `/health`) and the `dnssec_validator_root_anchors_active` gauge now count active pinned anchors, so a document with only unpinned active entries reports not-ready rather than up-and-unable-to-validate. Both shipped pins (KSK-2017 and KSK-2024) and their overlap remain accepted.
- **Files:** `internal/dns/anchors.go`, `internal/validator/dnssec.go`, `internal/validator/validator.go`, `health.go`, `main.go`, `internal/metrics/metrics.go`, `internal/dns/anchors_pinned_ra6x008_test.go` (new), `internal/validator/anchor_pin_ra6x008_test.go` (new), `health_test.go`, `handlers_test.go` (fixtures gained the `DigestType: 2` the exact pin requires)
- **Verification:** The review's probe is reproduced at both levels using the real IANA KSK-2017 public key (the test first proves its computed DS equals the pinned digest): with an anchor set holding the genuine pinned KSK-2017 DS plus an attacker DS matching an attacker root key, `VerifyRootTrustAnchor` still passes (genuine key present) but `CollectAnchorMatchedKeys` now returns only the pinned key and a DNSKEY RRSIG made only by the attacker key is rejected; through the hermetic harness, a root DNSKEY RRset containing the genuine KSK-2017 plus the attacker keys, signed only by the attacker, is **Bogus** in `validateZone`, and a set whose only active anchor is unpinned is **Indeterminate**. `GetActivePinnedAnchors` keeps both shipped pins and drops unpinned, tag-colliding, future and expired entries while `GetActiveAnchors` retains the diagnostics; an unpinned-only active set fails `/healthz`. A positive signature by the genuine root KSK cannot be produced hermetically (no private key); the positive path is covered structurally (the genuine key remains the sole anchor-matched signing candidate, and the crypto path is unchanged from the existing R-024 tests). `go vet ./...` clean; `go test -race -count=1 ./...` green.

### RA6X-016 — Trusted key material loses its zone identity during signature verification — FIXED
- **Change (Golang-dnssec-validator):** Trusted keys now carry their authenticated zone (`DNSKEYRecord.Owner`, set by the parser and threaded through `ZoneResult.DNSKEY` → next zone's parent keys). `trustedKeyForSigner` refuses to place an owner-bearing key at any signer name other than its own zone, so verification never rewrites a key's owner to make a signature fit; `keyBelongsToZone` is applied in `CollectDSMatchedKeys` (a key parsed at another owner is never "the child's key" even when the DS digest computed at the child matches its material) and `CollectAnchorMatchedKeys` (root keys must be owned by `.`). `VerifyDNSKEYRRSIGByKeys` takes the expected zone and requires the RRSIG signer/owner and every RRset member's owner to be that zone; `VerifyDNSKEYRRsetFromResponse` already required owner and signer to equal the zone and verifies the original wire records. The DS signer must equal the actual parent zone (RA6X-007). Denial verification (`VerifyDenialRRsetsFromResponse`) now takes the zone whose keys are used and requires every accepted NSEC/NSEC3 RRset to be owned within that zone and signed by it; `VerifyNSECDenialWithRRSIG` gained a `zone` argument, `verifyDSAbsence`/`finalizeNoDSDelegation` take the parent zone, and `verifyWildcard` takes the leaf zone. Leaf A/CNAME verification calls `VerifyRRsetFromResponse` with the queried owner and the zone as required signer. Legitimate key reuse (the same key material at two zones, each signing its own data) and key-tag collisions remain accepted.
- **Files:** `internal/validator/dnssec.go`, `internal/validator/authenticated.go`, `internal/validator/validator.go`, `internal/validator/nsec.go`, `internal/validator/zone_identity_ra6x016_test.go` (new), `internal/validator/chain_enforce_test.go`, `internal/validator/verification_fixes_test.go`, `internal/validator/wildcard_test.go`, `internal/validator/denial_rrsig_test.go`, `internal/validator/ds_section_ra6x007_test.go` (call sites updated for the new zone arguments)
- **Verification:** Two zones `a.test.`/`b.test.` share identical KSK/ZSK material. DNSKEY: each zone's own RRset verifies; A's response replayed as B's, B's RRset signed with A as signer, the review's record-level call shape (`VerifyDNSKEYRRSIGByKeys` for zone B with a signature made at A), and an A-owned DNSKEY folded into B's RRset are all rejected; `CollectDSMatchedKeys` no longer matches A's records as B's keys. DS: a DS RRset signed by parent P1 verifies under P1 and fails under sibling P2's identical keys. Denial: A's NSEC is rejected as B's evidence, an NSEC inside B signed with A as signer is rejected, and B's own NXDOMAIN proof verifies through `VerifyNSECDenialWithRRSIG`. Leaf: a signature over `www.b.test.` naming A as signer fails under B's keys (with and without an explicit expected signer). Caller level through `validateZone` with the hermetic harness: B's shared-key deployment is **Secure**, A's DNSKEY response replayed for B is **Bogus**, and B's RRset signed at A is **Bogus**. `go vet ./...` clean; `go test -race -count=1 ./...` green.


## Phase 2 — Unify signature selection and denial semantics

### RA6X-009 — Wildcard checks use metadata from a signature that never verified — FIXED
- **Change (Golang-dnssec-validator):** Leaf verification no longer picks the first covering RRSIG for its metadata. `verifyActualRecord` now calls `VerifyRRsetFromResponse` with all eligible zone keys (every covering signature at the queried owner is an indivisible candidate: owner, signer, validity window and crypto must all hold for that one signature), and the new `recordVerifiedLeaf` derives `SigningKeyTag`, `RecordCount` and the wildcard decision (`DetectWildcardSynthesis` → Labels) from the exact `VerifiedRRset.Signature` that verified. A candidate's successful crypto check can no longer validate another candidate's Labels/SignerName. The CNAME path uses the same engine. The old first-signature pre-checks (time validity, key lookup, signer check on the display record) were removed from the leaf path.
- **Files:** `internal/validator/validator.go`, `internal/validator/authenticated.go` (from RA6X-007), `internal/validator/signature_candidates_ra6x009_017_test.go` (new), `internal/validator/harness_test.go` (mock now preserves handler rcode)
- **Verification:** Through the real `verifyActualRecord` path with the hermetic harness: an authentic wildcard expansion (`*.child.test. A` served as `www.child.test.`, Labels 2) with an invalid same-tag RRSIG claiming Labels 3 prepended, appended, with a different SignerName, or both around the genuine one, is detected as wildcard from the verified signature and is **Bogus** without an NSEC no-exact-match proof and **Secure** (Wildcard, WildcardProofVerified, SigningKeyTag = verified key) with the proof, regardless of ordering. An ordinary exact-name answer stays Secure and non-wildcard, and a forged Labels-2 signature beside a genuine exact-name one does not mark it wildcard. `go vet ./...` clean; `go test -race -count=1 ./...` green.

### RA6X-017 — Unsupported DS algorithms and first-signature rejection produce false Bogus verdicts — FIXED
- **Change (Golang-dnssec-validator):** (1) Every eligible covering signature is tried as a whole: the leaf A/CNAME path (via RA6X-009's engine) and the NSEC/NSEC3 `WithRRSIG` denial wrappers, whose first-signature pre-checks (`findRRSIGForType` → time validity → `findDNSKEYByTag`) were deleted so an expired or unknown-key first signature no longer blocks a valid alternative; the wrappers dropped their now-unused parsed-RRSIG parameter (`VerifyNSECDenialWithRRSIG(qname, qtype, nsec, keys, zone, raw, rcode)`, likewise NSEC3). (2) Added `SupportedDNSKEYAlgorithm`, `SupportedDSDigestType` and `SupportedDSRecords`; after the parent has authenticated the DS RRset, `validateZone` classifies it and, when no DS uses a supported algorithm and digest type, sets the zone **Insecure** with an explanatory warning (RFC 4035 §5.2 / RFC 6840 §5.2) instead of Bogus; each unsupported DS is warned about; chain matching uses only the supported subset, so a supported path that fails is still Bogus. The classification runs only on the authenticated RRset, so an unauthenticated extra DS can neither downgrade nor rescue a chain.
- **Files:** `internal/validator/dnssec.go`, `internal/validator/validator.go`, `internal/validator/nsec.go`, `internal/validator/denial_rrsig_test.go`, `internal/validator/nsec_test.go`, `internal/validator/zone_identity_ra6x016_test.go` (call sites), `internal/validator/signature_candidates_ra6x009_017_test.go` (new)
- **Verification:** Permutations of expired, unknown-key, invalid-bytes and valid signatures with different key tags all verify via the valid one under A, CNAME, NSEC NXDOMAIN and NSEC3 NODATA through the real leaf path, in every order; an RRset with only expired/unknown-key signatures still fails, and an NSEC signed only by an expired signature fails with the validity-period reason. DS classification through `validateZone`: only-unsupported DS (ED448 algorithm, GOST digest) → **Insecure** with the no-supported-path warning; unsupported plus a good supported DS → **Secure**; unsupported plus a supported DS with a wrong digest → **Bogus**; an unsupported DS injected unsigned in Additional beside a genuine chain → **Secure**. `go vet ./...` clean; `go test -race -count=1 ./...` green.

### RA6X-010 — NSEC NXDOMAIN accepts existing empty nonterminals and crosses delegation cuts — FIXED
- **Change (Golang-dnssec-validator):** `verifyNSECNXDOMAIN` now rejects NXDOMAIN when the covering NSEC's next name lies below the queried name (the name exists as an empty non-terminal, RFC 7129 §5.5), when an NSEC is owned by the queried name or by the wildcard at the closest encloser (existence), and when any authenticated NSEC shows an ancestor of the queried name (below the apex) is a delegation point (NS set, SOA clear) or a DNAME owner (`nsecAncestorBoundary`, RFC 6840 §4.1). The same ancestor-boundary rule guards `verifyNSECNODATA`. The `WithRRSIG` wrappers additionally require the queried name to be within the zone whose keys sign the denial (`qnameWithinZone`), and RA6X-016 already binds every denial RRset owner to that zone. Proofs record their `ClosestEncloser`/`NextCloser` (additive JSON). Apex wraparound chains and exact DS-absence-at-delegation handling are unchanged.
- **Files:** `internal/validator/nsec.go`, `internal/validator/denial_semantics_ra6x010_013_test.go` (new)
- **Verification:** Signed end-to-end through `verifyActualRecord`: the review's `a.example.com.` example (NSEC `example.com. → x.a.example.com.`) and the `x.child.example.com.` example (parent NSEC `child.example.com. → example.com.` with NS+DS) are both rejected with the empty-non-terminal and delegation-point reasons and are never Secure; NODATA below the cut and NXDOMAIN below a DNAME are rejected; a name outside the zone cannot be denied; a genuine child NXDOMAIN signed by the child and an apex-wraparound NXDOMAIN are Secure; a parent DS NODATA with an authenticated delegation NSEC yields **Insecure** through `validateZone`. (Accepting a NOERROR/NODATA for an empty non-terminal is the RA6X-014 feature and is verified there.) `go test -race -count=1 ./...` green.

### RA6X-011 — NSEC wildcard proof does not establish the claimed closest encloser — FIXED
- **Change (Golang-dnssec-validator):** The NSEC branch of `VerifyWildcardDenial` derives the closest encloser from the covering NSEC (`closestEncloserFromNSEC`) and requires it to equal the closest encloser the authenticated signature claims (`lastNLabels(qname, Labels)`); it rejects an NSEC owned by the queried name or by the next closer name (a closer existing ancestor), a covering NSEC whose next name lies below the queried name (the query is itself an existing empty non-terminal), and any proof crossing an ancestor delegation or DNAME. Labels values that do not describe an expansion are rejected. Callers pass only authenticated records (RA6X-007) and the signature's Labels come from the verified signature (RA6X-009).
- **Files:** `internal/validator/nsec.go`, `internal/validator/denial_semantics_ra6x010_013_test.go`
- **Verification:** Signed through the real leaf path: a real signature over `*.example.com. A` expanded to `foo.bar.example.com.` with the review's signed NSEC `bar.example.com. → z.bar.example.com.` is rejected (**Bogus**, closest-encloser disagreement); the same NSEC with `*.bar.example.com.` is accepted (**Secure**, `ClosestEncloser = bar.example.com.`); a query that is itself an existing empty non-terminal is rejected; a proof crossing a delegation NSEC is rejected; canonical wraparound (last NSEC → apex) still proves a late name. Existing wildcard tests pass unchanged.

### RA6X-012 — NSEC3 proofs omit delegation boundaries and opt-out security semantics — FIXED
- **Change (Golang-dnssec-validator):** New chain engine in `nsec.go`: `nsec3Chain.closestEncloser` performs the RFC 5155 §8.3 proof and retains the exact closest-encloser and next-closer records; a matched closest encloser with NS-and-no-SOA or DNAME is rejected (`nsec3RecordBoundary`); records with unknown flag bits are ignored and reported (`Ignored`, RFC 5155 §8.2). Opt-out is represented separately from verification: an NXDOMAIN whose next closer is covered by an opt-out record, and a wildcard proof whose next-closer cover is opt-out, verify with `NSECProof.OptOut = true`, and `recordValidationVerdict` maps an opt-out conclusion to **Insecure** (RFC 5155 §9.2), recorded as a warning rather than an error. `verifyDSAbsence` (NSEC3) now accepts a delegation match (NS, no DS, no SOA) as a secure insecure-delegation proof, and requires a complete closest-provable-encloser proof (closest encloser matched, next closer covered by an opt-out record) for opt-out DS absence; a bare cover, a non-opt-out cover, and a matching record that is not a delegation no longer prove anything. Existing statuses are unchanged; `opt_out`, `closest_encloser`, `next_closer` and `ignored` are additive JSON fields.
- **Files:** `internal/validator/nsec.go`, `internal/validator/validator.go`, `internal/validator/denial_semantics_ra6x010_013_test.go`
- **Verification:** Signed fixtures through the leaf path: the review's self-wrapping NSEC3 for `child.example.com.` (NS+DS) no longer denies `x.child.example.com.` (delegation-point reason), nor does an unsigned-delegation or DNAME encloser; the signed self-wrapping apex NSEC3 with Flags=1 yields `Verified=true, OptOut=true` and an **Insecure** verdict (Secure without opt-out); a wildcard answer whose next-closer cover is opt-out is **Insecure** and Secure with a plain cover; flags 2 and 3 are ignored with the unknown-flags reason; DS absence: an opt-out cover without a closest-encloser match fails, a complete three-record opt-out chain proves an insecure delegation with `OptOut`, the same chain without opt-out proves non-existence (not DS absence), a matching NSEC3 with the DS bit is rejected, and a matching delegation NSEC3 without DS proves a secure insecure delegation. `go test -race -count=1 ./...` green.

### RA6X-013 — Wildcard and DS-absence paths combine incompatible NSEC3 chains — FIXED
- **Change (Golang-dnssec-validator):** One parameter-aware engine serves NXDOMAIN, NODATA, wildcard and DS-absence proofs: `nsec3Chains` partitions authenticated records by (algorithm, iterations, normalized salt — "" and "-" and hex case unified), builds an `nsec3Chain` per group, and every hash/match/cover lookup is a chain method, so a hash computed with one chain's parameters is never compared with another chain's intervals. `VerifyWildcardDenial` (NSEC3) and `verifyDSAbsence` no longer take `records[0]`'s parameters; each chain is evaluated independently and a complete valid chain beside an incomplete transition chain succeeds in either order. `verifyNSEC3NXDOMAIN`/`verifyNSEC3NODATA` reject a mixed-parameter record set outright.
- **Files:** `internal/validator/nsec.go`, `internal/validator/validator.go`, `internal/validator/denial_semantics_ra6x010_013_test.go`
- **Verification:** The review's probe (two self-wrapping records owned by `H(foo.example.com.)` under salts "" and "AA") fails alone, as a union, reversed, with a different iteration count, and as all three, both at the `VerifyWildcardDenial` level and signed through the leaf wildcard path; the same construction for DS absence fails in every combination; a complete valid chain beside an incomplete transition chain proves NXDOMAIN in both orders. Existing R-037 grouping tests pass. `go test -race -count=1 ./...` green.

### RA6X-014 — Valid wildcard NODATA, empty-nonterminal denial, and DNAME answers are unsupported — FIXED
- **Change (Golang-dnssec-validator):** `verifyNSECNODATA` gained two proof branches beyond the exact-owner match: an empty-non-terminal NODATA (a normal-interval NSEC covering the name whose next name lies below it, RFC 7129 §5.5) and a wildcard NODATA (covering NSEC → closest encloser; the NSEC owned by `*.<closest encloser>` must lack the type, have no CNAME bit and not be a delegation, RFC 7129 §5.6). `verifyNSEC3NODATAInChain` gained the RFC 5155 §8.7 wildcard NODATA branch (closest-encloser proof plus a matching NSEC3 for the wildcard lacking the type; opt-out on the next-closer cover is propagated as an insecure conclusion). DNAME: the parser now records DNAME records (`QueryResult.DNAME`, additive); `verifyActualRecord` detects an Answer-section DNAME that is a proper ancestor of the queried name, authenticates the DNAME RRset under the zone's keys (`verifyDNAMEAnswer`), requires exactly one DNAME record, computes the RFC 6672 §2.2 substitution (`substituteDNAME`, enforcing the 255-octet limit) and requires the served synthesized CNAME to equal it exactly; a YXDOMAIN answer carrying an authenticated DNAME is reported as an authenticated-but-unresolvable name (indeterminate, never secure), while an unsigned DNAME stays bogus. `RecordValidation` gained additive `target` (authenticated alias target for CNAME and DNAME answers) and `synthesized_from` fields. The synthesized CNAME is followed through the existing CNAME traversal with its loop/depth limits. Leaf-path selection is now owner-aware (`answerHasTypeAt`) so a DNAME/CNAME chain answer carrying the target's records is not mistaken for a direct answer. Ordinary unsigned CNAMEs, false NXDOMAIN for existing non-terminals and positive answers keep their strict handling.
- **Files:** `internal/dns/types.go`, `internal/dns/records.go`, `internal/dns/query.go`, `internal/validator/result.go`, `internal/validator/nsec.go`, `internal/validator/validator.go`, `internal/validator/authenticated.go`, `internal/validator/nodata_dname_ra6x014_test.go` (new), `internal/validator/nsec_test.go` (one message expectation)
- **Verification:** Through the real leaf path with signed fixtures: a zone with `*.example.com. A` answers `foo.example.com. AAAA` as **Secure** NODATA under NSEC (apex + wildcard NSEC) and NSEC3 (closest-encloser proof + wildcard NSEC3), and the proof fails when the wildcard lists the type or the wildcard record is missing; an empty non-terminal `a.example.com.` gets Secure NODATA while NXDOMAIN for it stays rejected; a signed DNAME answer verifies with `synthesized_from`/`target` set and is Secure, a tampered synthesized target is rejected, an unsigned DNAME is rejected, an ordinary unsigned CNAME is still rejected, and an over-long substitution (server YXDOMAIN with the signed DNAME) is never secure (indeterminate; bogus when the DNAME is unsigned); helper checks cover root-target substitution and the DNAME-owner-itself exclusion. `go vet ./...` clean; `go test -race -count=1 ./...` green.

### RA6X-019 — Recursive infrastructure lookups ignore truncation, owner identity, and some DNS errors — FIXED
- **Change (Golang-dnssec-validator):** The querier's UDP-then-TCP-on-truncation logic became the shared `Querier.exchange` transport used by both authoritative queries and every recursive infrastructure lookup (`exchangeRecursive`, which also sets EDNS0 and keeps CD on). `ResolveNS` and `CheckZoneCut` accept only NS records owned by the requested name (Answer first, then an Authority delegation for the same owner); NS reached through an alias or owned by another name is ignored. Non-success RCODEs are explicit: NXDOMAIN yields no nameservers, any other failure RCODE is an error. `ResolveAddressesDetailed` resolves A and AAAA independently, follows CNAMEs inside the answer for a nameserver host (bounded), keeps a working family when the other fails, reports each failed family, and errors only when both fail; `ResolveNSWithAddresses` carries those problems on `NSRecord.LookupErrors` (additive JSON) and `validateZone` surfaces them as zone warnings. `DiscoverZoneCuts` stops on a cancelled/expired context instead of guessing a chain. The DS query already targets the actual parent from the walked hierarchy (`parentZoneFromHierarchy`, RA6X-007), and RA6X-010/012's authority rules keep parent denial from applying below a possible child.
- **Files:** `internal/dns/query.go`, `internal/dns/resolver.go`, `internal/dns/types.go`, `internal/validator/validator.go`, `internal/dns/resolver_ra6x019_test.go` (new), `internal/validator/discovery_ra6x019_test.go` (new), `internal/validator/harness_test.go` (mock gained TCP, UDP truncation and dropped-reply modes)
- **Verification:** A loopback recursive fixture serving UDP and TCP: a TC-flagged empty UDP answer is retried over TCP and all three NS / both addresses are used; NS answers owned by an unrelated name, reached through a CNAME, or mixed with a decoy owner keep only the requested owner's records and do not make the name a zone cut; REFUSED is an error, NXDOMAIN is "no nameservers"; a SERVFAIL A with a good AAAA yields the AAAA plus a reported problem, both families failing is an error, an aliased host yields only the target's addresses, a dropped A with a good AAAA is partial, and `ResolveNSWithAddresses` carries the family problem. Validator side: a delegation `child.branch.example.` directly from `example.` validates **Secure** with the DS verified against `example.` and no query for the non-existent `branch.example.`; a cancelled discovery returns the context error promptly. `go test -race -count=1 ./...` green.

### RA6X-015 — CNAME traversal is detached from the authenticated leaf response and fails open on lookup errors — FIXED
- **Change (Golang-dnssec-validator):** Leaf verification now returns the authenticated alias target (`RecordValidation.Target`, set only from the exact verified CNAME RRset or the verified DNAME substitution), and `validateWithCache` follows that target directly (`followAlias`) — there is no second DNS lookup to rediscover the CNAME. A hop whose target validation fails outright is a `CNAMEChainResult` with `Indeterminate` and an `Error`, folded into the overall verdict; a target that validates Bogus/Insecure/Indeterminate bounds the answer as before. Below an insecure or indeterminate chain (no authenticated leaf) the alias is discovered unauthenticated from the Answer-section CNAME owned by the queried name only (`discoverAliasTarget`); CNAMEs at other owners or in Authority/Additional are never followed. An explicit `type=CNAME` query completes once its RRset is authenticated and performs no traversal. Loop/depth guards (`cnameGuard`), sticky insecure ancestry and the original requested type are preserved; `checkAndFollowCNAME` was removed.
- **Files:** `internal/validator/validator.go`, `internal/validator/result.go` (from RA6X-014), `internal/validator/alias_traversal_ra6x015_test.go` (new), `internal/validator/harness_test.go` (seeded-chain fixture)
- **Verification:** End-to-end through `validateWithCache` with the root and `test.` seeded as secure cache entries and everything below served live: a signed CNAME to a target whose zone is Bogus yields **Bogus** with the hop recorded; dropping the target zone's DNSKEY replies yields **Indeterminate** with an indeterminate hop, never Secure; a switching server that answers the alias with a bogus target first and a good one afterwards is followed to the authenticated first target with exactly one alias fetch; an unrelated Additional CNAME and a same-owner Additional CNAME are not followed; same-zone and cross-zone aliases validate Secure with the leaf carrying the authenticated target; an explicit CNAME query is Secure with no traversal; an unsigned alias in an insecure zone stays Insecure to a secure target and becomes Bogus to a bogus target. `go test -race -count=1 ./...` green.

### RA6X-018 — Per-server Secure indicators are transport success, and extended mode misses signature failures — FIXED
- **Change (Golang-dnssec-validator):** `ValidateMultipleServers` now records reachability only (NOERROR answer → `validating` with the response attached; failure → `indeterminate`). `validateZone` establishes the zone from the first server response whose DNSKEY RRset chains to the trust source (DS/anchor digest match plus a verifying RRSIG over that server's wire RRset) — one valid path suffices even if an earlier server is broken — and then `judgeDNSKEYServers` gives every queried server its own verdict against the authenticated RRset: identical key material (owner, flags, protocol, algorithm, public key, not 16-bit tags) and a verifying RRSIG → `secure`; empty, different or unsigned → `bogus` with a reason, a `Disagreement` entry and a warning; nonresponders stay `indeterminate`; insecure/unsigned zones mark their servers `insecure`. In extended mode the parent's remaining servers are also queried for the DS RRset and judged against the chosen answer (`judgeParentDSServer`; reported as `disagreements` with `server = parent <zone>`). The leaf path (`queryLeafAllServers`) verifies each server's positive answer under the zone keys, chooses the first that verifies, and flags others by content or signature; the fingerprint is type-aware (`canonicalRRString`: owner and RDATA domain names lowercased, TXT-style RDATA retained, TTL zeroed, duplicates collapsed). The UI shows each address's verdict with its reason and a Server Disagreements section; the project doc gained a table describing what each per-address status means and that disagreement is diagnostic. Quick mode still queries at most two servers.
- **Files:** `internal/validator/validator.go`, `static/app.js`, `static/style.css`, `CLAUDE.md`, `internal/validator/harness_test.go` (IPv6-loopback second server with per-family overrides), `internal/validator/server_consensus_ra6x018_022_test.go` (new)
- **Verification:** Two servers (127.0.0.1 and ::1) serving `child.test.`: identical data with the signature missing on one → that server `bogus` with a signature reason and one disagreement, zone still Secure; same-tag/different key material on one → `bogus` with a "differs" disagreement; an empty DNSKEY answer on one → `bogus` "no DNSKEY"; the FIRST server unsigned and the second fine → zone Secure via the second, first `bogus`; no responders → zone and both addresses `indeterminate`. Leaf: identical A data unsigned on the second server is flagged; an expired signature on the first server and a valid one on the second yields the valid answer with the first flagged; TTL-only and domain-name-case differences fingerprint equal, duplicates collapse, TXT case differs. `go test -race -count=1 ./...` green; `node --check static/app.js` passes.

### RA6X-022 — SSE completion and cached UI results do not represent the finished top-level validation — FIXED
- **Change (Golang-dnssec-validator):** All terminal emissions go through `emitComplete`, which emits only at depth 0, so a nested alias-target validation that ends Bogus early no longer sends a `complete`; its outcome is carried as a `CNAMEChainResult` in the single terminal event. The validated-zone cache holds zone authentication only: the per-name `RecordValidation` is no longer written into the shared cached object, so two names in one zone each keep their own result on their own chain copy. `RecordValidation` gained `name` and `ZoneEvent` gained `query_name`/`depth` (additive) to identify per-name results, and the leaf zone is re-emitted as a second `zone` event once its record validation exists. The UI reconciles instead of skipping: repeated zone events update the existing card/tab in place, `completeValidation` reconciles every card from the final payload (main chain and each alias hop's chain) and re-renders the selected zone, and zone details render every per-name record validation for the zone with its denial proof, wildcard proof (closest encloser, next closer, opt-out), alias target/DNAME source and per-server disagreements, plus the zone-level disagreements and per-address errors. Event names and existing payload fields are unchanged.
- **Files:** `internal/validator/validator.go`, `internal/validator/result.go`, `static/app.js`, `static/style.css`, `internal/validator/server_consensus_ra6x018_022_test.go`
- **Verification:** Recording the event stream for an alias to a bogus target through the seeded-chain harness: exactly one `complete` event, carrying the original chain and the CNAME failure, and the leaf zone re-emitted at depth 0 with its record validation named `alias.child.test.`. Two names in the same zone (a same-zone alias): the cache entry for the zone holds no record validation, the source chain's leaf carries the CNAME validation for `good.child.test.` and the hop's chain leaf carries the A validation for `www.child.test.`. The browser reconciliation is verified structurally (JS syntax check; handlers reconcile by zone from the final payload) — no browser test harness exists in the repository. `go test -race -count=1 ./...` green.

### RA6X-052 — Signature candidate search has no CPU-work budget or cancellation boundary — FIXED
- **Change (Golang-dnssec-validator):** New `VerifyBudget` (`internal/validator/budget.go`): one per top-level validation, created in `validateWithCache` at depth 0 from the validation context and shared by every nested alias hop and by every chain, DS, DNSKEY, leaf and denial check. Every signature verification attempt is charged one unit and DS/anchor digest matching is charged per DS×key pair; the charge fails once the limit (`DefaultVerificationBudget` = 2000 units, adjustable via `SetVerificationBudget`) is exceeded or the context is cancelled, and stays failed. Budget-aware internals (`verifyRRsetInRecords`, `verifyRRsetFromResponseB`, `verifyDNSKEYRRsetFromResponseB`, `verifyDenialRRsetsFromResponseB`, `verifyNSECDenialWithRRSIGB`, `verifyNSEC3DenialWithRRSIGB`, `verifyDSRRSIGSetB`) are used by the validator; the exported helpers remain unbounded wrappers. Candidate signatures are de-duplicated by content and trusted keys by material before any cryptography, and algorithm/tag/eligibility filtering still precedes crypto. Exhaustion maps to an explicit **Indeterminate** result with the resource-limit reason (zone level, DS validation, per-server judgment, DS-absence, leaf and wildcard/denial verdicts, and surfaced in the top-level errors), never Secure and never a fabricated cryptographic failure. Collision handling and any-valid-signature semantics are unchanged. Limit choice: a normal chain with denial proofs spends well under 100 units (the end-to-end fixture spends a handful), the review's adversarial 128×128 fixture would need 16,384, so 2000 leaves a large legitimate margin while bounding hostile responses to a fixed small CPU cost per request. NSEC3 hashing is not charged; it is already bounded by the RFC 9276 iteration cap and label count.
- **Files:** `internal/validator/budget.go` (new), `internal/validator/authenticated.go`, `internal/validator/nsec.go`, `internal/validator/validator.go`, `internal/validator/budget_ra6x052_test.go` (new), `internal/validator/harness_test.go`, `internal/dns/resolver_ra6x019_test.go` (mock port binding now retries on collisions)
- **Verification:** 30 duplicate bad signatures × 30 duplicate keys followed by the genuine signature verify within a 10-unit budget spending ≤2 units; a zero budget refuses the first attempt with the sentinel and stays exhausted; a cancelled context stops verification with the sentinel; an exhausted NSEC denial reports the reason and the leaf, denial and wildcard verdict mappings yield Indeterminate; end-to-end through the seeded chain, a 1-unit budget yields **Indeterminate** naming the limit while the default budget validates the same chain Secure with a small spend, and alias hops share the budget. `go test -race -count=1 ./...` green. Quantifying the remote end-to-end exposure with authenticated adversarial DNSKEY fixtures (the review's open investigation item) is not attempted; the budget bounds it regardless.

### RA6X-041 — Unusable anchor refreshes can replace working trust anchors and suppress fallback — FIXED
- **Change (Golang-dnssec-validator):** `finalizeAnchors` now validates dates (a malformed or missing `validFrom`/`validUntil` is a document error), drops expired anchors, retains future-dated anchors for rollover, and declares a load successful only when the document yields at least one currently active pinned anchor; a future-only, expired-only, unpinned-only or empty document fails. `LoadAnchorsWithFallback` falls back to the URL for any unusable file and, when both fail, returns an error naming both reasons; an empty URL document no longer counts as success. `AnchorsStore.Load` already left the previous set and `loadedAt` untouched on failure; that retention is now documented as the contract, and because activation dates are re-evaluated at every use (`GetActivePinnedAnchors`, RA6X-008), a retained set whose anchors have since expired is not trusted and readiness reports the gap. File-first/offline operation and static pinning are unchanged.
- **Files:** `internal/dns/anchors.go`, `anchors_store.go`, `internal/dns/anchors_lifecycle_ra6x041_test.go` (new), `anchors_store_ra6x041_test.go` (new)
- **Verification:** Future-only, malformed-date, missing-date, expired-only and empty documents all fail to load; a mixed active/future set loads with both anchors and one active; a future-only file falls back to a good URL, an empty URL document fails with both sources named, and a usable file is used without consulting the URL; a store refreshed from an unusable file with an unusable fallback keeps serving its last-known-good set with its load timestamp unchanged. Existing anchor tests pass unchanged. `go test -race -count=1 ./...` green.


## Phase 3 — Signer persistence and recovery

### RA6X-006 — Daemon startup writes state outside the cross-process lock — FIXED
- **Change (Golang-tudor-dnssec-signer):** `runServe` now loads the config, ensures `data_dir`, takes a new daemon-instance lock (`acquireInstanceLock`, an flock on `<data_dir>/serve.lock` held for the life of the process; a second `serve` is refused before it touches any shared state, so port binding is no longer what stands between two daemons) and then loads the startup state snapshot under the cross-process state lock (`loadStateLocked`), which is also used for the SIGHUP reload. `validateStartup` no longer calls `state.Save()`: it performs a nondestructive check (`checkStateFileAccessible` opens an existing file read-write without truncation; a missing file is fine because `data_dir` writability was already probed), so a stale startup snapshot is never written and a duplicate or failed startup cannot clobber live state. The first signing cycle adopts concurrent CLI commits under the lock as before. Existing startup diagnostics and CLI concurrency are unchanged; the Windows stub gained a matching process-local instance lock.
- **Files:** `main.go`, `daemon.go`, `filelock_unix.go`, `filelock_windows.go`, `daemon_state_ra6x006_026_test.go` (new), `filelock_test.go` (fixture gained the live KSK the new state validator requires)
- **Verification:** With a newer state committed by another writer before startup, `validateStartup` leaves `state.json` byte-for-byte unchanged and the first cycle's save keeps the other writer's zone; a second instance lock is refused immediately with a clear message; a `Run()` that fails at health-port binding leaves `state.json` and every key file byte-for-byte unchanged; a startup load blocks while a CLI holds the state lock and then observes the CLI's commit. `go test -race -count=1 ./...` green.

### RA6X-026 — The daemon continues signing and overwrites disk state after a failed reload — FIXED
- **Change (Golang-tudor-dnssec-signer):** `signAllZones` fails closed when `ReloadFromDisk` fails after taking the process lock: no zone is signed, no key or state file is mutated, the reason is recorded as a persistent daemon fault (`Daemon.StateFault`), and the read is retried next cycle; the pre-save reload likewise skips `Save()` on failure so an unreadable or newer file is never overwritten with the stale snapshot (already-written signed output stays in place). `/health` lists the fault under `dependency_errors` and `/healthz` returns 503 until a cycle loads the state successfully. A state file that existed at the last successful load or save and is now missing is an explicit `ErrStateFileMissing` error with the recovery instruction (restore the file, or restart the daemon to rebuild state from the key files if the removal was intentional) rather than an empty/no-change update; a daemon that never had a file still starts fresh.
- **Files:** `internal/state/state.go`, `daemon.go`, `health.go`, `daemon_state_ra6x006_026_test.go`, `internal/state/state_validation_ra6x036_test.go`
- **Verification:** After startup, replacing `state.json` with malformed JSON makes the cycle sign nothing, leave the malformed file untouched, expose the fault, and turn `/healthz` 503 with `/health` naming the state fault; repairing the file lets the next cycle sign and clears the fault; removing the file blocks signing, does not recreate it from memory, and reports the restart instruction; an interrupted CLI transition (an impossible rollover phase on disk) blocks signing and is left for the operator. `go test -race -count=1 ./...` green.

### RA6X-036 — Syntactically valid malformed state is accepted and later panics or changes rollover interpretation — FIXED
- **Change (Golang-tudor-dnssec-signer):** `LoadState` and `ReloadFromDisk` validate and normalize the document before exposing it (`validateAndNormalize`): a null zone map becomes an explicitly allowed empty map; null zone entries, empty or invalid zone names, NUL-bearing paths, a signed zone with an empty path, and key records without an algorithm are rejected; a rollover record is accepted only in the persisted phases (ksk/ds_add_wait, zsk/pre_publish, zsk/signing, algorithm/algo_ds_add_wait) with distinct old/new identities and a live key matching the phase (KSK = new key during KSK/algorithm rollover, ZSK = old key in pre-publish, new key in signing, both new keys for algorithm rollover with distinct non-empty algorithms); anything else fails closed with an actionable message that says the file was left unchanged. Omitted legacy fields keep their conservative zero defaults; a keyless placeholder zone remains valid. `State.Validate` exposes the same checks. Two synthetic fixtures (a rollover with no key record) were given the key the record refers to.
- **Files:** `internal/state/state.go`, `internal/state/state_validation_ra6x036_test.go` (new), `verification_fixes2_test.go`, `filelock_test.go`
- **Verification:** `{"zones":null}` loads as an empty map and a later write does not panic; null entries, bad names, signed-without-path, algorithm-less keys, unknown rollover types/phases, same-id rollovers, wrong live keys, keyless rollovers and algorithm rollovers without algorithms are all rejected with the file left byte-for-byte unchanged; every valid persisted rollover shape and the keyless placeholder load; a reload against an invalid disk document is rejected without merging and leaves the evidence; the existing old-schema tolerance tests pass. `go test -race -count=1 ./...` green.

### RA6X-040 — Startup can race configuration reload before workers obtain a guarded snapshot — FIXED
- **Change (Golang-tudor-dnssec-signer):** `Run` takes one guarded snapshot at entry and every startup reference (directory setup, startup validation, listen addresses, the heartbeat client it starts) goes through it; `ensureDirectories`/`validateStartup` take the config explicitly. The daemon publishes a ready lifecycle state (`ready` channel, `markReady`) only after the listeners are bound, the heartbeat is started and every worker is registered; `Reload` waits for it while a startup is in progress (and is a no-op once shutdown has begun or startup failed — `Run` cancels the context on error so a waiting reload is released). `runSigningLoop` reads its initial poll interval from a guarded snapshot instead of `d.cfg`. `Shutdown` cancels first and reads the heartbeat client under the lock, and `Run` re-checks for a shutdown request after binding so no heartbeat or worker starts on a cancelled daemon. Hot reload, listener restrictions, poll-interval reset and graceful shutdown are unchanged, and the daemon lock is still never held across network I/O.
- **Files:** `daemon.go`, `daemon_lifecycle_ra6x040_test.go` (new), `daemon_state_ra6x006_026_test.go` (call signatures)
- **Verification:** The review's exact probe — `runSigningLoop` registered on the wait group while `Reload` swaps fresh config copies, then cancel and wait — passes under `-race`; ten reloads delivered during `Run`'s startup block until readiness and then apply, shutdown drains cleanly, and a reload after shutdown returns immediately; a shutdown delivered before startup makes `Run` return without running the signing loop, and a failed startup (bound port) releases a pending reload. Full module suite green under `-race`.

### RA6X-044 — Ownership failures are reported as successful key/state publication — FIXED
- **Change (Golang-tudor-dnssec-signer):** `fsutil.ChownToTarget` now returns its error and is part of every staged write's success contract: `WriteFileAtomicOwned` assigns ownership to the staged file before the rename and aborts, leaving the readable predecessor in place, when it cannot; `CopyFile` stages through a temp file with the same rule; the state and instance locks fail if the lock file cannot be owned; `EnsureDir`/`EnsureDirSecure` assign ownership to directories they create. `InitOwnershipTarget` returns an `OwnershipStatus` and an error: a failed inspection is an error (no longer a silent no-op), a missing target is `OwnershipTargetMissing`, and root-owned or non-root cases stay inactive. Mutating CLI commands (`loadConfigStateLocked`) refuse to run as root against a missing `data_dir` and print the explicit bootstrap procedure (`fsutil.BootstrapInstructions`: create the tree owned by the named daemon account with `install -d`, or run as that account; root-only deployments create it as root first) instead of creating a root-owned tree; `serve` and read-only commands only fail on an inspection error. No account is ever inferred; config-file ownership handling is unchanged.
- **Files:** `internal/fsutil/fsutil.go`, `main.go`, `filelock_unix.go`, `internal/signer/keys.go`, `internal/fsutil/fsutil_ra6x044_049_test.go` (new), `ownership_bootstrap_ra6x044_test.go` (new)
- **Verification:** With ownership transfer active and a failing chown, `WriteFileAtomicOwned` returns a specific error, the predecessor's bytes are unchanged and no staged file remains; `CopyFile` leaves no unowned destination; with a working chown the staged file is the one chowned and the replacement becomes visible. With an injected root euid, a missing target reports `OwnershipTargetMissing` without activating transfer, an unreadable parent is an inspection error, an applicable target activates, and a non-root euid is inactive; an existing `data_dir` passes the bootstrap check and the instructions name the procedure. A real root bootstrap on a disposable host is not exercised here. Full module suite green under `-race`.

### RA6X-049 — Atomic-write helpers silently discard the durability result — FIXED
- **Change (Golang-tudor-dnssec-signer):** `fsutil.SyncDir` returns its outcome; a filesystem that does not support directory fsync at all (EINVAL/ENOTSUP/EOPNOTSUPP) is classified as such (warned once) and treated as success, while any other failure is returned. `WriteFileAtomicOwned`, `WriteConfigFileAtomic` and `CopyFile` return a `*fsutil.DurabilityError` when the rename succeeded but the directory sync failed — distinguishable via `fsutil.IsCommitted` from a pre-rename failure — and the signed-zone writer does the same. Callers reconcile with the visible generation instead of rolling back a possibly committed file: `SignZone` keeps the written zone and records a durability warning on the zone; `SaveKeyFiles` keeps the written key files; `StartKSKRollover`/`StartAlgorithmRollover` keep the rotated keys and the saved rollover record (the key rollback runs only for a pre-rename save failure); the daemon logs a committed-but-unsynced state save as a warning; CLI state saves (`persistState`) and config edits (`commitConfigEdit`) treat a committed write as success with a warning so no add/remove/import rollback deletes files that are actually in place. Atomic replacement, file formats and predecessor backups are unchanged.
- **Files:** `internal/fsutil/fsutil.go`, `internal/signer/sign.go`, `internal/signer/keys.go`, `internal/signer/rollover.go`, `daemon.go`, `main.go`, `registrar_cli.go`, `internal/fsutil/fsutil_ra6x044_049_test.go`
- **Verification:** `DurabilityError` is detected by `IsCommitted` and unwraps its cause while an ordinary error is not "committed"; EINVAL/ENOTSUP/EOPNOTSUPP classify as unsupported and EIO/EACCES/EROFS do not; `SyncDir` reports an unopenable directory and succeeds on a normal one. Directory-fsync fault injection after activation and crash/recovery on the deployment filesystems are not reproducible in this environment and remain the review's stated external verification. Full module suite green under `-race`.

### RA6X-025 — State merging loses CLI warnings and resurrects removed zones despite the process lock — FIXED
- **Change (Golang-tudor-dnssec-signer):** Under the process lock the disk is now authoritative: each signing cycle adopts the validated on-disk state wholesale (`State.ReplaceFromDisk`) before mutating anything, so every CLI mutation committed since the daemon's last save — warnings added or cleared, removals, phase metadata that did not sign — is preserved without heuristics. Only when the previous cycle failed to persist its results does the daemon fall back to the existing merge (`ReloadFromDisk`), so newer unsaved signing results are never replaced by older disk data (`lastSaveFailed`). Removal is a persisted deletion marker (`State.Removed`, additive `removed` field; `MarkRemoved`/`ClearRemoved`/`RemovedAt`): `remove` records it, `add`/`import` clear it, the merge adopts disk markers and drops a marker once the zone is signed again after it. Configurations carry `LoadedAt` (set by `LoadConfig`, not a TOML key); the daemon skips initializing a configured zone whose marker is newer than the configuration it holds (a stale pre-SIGHUP entry) and re-initializes it — clearing the marker — when a configuration loaded after the removal still lists it (the operator re-added it). Single-writer behaviour, state readability and unrelated zones are unchanged.
- **Files:** `internal/state/state.go`, `internal/config/config.go`, `daemon.go`, `main.go`, `state_merge_ra6x025_test.go` (new)
- **Verification:** Interleaving daemon cycles with a CLI process that loads/saves under the lock: an urgent registrar warning added by the CLI survives the daemon's save and appears in its memory; a warning cleared by the CLI stays cleared; rollover metadata changed with equal `LastSigned` persists; a zone removed while the daemon still holds the old configuration is not resurrected (no zone, marker persisted, dropped from memory); a configuration loaded after the removal that still lists the zone makes the daemon manage and sign it again and clears the marker; after a save failure the next cycle keeps the newer in-memory signing results and a successful save clears the flag. Existing reload/merge tests pass. `go test -race -count=1 ./...` green.


### RA6X-002 — Key rotation and rollover state are not one recoverable transaction — FIXED
- **Change (Golang-tudor-dnssec-signer):** Key files are now a two-slot transaction (`internal/signer/keytx.go`): every generation is staged at its tag-named slot (`<domain>.<role>.<tag>.{key,private}`, atomic + durable writes, refused on a tag collision), verified by reloading it exactly as signing would (`StageKeyPair`), and only afterwards copied over the live slot (`ActivateKeyPair`, `.private` before `.key`, idempotent, live predecessor preserved first). `StartKSKRollover`, `startZSKRollover` and `StartAlgorithmRollover` verify the old generation's backup, stage the replacement(s) without touching the live pair, save the rollover record naming both generations, and activate last; a save failure reverts memory with nothing live changed, and an activation failure leaves the durable record for recovery and tells the operator to run `sign`. Both algorithm-rollover pairs are staged before the single record is saved, so a ZSK failure after the KSK leaves the old generation live and unrecorded. Signing derives the key identity each live slot must hold from the persisted state (`expectedLiveKeyTags`: `KeyState.ID`, or `Rollover.NewKeyID` for the pre-published ZSK) and loads through `EnsureLiveKey`, which re-activates the recorded generation from its tag-named copy when the live pair differs or does not verify and fails closed otherwise; the ZSK phase switch uses the same check. Keys loaded by ID must carry the tag their file name claims (`checkKeyTag`). `SaveKeyFiles` (add/import/generation) runs the same stage-then-activate path. No new state fields; older key directories gain the tag-named copy on first use (documented in the signer `CLAUDE.md`).
- **Files:** `internal/signer/keytx.go` (new), `internal/signer/keys.go`, `internal/signer/rollover.go`, `internal/signer/sign.go`, `CLAUDE.md`, `internal/signer/keytx_ra6x002_023_030_test.go` (new)
- **Verification:** Fault injection at the backup, staging, state-save, activation and mid-activation (private copied, public not) steps of a KSK rollover start, followed by a restart from the persisted state: before the save the restart publishes exactly the old KSK+ZSK, after it exactly old KSK + new KSK + ZSK with the DNSKEY RRset signed by both KSKs and the live slot holding the recorded key; an algorithm rollover whose ZSK staging fails after the KSK leaves the old set live and unrecorded, and one whose ZSK activation fails after the record is repaired on restart to all four keys with both algorithms signing; a ZSK rollover whose persistence fails leaves the live ZSK untouched, and a crash between record and activation restarts into the pre-publish set (old signs, both published); a mixed live pair is repaired from the recorded generation; a live key the state does not name is refused with the prior signed zone kept; by-ID loads reject tag mismatches. Process termination was modelled by fault injection plus restart from disk rather than a killed subprocess. `go test -race -count=1 ./...` green.

### RA6X-023 — An existing truncated private-key backup is accepted before the live key is overwritten — FIXED
- **Change (Golang-tudor-dnssec-signer):** `ensureVerifiedBackup` replaces both backup paths (`RolloverManager.BackupKey` for rollover starts, `preserveLivePair` for the implicit backup in `SaveKeyFiles`/generation/import): the live pair must load and verify; the tag-named backup is checked as a complete pair — both halves present and parseable, owner/role/algorithm validated, private half cryptographically corresponding to the live public key — before any live overwrite. A missing, empty, truncated, mismatched or unparseable private half is set aside as `<backup>.private.invalid.N` and rewritten atomically and durably from the verified live pair, then re-verified; an unreadable half is an error that aborts before replacement; a slot holding a different key (tag collision) is never overwritten — the rollover refuses, and generation preserves the displaced live pair under a unique name. `BackupKey` also refuses a live key whose tag is not the one the state records. Backup copies go through the staged, fsynced, dir-synced `CopyFile`, and durability failures on backups are hard errors.
- **Files:** `internal/signer/keytx.go`, `internal/signer/keys.go`, `internal/signer/rollover.go`, `internal/signer/keytx_ra6x002_023_030_test.go`
- **Verification:** Missing, zero-length, truncated, mismatched (another key's private file) and garbage private backups are each repaired with the damaged bytes preserved, the live pair untouched, the repaired backup loadable by ID, and the rollover then signing with both generations; the review's probe (valid backup, private half truncated to nothing, rollover start) now starts and signs with the old KSK; an unreadable private backup aborts the rollover with nothing recorded and the live pair intact; the implicit backup during generation repairs an empty half before the overwrite; a tag slot holding a different key is left byte-for-byte intact while the live pair is preserved as `<tag>.0.*` and the direct backup refuses. Interruption during the private copy cannot leave a partial destination (temp + rename); the truncated case models a partial file from any other source. `go test -race -count=1 ./...` green.

### RA6X-030 — Expanded Ed25519 private-key validation does not verify the seed against its public half — FIXED
- **Change (Golang-tudor-dnssec-signer):** `VerifyKeyPairCorrespondence` derives the expanded Ed25519 key from the 32-byte seed for the 64-byte form and rejects the pair when the stored public suffix differs from the derived one, then compares the derived public key with the DNSKEY; 32-byte seeds and valid 64-byte keys remain accepted, other lengths rejected. ECDSA correspondence now validates the private scalar range `[1, n-1]` before deriving the point. `signRRSIG` derives the signing key from the seed for the 64-byte form so the suffix can never be used directly. The check runs at every load and in `StageKeyPair`, so malformed material is refused before any file is written (import validation is covered under RA6X-024).
- **Files:** `internal/signer/keys.go`, `internal/signer/sign.go`, `internal/signer/keytx_ra6x002_023_030_test.go`
- **Verification:** Valid seed and expanded forms verify, sign and validate; seed-corrupted-with-intact-suffix, suffix-corrupted, 32-byte-seed-corrupted, public-half-corrupted, 31/33/63/65-byte and empty inputs are rejected; the review's probe (flipped seed byte, intact suffix) is rejected and its signature no longer verifies under the advertised key; for P-256 and P-384 the zero scalar, `n`, `n+1`, all-0xff, flipped, truncated, oversized and public-flipped forms are rejected while the valid scalar passes; staging a non-corresponding pair writes no file and `SaveKeyFiles` leaves the live key untouched. `go test -race -count=1 ./...` green.

### RA6X-024 — Import mutates live keys/output before validating ownership and does not restore prior artifacts on failure — FIXED
- **Change (Golang-tudor-dnssec-signer):** `runImport` is a staged, fully reversible transaction. Before any write, both keys pass `signerpkg.ValidateKeyForImport` (canonical owner for the zone, protocol 3, supported algorithm, role flags, private/public correspondence with the RA6X-030 rules, and a real sign-then-verify probe) plus the existing same-algorithm and config-append preflight checks. Staging then writes both pairs to their tag-named slots (`StageKeyPair`, verified by reload, never over a different key) and signs the complete zone with exactly those keys into a staging file beside the output (`Signer.StageZoneWithKeys`, built on a `SignZone` split into `prepareSignedZone`/`recordSignedZone`); the live key slots and the served output are untouched until every artifact exists. Commit runs activate keys → publish output (same-directory rename) → record zone in state → append config, each step recorded in an `importTx` that snapshotted the prior live key files (bytes and modes), the prior output and the removal marker. Any failure restores the exact prior bytes and modes of every replaced file, re-installs the removal marker with its original time, removes the zone from state (persisting the undo only if the registration had been persisted), discards an unpublished staging file, and empties a previously empty live slot only when its halves are byte-identical to the staged tag-named copies; a pre-existing signed output is never deleted, and a rollback that cannot complete is reported in the error. Staged tag-named copies are kept (key material is never deleted) and a retry reuses them. Import syntax, BIND input forms and unrelated zones are unchanged. `State.RestoreRemoved` re-establishes a marker with its original timestamp.
- **Files:** `main.go`, `internal/signer/sign.go`, `internal/signer/keytx.go`, `internal/state/state.go`, `import_tx_ra6x024_test.go` (new)
- **Verification:** Importing a valid pair generated for another zone is refused at the owner check with the keys directory, output, state and config unchanged; a 64-byte Ed25519 key with a corrupted seed is refused at load with nothing written. With pre-existing live keys, a served output and a removal marker present, faults injected at KSK staging, ZSK staging, signing, KSK activation, ZSK activation, publish, state save and config append each leave the prior live pairs byte-identical and loadable, the served output intact, no staging file, the config untouched, the state without the zone and the marker restored with its original time; a retry after every interruption then publishes exactly the imported DNSKEY pair, records it in state, clears the marker and lists the zone in the config. A fresh import failing at the config append removes only the output it created, empties the live slots it filled while keeping the staged copies, and retries cleanly. Process termination was modelled by fault injection at each boundary plus retry rather than a killed subprocess. `go test -race -count=1 ./...` green.

### RA6X-038 — Config add/remove operations can turn valid TOML into a corrupted configuration — FIXED
- **Change (Golang-tudor-dnssec-signer):** Zone-table editing moved to `internal/config/config_edit.go`. Table headers are recognized only at lines that begin outside a multi-line string: `tomlHeaderPositions` scans each line with a small TOML lexer (`scanTOMLLine`) that tracks triple-quote basic and triple-apostrophe literal string context across lines, honours backslash escapes and line-ending backslashes in basic strings, the up-to-two extra closing quotes, single-line strings and comments. Headers are parsed with the TOML key grammar (`parseTableHeader`: whitespace inside the brackets and around dots, bare keys, basic keys with escapes, literal keys, array-of-tables, trailing comment), so `IsZoneTableHeader` matches every spelling of `zones.<domain>` and `isTOMLTableHeader` bounds the removal at the next real header. New values are serialized with `tomlBasicString` (TOML escapes, `\uXXXX` for control characters, invalid UTF-8 refused) instead of Go `%q`. Before the atomic replacement, `validateZoneEdit` parses the original and the candidate as generic TOML, detaches `zones.<domain>` from both, requires every other table/key/value to be identical, requires the zone to be exactly `{path}` after an add and absent after a remove, and requires a configuration that loads under the strict `LoadConfig` rules (now shared via `ParseConfig`/`decodeConfig`) to still load afterwards; any failure returns an error with the original bytes untouched. Ownership, mode, CLI syntax and the comment-preserving line-based removal are unchanged. Because whitespace inside the brackets is now rewritable, the root test for an unlocatable header uses a header-less dotted-key definition, the one remaining shape `remove` must still report.
- **Files:** `internal/config/config_edit.go` (new), `internal/config/config.go`, `internal/config/config_edit_ra6x038_test.go` (new), `verification_fixes1_test.go`
- **Verification:** Both review probes pass: removing a zone whose header text also appears inside a multi-line literal hook string deletes only the real table, leaves valid TOML and the hook value unchanged; adding a path containing a BEL byte writes `\u0007` and reloads to the same path (also quotes, backslashes, tab/newline/CR, non-ASCII, DEL). A multi-line basic hook string with an escaped delimiter, an inner array-of-tables line and a four-quote terminator is preserved; commented headers are ignored; `[ zones . "d" ]`, `[zones.'d']`, `[ zones.'d' ] # c`, tab-indented and backslash-u-escaped keys are all located; an escaped Windows path in a sibling zone survives byte-for-byte; a final table without a trailing newline removes and adds cleanly; unknown `[[future.items]]` tables survive an edit; a zone that would remain configured via a sub-table is refused with the file intact; unparseable candidates, candidates that drop or add another zone, change a setting, keep the zone or record it differently are all refused. Existing header/removal/mode tests pass. `go test -race -count=1 ./...` green.

### RA6X-028 — Equivalent escaped DNS owner names are signed as different RRsets — FIXED
- **Change (Golang-tudor-dnssec-signer):** One canonical wire-label identity (`canonicalName` in `internal/signer/zonemodel.go`: every label decoded to its wire octets with decimal and single-character escapes resolved, ASCII case folded, re-encoded so only dots, backslashes and non-printable octets are escaped) now keys every owner comparison: RRset formation for signing and verification (`groupRRsets`), signatures computed over canonical-owner copies of the complete semantic RRset (`canonicalRRset`, so `www`, `WWW` and `\119ww` sign as one RRset while records keep their original spelling in the output), delegation/occlusion/DNAME checks, empty non-terminals, NSEC owners/next pointers and NSEC3 hashing inputs, apex identity in DNSSEC stripping and output ordering, in-zone checks by label comparison (`labelsWithin`, so an escaped dot never matches a label separator), and the wildcard label count in `createRRSIG`. Distinct binary labels (`a\.b` vs `a.b`, `x\000y` vs `xy`) stay distinct; RDATA is never touched. All of this is built on a per-snapshot `zoneModel` (see RA6X-053).
- **Files:** `internal/signer/zonemodel.go` (new), `internal/signer/sign.go`, `internal/signer/zone_semantics_ra6x028_029_056_053_001_test.go` (new)
- **Verification:** A zone with `www`, `\119ww` and `WWW` A records, an escaped-asterisk wildcard, `*.wild`, `a\.b` beside `a.b`, `x\000y` beside `xy`, a mixed-case delegation and glue under its escaped spelling signs in both NSEC and NSEC3 modes; an independent wire-normalized parser (owners packed with `dns.PackDomainName`, RRsets regrouped, signatures verified over normalized copies) finds exactly one three-record `www` A RRset with a verifying RRSIG, two distinct escaped-dot owners, two distinct binary-label owners, unsigned delegation NS and glue, wildcard RRSIG label counts of 2 and 3, and exactly one NSEC/NSEC3 owner for `www`. `canonicalName` unit cases cover case, decimal escapes, escaped dots, NUL bytes, spaces, backslashes and asterisks. `go test -race -count=1 ./...` green.

### RA6X-029 — Zone validation accepts CNAME/data conflicts and self-verifies the invalid zone — FIXED
- **Change (Golang-tudor-dnssec-signer):** `validateZoneRecords` (shared by `ValidateZoneFile` for `add`/`import` and by every signing path) now validates canonical owner/type constraints before any output is written: a CNAME must be the only CNAME at its owner and must not coexist with other data apart from DNSSEC metadata (RRSIG/NSEC/NSEC3, so a previously signed file fed as input still validates); a CNAME at the apex is rejected explicitly; a DNAME is singular, may not coexist with a CNAME, and nothing may exist beneath it (RFC 6672 §2.4, via `zoneModel.occludingDNAME`); a delegation point may hold only NS, DS and glue A/AAAA (plus DNSSEC metadata); DS is allowed only at a delegation point. Existing SOA/NS/in-zone rules are kept, all on canonical identities so an escaped spelling cannot evade a check.
- **Files:** `internal/signer/sign.go`, `internal/signer/zonemodel.go`, `internal/signer/zone_semantics_ra6x028_029_056_053_001_test.go`
- **Verification:** CNAME+A, CNAME+MX, two CNAME targets, CNAME+A under different spellings, apex CNAME, DNAME+CNAME, data beneath a DNAME, MX at a delegation and DS without a delegation are each rejected by `ValidateZoneFile` and by `SignZone` with the prior signed output and zone state byte-for-byte intact. A zone with a CNAME chain, a DNAME, a secure delegation with DS and A/AAAA glue, an insecure delegation and data under an empty non-terminal validates, signs in both modes, passes the independent wire-normalized verifier (delegation NS and glue unsigned, DS signed), and its signed output validates again as input. No external authoritative zone checker is installed here; the independent check is the test's own wire-level parser/verifier. `go test -race -count=1 ./...` green.

### RA6X-056 — Post-sign verification does not establish denial-chain completeness — FIXED
- **Change (Golang-tudor-dnssec-signer):** `verifySignedZone` now takes the immutable input snapshot (zone data plus the DNSKEY RRset) and derives every expectation from it rather than from what the generator emitted: for NSEC mode, exactly one NSEC per authoritative owner (non-occluded owners of the snapshot; a missing chain is an error), in canonical order and closed, no NSEC at any other name, and a type bitmap equal to the data types at the owner (NS and DS only at delegations) plus NSEC and RRSIG; for NSEC3 mode, exactly one apex NSEC3PARAM carrying the configured hash/flags/iterations/salt, every NSEC3 with the same parameters and an owner of the form `<hash>.<apex>`, one NSEC3 for every authoritative owner and empty non-terminal hashed with those parameters and none other, hash order and closure, and bitmaps per RFC 5155 §7.1 (NSEC3PARAM at the apex, RRSIG only where the zone signs, empty at empty non-terminals); records of the other denial mode are rejected. The gate also checks that every input RRset survives into the output. Membership is checked before order so a missing or foreign owner is reported as such. Expectations come from the snapshot's `zoneModel` (`authoritativeOwners`, `emptyNonTerminals`, `denialTypes`), computed separately from the generators' own owner collection. `prepareSignedZone` passes the snapshot and a test-only `mutateBeforeVerify` seam lets a mutated generation reach the real publication boundary.
- **Files:** `internal/signer/sign.go`, `internal/signer/zonemodel.go`, `internal/signer/post_sign_verify_test.go`, `internal/signer/verification_fixes3_test.go`, `internal/signer/zone_semantics_ra6x028_029_056_053_001_test.go`
- **Verification:** Mutations of a correctly signed fixture (re-signed after mutation so only chain semantics are under test) are all rejected: whole NSEC or NSEC3 chain removed, an owner omitted with the ring re-closed (both modes), wrong and missing bitmap types (including DS lost and glue leaked at a delegation), an extra NSEC at an empty non-terminal, an out-of-zone NSEC owner, an NSEC3 renamed to a wrong hash, NSEC3 iteration mismatch, NSEC3PARAM salt mismatch, an empty non-terminal's NSEC3 dropped, and NSEC records inside an NSEC3 zone; valid fixtures with an empty non-terminal and a secure delegation pass. A generator that drops the chain, driven through `SignZone` in both modes, fails at the post-sign gate and leaves the previous file and state untouched. The independent zone verifier of RA6X-028 is the test-side comparison; no external DNSSEC zone verifier is available in this environment. `go test -race -count=1 ./...` green.

### RA6X-053 — Delegation occlusion checks scale quadratically with large delegated zones — FIXED
- **Change (Golang-tudor-dnssec-signer):** `delegationInfo`/`findDelegationPoints`/`isOccluded` (a full scan of every cut with a string-suffix test per lookup) are replaced by `zoneModel`: delegation and DNAME owners are indexed by canonical name once per input snapshot, and `isOccluded` looks up each proper ancestor of a name (bounded by label depth, memoized per name), so misinterpretation of escaped label separators is impossible and the work is proportional to total label depth. `prepareSignedZone` builds the model once and reuses it for chain generation, signing and self-verification; the wrapper entry points used by tests build their own. Unsigned delegation/glue semantics, signed DS/NSEC treatment and all outputs are unchanged.
- **Files:** `internal/signer/zonemodel.go`, `internal/signer/sign.go`, `internal/signer/zone_semantics_ra6x028_029_056_053_001_test.go`
- **Verification:** Nested delegations, an escaped-dot delegation beside a real-dot name, a delegation spelled with a decimal escape, and a name that is merely a string suffix of a delegation (`childish` vs `child`) all classify correctly. `BenchmarkOcclusion` for an all-non-matching pass scales linearly — 0.20 ms (1,000 × 1,000), 0.41 ms (2,000 × 2,000), 0.81 ms (4,000 × 4,000) on this machine — against the review's 21 ms / 89 ms / 353 ms quadratic baseline. Full-cycle lock-hold and shutdown latency at a deployment's capacity target were not measured here. `go test -race -count=1 ./...` green.

### RA6X-001 — Ordinary KSK rollover can unintentionally change algorithm — FIXED
- **Change (Golang-tudor-dnssec-signer):** `StartKSKRollover` stages the replacement KSK with the existing KSK's algorithm (`StageKSK(domain, oldKSK.Algorithm)`) instead of the configuration default, so only `rollover algorithm` can introduce another algorithm. `loadKeysForSigning` rejects a live key set whose members use different algorithms unless an algorithm rollover is in progress (`signingKeys.validateAlgorithms`), failing closed before any output replacement with a message that points at `rollover algorithm`. The post-sign gate enforces algorithm completeness (RFC 6840 §5.11): for every algorithm advertised in the published DNSKEY RRset, every signable RRset must carry a verifying RRSIG from a key of the appropriate role (KSK for the DNSKEY RRset, ZSK for everything else). CLI syntax, key filenames and state compatibility are unchanged; explicit dual-algorithm rollover still signs everything with both.
- **Files:** `internal/signer/rollover.go`, `internal/signer/sign.go`, `internal/signer/zone_semantics_ra6x028_029_056_053_001_test.go`
- **Verification:** ECDSA P-256 keys installed under the default ED25519 configuration: an ordinary KSK rollover mints an ECDSA KSK, signs through start and completion, and the output passes the independent verifier; after an explicit ECDSA→ED25519 algorithm rollover completed with the global configuration left on a third algorithm, the next KSK rollover stays ED25519. Injecting a ZSK of another algorithm as the live ZSK with no algorithm rollover is rejected before the output is replaced with the prior output intact; a DNSKEY of an extra algorithm that signs nothing is rejected by the post-sign gate. `go test -race -count=1 ./...` green.

### RA6X-005 — Source-path reconciliation happens too late and is absent from CLI signing — FIXED
- **Change (Golang-tudor-dnssec-signer):** Source selection is central: `Signer.SourcePath` returns the path the active configuration names (falling back to the state's path only for a zone the configuration does not list), and `SignZone`, `SignAll`, `resign`, the daemon cycle and the import staging path all sign that file. `NeedsSign` treats a configured path that differs from the one recorded in state as a forced sign ("source path changed") before any mtime/size comparison, so an older or same-size replacement is signed immediately. The state's source reference is replaced only in `recordSignedZone`, after the new source has parsed, signed, self-verified and been published; a missing or unparseable new path leaves the prior reference and the prior signed output in place and records a per-zone error. The daemon's pre-sign path rewrite is gone. Change detection, signing and hook metadata therefore always name the same file.
- **Files:** `internal/signer/sign.go`, `daemon.go`, `source_ra6x005_035_042_test.go` (new)
- **Verification:** Source A signed, then the configuration pointed at source B with an older mtime, equal size and different RR data: a daemon reload plus cycle, `SignAll` (the `sign` path) and `runResign` each publish B and record B as the source. A missing and an invalid B each leave A's output byte-for-byte, keep A as the recorded source and record the error on the zone. `go test -race -count=1 ./...` green.

### RA6X-042 — Signing has no stable-source snapshot boundary — FIXED (producer contract documented; no producer scripts exist in the repository)
- **Change (Golang-tudor-dnssec-signer):** Every signing entry point reads the unsigned zone through `readSourceSnapshot`: the file is opened without blocking (`openSourceFile`, `O_NONBLOCK` on Unix) so a FIFO cannot hang the signer while it holds the state lock; the descriptor is inspected and anything but a regular file is rejected; the bytes are read through that one descriptor, the descriptor is stat'ed before and after, and a size or mtime change during the read is refused with a message naming the atomic-replacement contract; the parsed records and the snapshot's own mtime/size become the change-detection reference. An atomic replacement during a read is harmless — the descriptor holds the complete previous generation and the next check detects the new one. The producer contract (write a temporary file, `fsync`, `rename`) and its limit (a paused in-place writer's valid SOA/NS prefix cannot be told from a complete small zone by metadata) are documented in the README and the signer `CLAUDE.md`; the repository contains no zone-producing deployment scripts to inspect, so that assumption remains the operator's to verify.
- **Files:** `internal/signer/sign.go`, `internal/signer/source_unix.go` (new), `internal/signer/source_windows.go` (new), `internal/signer/export_test_seams.go` (new), `README.md`, `CLAUDE.md`, `source_ra6x005_035_042_test.go`
- **Verification:** A FIFO source is rejected as "not a regular file" without hanging (10 s guard) and the prior output stays; an in-place rewrite between the read and the consistency check — truncation to a valid SOA/NS prefix, and a rewrite with more data — is refused and nothing is published; an atomic rename during the read signs the complete previous generation, after which the change is detected and the new generation published. A writer paused after a valid prefix before the read is the documented, undetectable case; the daemon's existing quiescence deferral still covers a file modified within the last three seconds. `go test -race -count=1 ./...` green.

### RA6X-035 — Missing signed output does not trigger regeneration while state still looks fresh — FIXED
- **Change (Golang-tudor-dnssec-signer):** `NeedsSign` checks the published output of every previously signed zone (`outputUnusable`): a missing, unreadable or non-regular `<output_dir>/<domain>.zone.signed` requires signing ("signed output missing" / "unreadable" / "not a regular file"), judged on presence and file type only, never on the output's mtime, and only when the zone has been signed before, so steady-state zones are not re-signed every poll. `Daemon.Reload` reconciles a changed `output_dir` immediately (creates it or logs the failure); because the output is then missing at the new destination, every zone is regenerated there on the next cycle while the previous directory's files are left for whatever still serves them. A destination that cannot be written fails the zone visibly (error recorded on the zone, which readiness reports) with the previously served file intact. The synthetic `TestNeedsSign` fixture now creates the output it claims to have signed.
- **Files:** `internal/signer/sign.go`, `daemon.go`, `internal/signer/dnssec_test.go`, `source_ra6x005_035_042_test.go`
- **Verification:** Removing a signed zone's output with source and state unchanged makes `NeedsSign` report it and the next cycle restores it with the same data, after which the zone is idle again; a reload with a new `output_dir` regenerates the output there on the next cycle and leaves the old file; an unwritable new destination fails the cycle, records the error on the zone and keeps the prior file. `go test -race -count=1 ./...` green.

### RA6X-034 — Hook timeouts do not bound descendants holding inherited stderr pipes — FIXED
- **Change (Golang-tudor-dnssec-signer):** All hook execution goes through one bounded runner (`runHook` in `hooks.go`): the command runs under `hookTimeout` (30 s), `cmd.WaitDelay` (`hookWaitDelay`, 5 s) bounds the wait for the stdout/stderr pipes after cancellation or exit so a descendant that inherited them cannot block `Run`, and on Unix the hook starts in its own process group with a `Cancel` that sends SIGTERM to the whole group and SIGKILL to it after `hookKillGrace` (2 s) (`hooks_unix.go`; Windows keeps the default direct kill). The stderr prefix capture, hook environment, exec/shell selection and successful long-but-within-budget hooks are unchanged; a timed-out hook is logged as such. A descendant that leaves the group by starting its own session is outside the guarantee, documented in the README.
- **Files:** `hooks.go`, `hooks_unix.go` (new), `hooks_windows.go` (new), `README.md`, `hooks_ra6x034_043_test.go` (new), `lifecycle_test.go`
- **Verification:** With the timing variables shortened, a shell-style hook, an exec-style hook and a hook whose child ignores SIGTERM each launch `sleep 30 &` (inheriting stderr) and `wait`: the caller returns within timeout + wait delay, reports a timeout, and the recorded child pid is gone within three seconds; a hook within budget succeeds and a failing hook still returns its stderr prefix. Verified on macOS (darwin/arm64) only; FreeBSD/Linux use the same POSIX process-group calls but were not exercised here. `go test -race -count=1 ./...` green.

### RA6X-043 — CLI signing does not honor the daemon's per-zone/batch hook contract — FIXED
- **Change (Golang-tudor-dnssec-signer):** One function, `firePostSignHooks`, implements the hook contract for the daemon (asynchronous, tracked on the wait group) and the CLI (synchronous, first failure returned): given the zones a pass signed successfully, it runs one batch invocation with `DNSSEC_DOMAINS`/`DNSSEC_BATCH_SIZE`/`DNSSEC_OUTPUT_DIR` when `coalesce_post_sign` is on (a single zone included), else one per-zone invocation per zone with its exact source and output paths; nothing runs when nothing signed; every inherited `DNSSEC_*` variable is stripped from the hook environment so opposite-mode or stale values cannot mislead a script. `Signer.SignAllReport` returns the zones that signed so `sign` hands exactly those to the hook and exits non-zero when the hook fails (the zones stay signed); `resign`, `add`, `import` and the rollover commands go through the same contract via `runPostSignHook`; the daemon's per-zone and batch paths call the same function. The README documents the parity.
- **Files:** `hooks.go`, `main.go`, `daemon.go`, `internal/signer/sign.go`, `README.md`, `hooks_ra6x034_043_test.go`, `lifecycle_test.go`
- **Verification:** The real `runSign`, `runResign`, `runAdd`, `runImport` and `runRolloverStart` handlers run against a config whose hook records every `DNSSEC_*` variable, with stale `DNSSEC_DOMAIN`/`DNSSEC_DOMAINS`/`DNSSEC_BATCH_SIZE` set in the process environment: `sign` with three zones yields three per-zone invocations with exact paths (or one batch invocation naming all three), a mixed run with one invalid zone hands only the two that signed, a run where nothing signs runs no hook; each single-zone command yields exactly one per-zone or one single-domain batch invocation; batch invocations never carry `DNSSEC_DOMAIN` and per-zone ones never carry the batch variables; a failing hook makes `sign` exit non-zero with the zone still signed; all invocations complete before the handler returns. `go test -race -count=1 ./...` green.

### RA6X-031 — A lost DELETE response can strand the registrar with no DS and bypass emergency recovery — FIXED
- **Change (Golang-tudor-dnssec-signer):** The Dynadot request core classifies every failure that occurs after a request may have reached the registrar (transport error, timeout, cancellation in flight, unreadable or non-JSON body, HTTP or envelope error status) as `dispatchedError`. `ReplaceDS` treats the DELETE accordingly: only a failure that provably never sent the request returns early (the desired records were already published additively); any dispatched failure or success proceeds to recovery on a context detached from the caller with a fresh two-minute budget and recovery priority at the rate limiter. Recovery PUTs the desired records and then reads the DS set back, and succeeds only when every desired record is verified present; an unverifiable set (restore failing, read-back failing, or a record still absent after the attempts) returns an error unwrapping to `ErrRegistrarDSEmpty` so callers persist the sticky `URGENT:` remediation warning; a set that holds the desired records plus survivors of a DELETE that did not apply is reported as an ordinary error and never "fixed" by another DELETE. An explicit `registrar clear` with an uncertain DELETE is reconciled by read-back (empty = success, otherwise reported). `base_url` (documented) lets the Dynadot endpoint be pointed at a proxy or test double. The existing CLI/auto-publish handling of the sentinel (sticky warning, remediation log, non-zero exit) is unchanged.
- **Files:** `internal/registrar/registrar_dynadot.go`, `internal/config/config.go`, `CLAUDE.md`, `internal/registrar/registrar_ra6x031_051_test.go` (new), `internal/registrar/registrar_test.go` (read-back fixtures), `registrar_recovery_ra6x031_test.go` (new)
- **Verification:** Against a simulated DS store: a DELETE that applies and then drops the connection, times out, returns a non-JSON body, or returns HTTP 500 each ends with the desired DS present; a DELETE that dropped without applying leaves the old record and reports it as an ordinary "extra records" error; a caller cancelled mid-DELETE still gets the restore on the detached context; asynchronous acceptance (record visible only after later GETs) is waited out by the read-back; a permanently failing restore and a failing read-back both unwrap to `ErrRegistrarDSEmpty`; a DELETE that was never dispatched is an ordinary failure with nothing cleared; explicit clear is confirmed or reported by read-back. Through the real `registrar push` command with a `base_url` test double: a lost DELETE plus failing restore persists the sticky warning in `state.json`, and a second push from a fresh process after the registrar heals restores exactly the live KSK's DS and clears the warning. `go test -race -count=1 ./...` green.

### RA6X-051 — Registrar throttling does not enforce its stated quota or cancellation contract — FIXED
- **Change (Golang-tudor-dnssec-signer):** `slidingLimiter` is replaced by `windowLimiter`: explicit rolling-window accounting (admission timestamps pruned at 60 s) that admits at most 60 requests in any rolling minute, with 10 of those reserved for recovery work (contexts marked `withRecoveryPriority`, used by `ReplaceDS`'s restore and read-back), on top of the existing burst tiers now counted per window (100 ms / 500 ms / 2 s gaps). `gate` checks cancellation before admission even when no delay is due, computes the wait under the mutex but waits outside it (so a queued caller observes its own deadline and never sits behind another caller's timer), re-evaluates after waking, and never records a slot for a cancelled caller. Every HTTP attempt goes through `do`, so retries and recovery are all counted. The clock and wait are injectable. The process-wide sharing and the `Registrar` interface are unchanged; the documentation states that separate processes sharing one account need external accounting.
- **Files:** `internal/registrar/registrar_ratelimit.go`, `internal/registrar/registrar_dynadot.go`, `internal/registrar/registrar_ratelimit_test.go`, `internal/registrar/registrar_ra6x031_051_test.go`, `CLAUDE.md`
- **Verification:** With an injected clock, 400 consecutive ordinary admissions never exceed 50 in any rolling 60 s window while the first 20 keep the 100 ms gap and later ones the 500 ms tier; across idle resets and an exhausted ordinary budget, recovery-priority callers are admitted from the reserve without waiting a minute and the combined total never exceeds 60 per rolling window. An already-cancelled caller with no delay due returns `context.Canceled` without consuming a slot; a caller with a 10 ms deadline queued behind the 2 s tier returns within 200 ms without consuming a slot; a caller queued behind another waiter observes its own deadline within 60 ms. A `ReplaceDS` with a failing restore issues exactly as many limiter admissions as HTTP requests the server received. The actual account-wide Dynadot limit was not confirmed against the live API. `go test -race -count=1 ./...` green.

### RA6X-004 — Successful file signing is mistaken for successful authoritative publication — FIXED
- **Change (Golang-tudor-dnssec-signer):** Output generation and deployment are tracked separately. `ZoneState` gains `pending_publication`, `published_at` and `published_generation_signed_at` (additive, documented); `SignZone` marks a generation pending unless the deployment model says the write is the publication. `dnssec.publication` selects that model: `hook` (default with a post-sign hook) confirms a generation when the hook that deployed it succeeds — the daemon's hook completion callback records the confirmation under the cross-process state lock and persists it, and CLI commands confirm after their synchronous hook; `immediate` (default without a hook) is the explicit documented assumption that the nameserver reads `output_dir`; `probe` confirms only when every authoritative server of the zone answers SOA with the published serial (`Validator.ProbePublishedSerial`, all servers resolved; requires `serial_policy = "epoch"`, enforced at config load). A failed or timed-out hook leaves the zone pending and the daemon re-runs the deployment hook every cycle — and after a restart, since the flag is persisted — without re-signing (per-zone or as part of the coalesced batch); pending zones in probe mode are probed each cycle. Every ZSK phase gate now requires a generation signed after the phase start to be *confirmed* (`PublishedSince`) and measures its floor from `PublishedAt`; the CLI holds back DS automation (`add`, `rollover start`, `rollover complete`, `rollover algorithm`) when the hook failed. Signing of unrelated zones and previously served output are untouched; hook environment and manual deployment are preserved.
- **Files:** `internal/config/config.go`, `internal/state/state.go`, `internal/signer/sign.go`, `internal/signer/rollover.go`, `internal/validate/parentds.go` (new), `hooks.go`, `daemon.go`, `main.go`, `CLAUDE.md`, `README.md`, `publication_ra6x004_027_003_test.go` (new), `internal/validate/parentds_test.go` (new), `rollover_phase5_test.go`, `verification_fixes2_test.go`
- **Verification:** With the reload hook failing for longer than both configured rollover windows, daemon cycles keep writing the file but the zone stays pending, `published_at` stays unset and the ZSK rollover stays in pre-publish; after the hook is healed the pending deployment is retried without re-signing, confirmed asynchronously, and the phase advances only after the cache-safe wait from the confirmed publication. A pending deployment persisted by a failing hook is retried and confirmed by a fresh daemon over the same state, and the confirmation is persisted. The all-servers serial probe confirms only when every authoritative server serves the published serial (hermetic two-server lab). `go test -race -count=1 ./...` green.

### RA6X-027 — Lowering TTL immediately before rollover discards still-live cache lifetimes — FIXED
- **Change (Golang-tudor-dnssec-signer):** `ZoneState.ConfirmPublication` raises persisted cache horizons (`dnskey_cache_horizon`, `rrsig_cache_horizon`) at every confirmed publication to confirmation time plus the *previously served* generation's TTLs (`served_dnskey_ttl`, `served_max_rrsig_ttl`), never lowering them, so a re-sign with a lower TTL keeps the longer lifetime of the RRsets resolvers already hold. `SignZone` records the actual TTLs of each generation (the configured TTL stays on the wire) and the signature-lifetime figure now includes generated denial records and the DNSKEY RRset. Each ZSK phase snapshots `phase_horizon` = max(cache horizons, confirmed publication + floor) when its key set is first confirmed served and advances only after it; re-signs of the same set do not move it. All fields persist across restarts; old state without them falls back to the previous floors.
- **Files:** `internal/state/state.go`, `internal/signer/sign.go`, `internal/signer/rollover.go`, `CLAUDE.md`, `README.md`, `publication_ra6x004_027_003_test.go`, `verification_fixes2_test.go`, `rollover_phase5_test.go`
- **Verification:** A zone served with a seven-day DNSKEY TTL, then re-signed immediately with a one-hour TTL, keeps a horizon more than six days out through repeated TTL increases and decreases; a ZSK rollover started afterwards snapshots that horizon into its phase and does not advance after the one-hour floor; the horizons and the phase wait survive a state reload. The review's probe (seven-day TTL → one hour → immediate re-sign) no longer loses the cache horizon. A live validating-resolver cache was not exercised; the behaviour is asserted on the persisted horizons and phase gates. `go test -race -count=1 ./...` green.

### RA6X-003 — KSK/algorithm completion does not wait out cached parent DS records — FIXED
- **Change (Golang-tudor-dnssec-signer):** `rollover complete` no longer retires anything. It probes every authoritative server of the parent (`Validator.ProbeParentDS`: all NS resolved over IPv4/IPv6, DS matched per key with either digest, largest DS TTL reported, any unreachable server fails the probe), requires the new DS present on all of them, and records `ds_observed_at`/`parent_ds_ttl` in the rollover; `--force` records now with `dnssec.parent_ds_ttl` (default 24h). New persisted phases carry the retirement: KSK `ds_add_wait → ds_propagation_wait → retiring`; algorithm `algo_ds_add_wait → algo_ds_propagation_wait → algo_old_ds_removal_wait → algo_retiring` (RFC 6781 §4.1.4: after the DS TTL the operator removes the old DS, the daemon observes it absent at every parent server, waits another DS TTL, then drops the old algorithm). `CheckKSKRollover`/`CheckAlgorithmRollover` (daemon cycle and `sign`) advance on absolute persisted times that a later TTL change cannot shorten, and end the rollover only once the retiring generation is confirmed served (RA6X-004). The signing key selection keeps both KSKs / both algorithms through every waiting phase; state validation accepts the new phases with the live keys unchanged; `Status()` reports automatic waits as `warning` and operator phases as `action_required`; `rollover complete` refuses during automatic phases. Old in-progress `ds_add_wait` state is untouched by the migration (it simply continues under the new sequence); removal of the old DS at the registrar remains part of `rollover complete` (safe once a served KSK matches the new DS).
- **Files:** `internal/state/state.go`, `internal/signer/rollover.go`, `internal/signer/sign.go`, `internal/validate/parentds.go`, `internal/validate/validate.go` (auth port seam), `main.go`, `daemon.go`, `CLAUDE.md`, `README.md`, `publication_ra6x004_027_003_test.go`, `internal/validate/parentds_test.go`, `internal/signer/dnssec_test.go`, `internal/signer/zone_semantics_ra6x028_029_056_053_001_test.go`, `rollover_phase5_test.go`
- **Verification:** Two resolver caches are modelled: one holding the old-only DS set fetched just before the new DS appeared (DS TTL 2 s) and one fetching fresh; through a restart mid-wait and a configuration TTL lowered to a millisecond, every signed generation before the cache expiry serves both KSKs, the old KSK is retired only after the persisted wait, and the rollover ends once the retired generation is served. An algorithm rollover keeps both algorithms while any parent server still serves the old DS, records the removal only when every server lacks it, retires one DS TTL later, and ends once served. `rollover complete` falls back to `parent_ds_ttl` when the TTL is unobservable, refuses during automatic phases, and automatic waits are not `action_required`. The all-parents probe (hermetic two-server lab on 127.0.0.1 and ::1) reports present-on-all/absent-on-all only when the servers agree, the largest TTL, and fails on an unreachable server. Real validating resolvers were not run. `go test -race -count=1 ./...` green.

### RA6X-032 — The signer's live validation can pass invalid signatures and understate a broken DS chain — FIXED
- **Change (Golang-tudor-dnssec-signer):** `evalRRSIGCover` now takes the expected key material instead of a tag and verifies each candidate RRSIG cryptographically (`rrsig.Verify`) over the complete served RRset bound to one owner, requiring the RRSIG's owner and signer name to be that owner and the key's tag *and* algorithm to match; a present in-window signature that does not verify is reported as `broken`, expired ones as before, and one valid signature still supersedes any other (RFC 4035 §5.3.3). `checkRRSIGPresent` verifies the SOA against the local ZSK material (plus the old ZSK during a ZSK/algorithm rollover) and the DNSKEY RRset against the local KSK material (plus the old KSK during a KSK/algorithm rollover), falling back to the served DNSKEY RRset only when no local key can be loaded; `RRSIGCheckResult` gains `verified` and `broken`, and a missing required signature is `broken` too. `checkDNSKEYVisible` compares served keys by complete material (owner, flags, protocol, algorithm, public key) with the local KSK/ZSK, tags only as the no-material fallback (`material_matched`). The overall verdict fails — and the new structured `servfail` flag is set — when a DS is present at the parent and the chain is proven broken: a DS matching none of our keys, a failed DNSKEY check, or a failed/broken RRSIG check; the refresh-window notice stays `partial`, and an unreachable local resolver stays `error` with the new `resolver_outage` flag. The static dashboard (`dnssecstatus/index.html`) reads `servfail`/`resolver_outage` instead of matching error text, listing resolver outages separately as not assessed. Existing JSON fields and CLI flags are unchanged.
- **Files:** `internal/validate/validate.go`, `dnssecstatus/index.html`, `internal/validate/validate_ra6x032_test.go` (new), `internal/validate/evalcover_test.go`, `internal/validate/validate_verdict_test.go`, `internal/validate/validate_test.go`
- **Verification:** Against a hermetic authoritative server serving a really signed zone with a matching parent DS: a valid zone passes with verified/material-matched evidence; a corrupt SOA signature with the right tag and window fails with `servfail`; an empty DNSKEY signature fails; a served key sharing our role but not our material is not found and a SOA signed only by it fails; a parent DS authenticating none of our keys fails instead of partial; exactly one missing required signature fails; a valid dual-signature KSK rollover (old and new KSK signing, parent holding the old DS) passes; signatures inside the refresh window stay partial; a resolver outage is `error` with `resolver_outage` and no `servfail`. Unit cases cover corrupt/empty signatures, signatures over other data, foreign keys, tag-only impostors, wrong owner/signer and order independence. `go test -race -count=1 ./...` green.

### RA6X-033 — Configured zones that fail initialization can be absent from every health/status count — FIXED
- **Change (Golang-tudor-dnssec-signer):** A failed key recovery/generation in the daemon cycle now leaves (or creates) a keyless placeholder for the zone carrying an `init:` error, records a failed signing operation in metrics and sends the signing-error heartbeat; the placeholder is retried every cycle and the error is cleared once initialization and signing succeed. Status output (`status`, `/api/status`, the dashboard) and zone metrics receive the configured zone list: a configured zone with no state entry is reported as an `init:` error and counted in the totals rather than omitted. Readiness (`/health` `dependency_errors`, `/healthz`) fails for every configured zone that has no state entry, no keys, has never been signed, or was signed but never confirmed served. Liveness of the signing loop is reported as before.
- **Files:** `daemon.go`, `health.go`, `status.go`, `main.go`, `internal/metrics/metrics.go`, `internal/signer/sign.go`, `CLAUDE.md`, `bookkeeping_ra6x033_048_test.go` (new), `reload_test.go` (fixture now a ready zone)
- **Verification:** With writable directories and a corrupt key pair for a configured zone absent from state: before the first cycle the zone is already counted as an error in status and readiness; after a cycle the zone appears with its `init:` error, `dnssec_tudor_signing_operations_total{status="failure"}` increments, `/health` is 503 naming the zone and `/healthz` is 503; removing the corrupt pair lets the next cycle initialize and sign the zone, clear the error and restore `/healthz`. `go test -race -count=1 ./...` green.

### RA6X-048 — CLI paths inconsistently persist and clear operational errors — FIXED
- **Change (Golang-tudor-dnssec-signer):** Zone errors are operation-scoped (`ZoneState.SetOperationError`/`ClearOperationError`, prefixes `signing:`, `rollover:`, `init:`, `deployment:`, `registrar:`; JSON shape unchanged). The signer clears exactly the signing (and init) error inside `recordSignedZone` on a successful publication, so every path that signs — daemon cycle, `sign`, `resign`, the rollover commands — shares one bookkeeping: failures are recorded as `signing:` (`recordSigningFailure` persists them in the CLI), rollover check failures as `rollover:` (daemon and `sign`, cleared when the checks pass), initialization failures as `init:`, and hook failures as `deployment:` (cleared when a hook later confirms the generation, per RA6X-004). `sign` returns a non-zero exit for a rollover failure in addition to signing failures, while healthy zones are still signed and deployed; `resign` persists its failure and leaves the last good output. Unrelated errors of other operations are retained.
- **Files:** `internal/state/state.go`, `internal/signer/sign.go`, `daemon.go`, `main.go`, `CLAUDE.md`, `bookkeeping_ra6x033_048_test.go`
- **Verification:** A one-shot `sign` whose ZSK rollover cannot back up the old key (read-only keys directory) exits non-zero with "rollover failed", persists a `rollover:` error on the zone, and still signs the zone; a seeded `signing:` error is cleared by a successful `resign` while a seeded `registrar:` error survives; a `resign` of a broken source records a `signing:` error beside the unrelated one, keeps the last good output and turns the status to `error`. `go test -race -count=1 ./...` green.

### RA6X-037 — Signer heartbeat redirects can disclose its API key across origins or over HTTP — FIXED
- **Change (Golang-tudor-dnssec-signer):** The heartbeat HTTP client now carries `heartbeatRedirectPolicy`, the same policy the sibling validator applies: a redirect is followed only when it stays on the configured origin (same scheme, host and port), never when it would downgrade HTTPS to another scheme, and at most ten times; anything else is refused with an error before the credential-bearing form body is replayed. The explicit `allow_insecure` development override still only permits an initial `http://` URL and does not relax the same-origin rule. Neither the key, the body nor a URL is logged; the event queue and intervals are unchanged.
- **Files:** `heartbeat.go`, `heartbeat_ra6x037_test.go` (new)
- **Verification:** A trusted local TLS endpoint answering 307 and 308 to a second HTTPS origin and to a cleartext HTTP server: the send fails and the destination receives no request (a synthetic key marker is never seen); a same-origin path redirect is followed and delivered exactly once; a redirect loop fails instead of spinning; under `allow_insecure` an initial http origin is contacted once and a cross-origin redirect from it is still refused with nothing sent to the target. `go test -race -count=1 ./...` green.

### RA6X-039 — Registrar diagnostic script signs the wrong bytes for empty request bodies — FIXED
- **Change (Golang-tudor-dnssec-signer):** `scripts/dynadot-probe.sh` builds the string to sign with `printf -v`, which preserves the trailing newline separators that command substitution stripped when the request ID and/or body were empty, so GET and DELETE (and bodies ending in a newline) sign exactly `apiKey "\n" path "\n" requestID "\n" body` as the Go adapter does; the message is piped to the HMAC helper on stdin so the bytes stay intact, the raw request body is unchanged, and the redacted diagnostic is built the same way so it shows every separator byte. Optional request IDs and macOS Bash 3.2 compatibility are preserved; the Go adapter's formula is untouched.
- **Files:** `scripts/dynadot-probe.sh`, `internal/registrar/probe_script_ra6x039_055_test.go` (new)
- **Verification:** The script is run under a stubbed `curl` with synthetic credentials for GET, DELETE, PUT (JSON body), a raw PUT whose body ends in a newline, and GET with `--send-id`; each printed signature equals the Go adapter's `sign()` over the same path, request ID and body, and the redacted string-to-sign shows the `\n` separators. No registrar is contacted. `go test -race -count=1 ./...` green.

### RA6X-055 — The registrar probe exposes credentials in child-process arguments — FIXED
- **Change (Golang-tudor-dnssec-signer):** The HMAC secret no longer appears in any argv: `hmac_b64` hands it to `python3` on a private file descriptor (process substitution) and reads the message on stdin; only when python3 is absent does it fall back to `openssl dgst -hmac` with an explicit warning that the fallback exposes the secret in process arguments. The API key reaches curl through a config file on a private descriptor (`curl -K <(...)`) instead of an `Authorization` argument. Shell tracing is switched off around both secret-handling steps so `bash -x` cannot echo an expanded secret, and printed diagnostics remain redacted. Variants, exact HMAC bytes (RA6X-039), macOS shell compatibility and the command-driven nature of mutations are unchanged.
- **Files:** `scripts/dynadot-probe.sh`, `internal/registrar/probe_script_ra6x039_055_test.go`
- **Verification:** With `curl` and `python3` stubbed on PATH to record their arguments (the python stub then runs the real interpreter) and synthetic marker credentials, neither marker appears in any child's argv across all subcommands, curl receives its credentials via `-K` with the Authorization header inside the config, stdout contains no credential, and the signatures still match the Go adapter. Process-list visibility on the deployment OS was not measured here. `go test -race -count=1 ./...` green.

### RA6X-020 — JSON write deadline expires before valid configured validation work finishes — FIXED
- **Change (Golang-dnssec-validator):** `HandleValidateJSON` now separates work time from delivery time: the entry write deadline (`nonSSEWriteTimeout`, now a variable) still guards the request as a whole, but once the validation has completed the handler re-arms the socket write deadline with its own bounded delivery budget (`armResponseWriteDeadline`, 10 s) before encoding the result, so a validation that legitimately runs up to `total_timeout_seconds` — including the timeout diagnostics at the default 30 s boundary — is still delivered. A failed response write is logged and counted under the fixed metric status `write_failed` instead of being recorded as a delivered 200. Request cancellation and the non-SSE slow-client protection are unchanged; validation construction moved into `runJSONValidation` with a test seam.
- **Files:** `handlers.go`, `server.go`, `delivery_ra6x020_test.go` (new)
- **Verification:** Against the real HTTP server over a real socket with the entry deadline shortened to 1 s: a validation that takes 2 s with a 10 s total timeout returns its complete JSON result with 200; a validation that runs to a 1.5 s total timeout delivers its indeterminate diagnostics; a client that sends a request and never reads an 8 MB result is cut off by the delivery budget and the server answers other requests well before the client gives up. `go test -race -count=1 ./...` green.

### RA6X-021 — Arbitrary HTTP methods create unbounded Prometheus time series — FIXED
- **Change (Golang-dnssec-validator):** `metrics.RecordAPIRequest` normalizes the method label through `NormalizeMethod`: the seven standard methods keep their own label value and any other token is recorded as `OTHER`, including rejected (405) requests. Metric names and label keys are unchanged; endpoint and status labels were already bounded; the actual method still appears in the structured request log.
- **Files:** `internal/metrics/metrics.go`, `internal/metrics/metrics_ra6x021_test.go` (new)
- **Verification:** Three thousand distinct invented methods recorded against one endpoint produce at most the seven known values plus `OTHER` in the gathered registry, with all three thousand counted under `OTHER`, while GET/POST/HEAD/PUT/DELETE/OPTIONS/PATCH keep their labels. `go test -race -count=1 ./...` green.

### RA6X-054 — Public validation can direct DNS queries to non-public network addresses — FIXED (public-only egress by default; deployment firewall not inspected)
- **Change (Golang-dnssec-validator):** An outbound egress policy (`internal/dns/egress.go`) is enforced at the single dial boundary every query passes through — `Querier.exchange`, covering UDP and its TCP retry, authoritative and recursive. The default for the service is public-only: loopback, link-local, private (RFC 1918, ULA, CGNAT), unspecified, multicast, benchmarking/documentation and reserved ranges are refused for IPv4 and IPv6, IPv4-mapped and NAT64 addresses are judged as (or refused like) their IPv4 form, non-IP destinations are refused unless trusted, and every resolved address of every nameserver goes through the same check. The configured `recursive_resolver` is always trusted; `private_destination_allowlist` (CIDRs, validated at load) admits internal ranges for a private diagnostic deployment and `allow_private_destinations = true` disables the check for a deliberately internal instance. Both JSON and SSE handlers apply the policy built from the configuration; the hermetic test harness opts in to loopback explicitly. Documented in the example configuration and `CLAUDE.md`. The production network rules and public exposure were not inspected here.
- **Files:** `internal/dns/egress.go` (new), `internal/dns/query.go`, `internal/dns/resolver.go`, `internal/validator/validator.go`, `internal/config/config.go`, `handlers.go`, `dnssec-validator.toml.example`, `CLAUDE.md`, `internal/dns/egress_ra6x054_test.go` (new), `internal/validator/egress_ra6x054_test.go` (new), `internal/validator/harness_test.go`
- **Verification:** Policy unit cases cover public IPv4/IPv6 (permitted), loopback, private, CGNAT, link-local, ULA, multicast, unspecified, reserved, IPv4-mapped, NAT64 and documentation addresses (refused), hostnames (refused unless trusted), the allowlist and the blanket allowance. Through the real querier against a loopback server: loopback IPv4 and IPv6 destinations are refused with no packet sent, an allowlisted destination is queried, the trusted resolver is always reachable while another loopback address is not, and the server sees exactly the permitted queries. Through the real validation path with the fixture's nameservers on ::1 and the recursive resolver on 127.0.0.1: under the public-only policy no authoritative dial reaches the mock and the result is not secure; with the allowlist the same validation is secure and the authoritative servers are queried. `go test -race -count=1 ./...` green.

### RA6X-050 — Several regression tests cannot detect the failure they claim to prevent — FIXED (the new NSEC3 vectors also exposed and fixed an escape-handling defect in the validator's hash)
- **Change (Golang-tudor-dnssec-signer):** `TestLoadConfig_TestdataStillLoads` resolves the committed fixture at the module root, fails (never skips) when `testdata/config.toml` or its zone is missing, loads the config from the module root and asserts the fixture zone and its path are what was committed. `TestDynadotSign_Deterministic` compares `sign()` against five independently computed HMAC-SHA256 vectors (Python `hmac`/`hashlib` over `key "\n" path "\n" requestID "\n" body`), including the empty request-ID and empty-body shapes GET/DELETE use and a body ending in a newline, so a field-order or separator change fails. `TestDNSKEYCheck` now drives `checkDNSKEYVisible` through the real decision path — the mock recursive resolver, NS resolution and the hermetic authoritative server of the RA6X-032 lab — and asserts every outcome (both keys found by material → pass; served ZSK missing → partial; a foreign key of the same role → fail; empty DNSKEY set → fail; unreachable resolver → error, not a verdict) plus that the authoritative server actually received the DNSKEY query (`servedZone.queries`). Verdict-level coverage at the caller boundary (`ValidateZone`) is the RA6X-032 lab, which now expands the loader's raw ED25519 material (`expandED25519`) so it also runs on seed-format key files (RA6X-047).
- **Change (Golang-dnssec-validator):** `TestComputeNSEC3Hash` checks fixed hashes: the RFC 5155 Appendix A vectors (`example.`, `a.example.`, `ai.example.`, `ns1.example.`, `*.w.example.`, `x.y.w.example.`, `xx.example.` with salt `aabbccdd`, 12 iterations) and independently computed vectors for an empty salt with zero and five iterations, an upper-case owner, a name without the trailing dot and a label carrying a `\000` escaped octet. The escaped-octet vector failed against the implementation: `toWireFormat` hashed the presentation characters of a label rather than the octets they denote, so any NSEC3 owner containing an escape hashed differently from the signer's value. It now builds the canonical wire form from `dns.SplitDomainName` and the existing R-045 `canonicalLabelBytes` helper (unescape, lowercase); unescaped names hash exactly as before.
- **Files:** signer `internal/config/config_validation_test.go`, `internal/registrar/registrar_test.go`, `internal/validate/validate_test.go`, `internal/validate/validate_ra6x032_test.go`; validator `internal/validator/nsec.go`, `internal/validator/nsec_test.go`
- **Verification:** The config test executes from its package directory with no `SKIP` line and passes; removing the fixture path makes it fail. Mutation checks, each restored afterwards: swapping the HMAC field order fails `TestDynadotSign_Deterministic` on the first vector; starting the NSEC3 iteration loop at 1 fails eight vectors; hashing the presentation label bytes fails the `\000` vector; making `sameKeyMaterial` ignore the public key fails `TestDNSKEYCheck` on the foreign-key case. No public DNS is used; platform skips are untouched. `go vet` clean and `go test -race -count=1 ./...` green in both modules.

### RA6X-047 — Generated ED25519 private files are not directly readable by the BIND-format reader — FIXED
- **Change (Golang-tudor-dnssec-signer):** External interoperability is now the contract: `formatPrivateKey` writes an ED25519 private file as the 32-byte seed that BIND, ldns and RFC 8080 carry. Go's 64-byte expanded key is reduced to its seed only when its embedded public half matches the key derived from that seed (the RA6X-030 correspondence rule); an inconsistent expanded key is written unchanged so nothing is silently "repaired". Every serialization goes through the same path (key generation, staging, import), the loader and signer continue to accept both 32- and 64-byte files, the DNSKEY/DS bytes are untouched and no key is regenerated. The README's migration section documents the format, the legacy 64-byte files (still readable, not rewritten in place) and how to hand one to another tool.
- **Files:** `internal/signer/keys.go`, `internal/signer/keyfile_ra6x047_test.go` (new), `README.md` (also carries the RA6X-045 per-zone `algorithm` comment), `internal/validate/validate_ra6x032_test.go` (lab expands the loader's seed)
- **Verification:** A freshly generated KSK's private file holds 32 bytes; miekg/dns's own BIND-format `ReadPrivateKey` — an independent reader — accepts it and derives the same public key; a TXT RRset signed with that externally read key verifies under our DNSKEY; a legacy 64-byte file loads with unchanged DNSKEY and DS digest, re-serializes as the seed, and a corrupted expanded key is not converted. Every existing key, rollover and signing test still passes on seed-format files. `go vet` clean, `go test -race -count=1 ./...` green.
