# Fix log — REVIEW_DAYBREAK_BLUE_XHIGH.md

Worked in the suggested fix order (REVIEW_DAYBREAK_BLUE_XHIGH.md §"Suggested fix order and dependencies").
Each entry: finding ID, what changed, files touched, verification result. SKIPPED entries record why the spec was ambiguous or conflicted with actual code behavior. Paths are relative to the module named in the entry.

Baseline before any changes (review baseline `3a43241`, source baseline `87eb640`): both modules `go vet ./...` clean and `go test -race -count=1 ./...` green. Toolchain: go1.27.1 darwin/arm64 (modules pin `toolchain go1.27.1`).

---

## Group 1 — Make state and publication transitions transactional

### RDAYBLUEX-019 — State merge can erase same-generation CLI diagnostics — FIXED
- **Change (signer):** `ZoneState` gained a persisted monotonic `revision` (`omitempty`; old documents load at 0). `State` keeps the last synchronized disk image per zone (`base`, a deep copy taken at `LoadState`, `ReplaceFromDisk`, `ReloadFromDisk` and after every committed `Save`). `ReloadFromDisk` is now a three-way merge against that base instead of the `LastSigned`/`rolloverEqual` heuristic: a zone the disk did not change keeps memory (unsaved work included); a zone memory did not change adopts the disk; a zone both changed is merged per field class — signing fields follow the later `LastSigned` (disk on a tie), key identities and the full rollover record follow the disk (the later, key-file-consistent actor) and force a re-sign, publication fields follow the later confirmed generation with cache horizons never lowered and `PendingPublication` recomputed whenever signing and publication come from different sides, and warnings/errors are a three-way set merge in which a diagnostic either side removed stays removed and one either side added is kept (same-operation errors collapse to the disk's). A zone created independently on both sides adopts the disk copy; a zone the disk dropped without a removal marker is released only when memory holds nothing unsaved for it. `Save` advances the revision of every zone whose content differs from the base past both the in-memory and the base value before writing, and a committed write (including a `DurabilityError`) becomes the new base. `rolloverEqual` (type/phase/four key IDs) was replaced by `rolloverStateEqual` over every persisted field. `ReplaceFromDisk` and `ReloadFromDisk` share one validated disk read.
- **Files:** `internal/state/state.go`, `internal/state/rollover_equal_test.go` (rewritten for `rolloverStateEqual`), `internal/state/merge_rdaybluex019_test.go` (new), `state_merge_rdaybluex019_test.go` (new, root)
- **Verification:** The finding's sequence is reproduced through the real daemon: cycle 1 saves generation 1; cycle 2 re-signs generation 2 in memory and its pre-commit save fails (read-only directory); a CLI under the lock persists an urgent `URGENT:` registrar warning and a `registrar:` operation error at generation 1; cycle 3 merges and saves — disk and memory hold generation 2 AND both diagnostics. The CLI then clears them and the daemon's later saves (clean, and again with unsaved generation 3) do not resurrect them. Unit tests cover every class rule: unchanged disk keeps dirty memory, clean memory adopts disk, later signing wins in both directions, a disk-side publication confirmation of an older generation is adopted while the newer unsaved signing stays pending, CLI rollover timestamp/action changes are adopted without forcing a re-sign while a two-sided rollover transition adopts the disk record and forces one, `ForceResign` set by either side survives, removal markers and re-addition, independent creation, a revisionless old document merging by content and gaining a revision on save, and revision monotonicity across a failed save. A concurrent CLI-vs-daemon stress (12 interleaved locked CLI writes against repeated cycles) loses no warning under `-race`. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-013 — A configured hook defeats explicit publication modes — FIXED
- **Change (signer):** "Run the post-sign command" and "this command is the publication authority" are decoupled. Daemon: hooks are fired with `d.hookCompletion(snap)`, which binds the callback to the effective mode of the cycle's snapshot; the callback (`recordHookResult`, formerly `confirmPublication`) records/clears the zone's `deployment:` operation error in every mode and calls `ConfirmPublication` only in effective hook mode. The pending-hook retry loop stays hook-mode only; probe mode confirms exclusively through `probePendingPublications`, now a shared function behind an injectable `newPublicationProber` seam (production: the validator's `ProbePublishedSerial`). CLI: `runPostSignHook` reports "served" by mode — hook mode: success confirms, failure leaves pending (unchanged); immediate mode: the atomic write already confirmed, so a failed hook only records the deployment error and returns true (DS automation is not blocked on the false premise that publication failed); probe mode: the hook (if configured) runs as the deployment mechanism and never confirms, the every-authoritative-server serial probe is run synchronously and is the sole authority, and an unconfirmed zone is reported as pending for the daemon/next `sign`. `runSign` applies the same rule through `recordHookResultCLI` and, in probe mode, probes every pending zone (including ones left pending by earlier runs). The unspecified-mode default (hook configured → hook mode) and config validation are unchanged.
- **Files:** `daemon.go`, `main.go`, `publication_mode_rdaybluex013_test.go` (new)
- **Verification:** Daemon matrix through real cycles: explicit probe mode with a succeeding hook and a probe answering "not yet" keeps the zone pending with `PublishedAt` zero and no deployment error across 2.5 s of cycles while a due ZSK rollover stays in `pre_publish`; flipping the probe confirms and the phase then advances after the cache-safe wait. Explicit immediate mode with a hook: `PublishedAt` equals `LastSigned` immediately after signing, hook completion does not move it, a failed hook on the next generation leaves the zone published/not pending with a persisted `deployment:` error and never lowers a cache horizon, and a later successful hook clears the error without moving timestamps. Hook mode: failure pending + error, retry confirms (unchanged). CLI `runPostSignHook`: immediate + failing hook → true, not pending, error persisted; probe + succeeding hook → false while the probe fails (probe called once), true and persisted once it confirms; probe + failing hook + confirming probe → true with the hook failure recorded; hook mode success/failure unchanged. A probe-mode one-shot `sign` (real config file, epoch serials) leaves a hook-deployed zone pending and a later `sign` with nothing to sign confirms it through the probe. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-006 — Hook execution is not conditioned on durable state publication — FIXED
- **Change (signer):** `signAllZones` no longer launches any hook during the cycle. `checkAndSignZone` only signs; the cycle collects the deployments to launch — zones signed now, deployments a previous cycle deferred, and (hook mode) pending generations to retry — and launches them (per zone or as one coalesced batch, in domain order, under the hook contract) only after the merged state file has committed. A committed save is a clean `Save` or a post-rename `DurabilityError` (the file is visible); a pre-save reload failure or a pre-commit save failure launches nothing, records the collected deployments in the daemon's in-memory `deferredHooks` (keyed by domain, newest generation) and the first later cycle whose save commits launches them; a deferred zone no longer configured is dropped. Every cycle now starts with the three-way `ReloadFromDisk` merge (RDAYBLUEX-019) instead of the wholesale replace, and the hook completion callback merges the same way, so a completion arriving while memory holds newer unsaved signing state never replaces it with the older disk snapshot; the callback changes only the covered zone's deployment error and, in hook mode, its publication fields for the exact generation the hook deployed. Synchronous CLI ordering (save, then hook) was already correct and is unchanged. Added `fsutil.SetSyncDirForTest` so tests outside the package can inject the durability-warning path.
- **Files:** `daemon.go`, `internal/fsutil/export_test_seams.go` (new), `deploy_after_commit_rdaybluex006_test.go` (new), `state_merge_rdaybluex019_test.go` (helper now returns the read-only copy's path)
- **Verification:** The finding's injection is reproduced for per-zone and coalesced modes: with an older valid state file on disk and a pre-commit save failure (read-only directory), the cycle that signed generation 2 starts no hook, leaves the older file byte-for-byte unchanged, keeps generation 2 intact and pending in memory, and records exactly that generation as deferred; after repair the next cycle saves first, runs the hook exactly once (per-zone or batch variables naming the zone), clears the deferred set, and the persisted state shows generation 2 saved and then confirmed by its own hook. An invalid state file at cycle start withholds hooks and reports the state fault. An injected directory-sync failure (visible file) still deploys once and persists the confirmation. Immediate mode defers the hook the same way. A blocking generation-1 hook released after cycle 2's failed save confirms exactly generation 1 while generation 2 stays in memory and pending, and its save persists both. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-005 — A post-sign hook can confirm publication in the wrong reload generation — FIXED
- **Change (signer):** `Daemon` gained an immutable `generation` (1 at construction; `Reload` increments it in the same `pubMu`-guarded step that publishes the new config/state) and `Generation()`. Every cycle snapshot carries its generation, and `hookCompletion(snap)` binds the callback to the launching snapshot. `SignedZoneRef` gained `Serial` (the deployed generation's published serial; `zoneRef` fills `SignedAt` and `Serial` from the zone record, and the daemon uses it for every launch). `recordHookResult` now takes the cross-process state lock first (so a `Reload` is never held up by that wait), then under `pubMu` requires the active generation to equal the launching one, merges the disk into memory, and for each zone requires the domain to still be configured with the same output path and the zone's current `LastSigned`/`PublishedSerial` to equal what the hook deployed; a hook-mode confirmation additionally requires the publication to still be pending. Any mismatch — reload, removed/re-added zone, changed output path, superseded signing — is logged and discarded whether the hook succeeded or failed, so a stale completion neither certifies output the active configuration never deployed nor overwrites a newer generation's status. Matching completions change only the covered zone's deployment error and, in hook mode, its publication fields for the exact generation, then persist. The generation token is the one RDAYBLUEX-025 reuses.
- **Files:** `daemon.go`, `hooks.go`, `hook_generation_rdaybluex005_test.go` (new), `deploy_after_commit_rdaybluex006_test.go` (stale-completion case updated to the discard semantics)
- **Verification:** Blocking-hook tests through the real daemon: a generation-A hook is launched, a reload publishes generation B (fresh state loaded from disk, same domain pending), A is released successfully — B stays pending with `PublishedAt`, `PublishedGenerationSignedAt`, both cache horizons and served TTLs unchanged and nothing persisted as confirmed; B's own cycle then confirms exactly B and persists it. A stale FAILURE released after a reload records no deployment error in B. Repeated with the domain removed by a CLI and dropped from the reloaded configuration (nothing resurrected or persisted) and then re-added and confirmed by its own hook, with a changed output directory (discarded), and with a newer signing of the same domain under one generation (both hooks complete; exactly the newer generation is confirmed and persisted). The 006 stale-completion case now asserts the superseded completion confirms nothing while memory keeps unsaved generation 2. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-018 — Pending hooks can overlap without a process-wide bound — FIXED
- **Change (signer):** Added `hook_dispatch.go`: a long-lived `hookDispatcher` owned by the daemon (workers tracked on `hookWG`) through which every asynchronous post-sign hook now goes. It runs at most `hookMaxConcurrent` (fixed default 4; package variable for tests) hook processes at a time process-wide; single-flights each exact publication generation (`domain` + `SignedAt`, or the members of a coalesced batch) so a generation that is queued or running is not enqueued again by the per-cycle pending-publication retry; enqueues one job per zone in per-zone mode (one batch job when coalescing) so a newer generation of a zone drops the zone's still-queued older generation (a running one finishes and its completion is discarded by the generation-aware callback, RDAYBLUEX-005); after a failure makes the same generation eligible again only after an exponential backoff whose base is the daemon's poll interval and whose cap is `hookRetryBackoffMax` (1 h), keyed to the hook command so a changed (fixed) command is retried at once; and drains its queue on shutdown within the daemon's existing bounded `waitForHooks`. `Reload` purges queued (not running) work for the superseded generation. `firePostSignHooks` keeps its signature: the synchronous CLI path is unchanged, and a caller with only a wait group is routed through a fresh dispatcher; its invocation building and execution were extracted into `buildHookInvocations`/`runHookInvocation`, shared by both paths, so the hook environment and order contract are identical.
- **Files:** `hook_dispatch.go` (new), `hooks.go`, `daemon.go`, `hook_dispatch_rdaybluex018_test.go` (new)
- **Verification:** Blocking-hook tests across short poll cycles through the real daemon: a running generation is not started again by six retry cycles and nothing is queued for it (per-zone and coalesced), and the single invocation confirms it; with the bound set to 2 and eight zones holding 150 ms each, the maximum observed overlap of hook processes is ≤ 2, every zone is deployed exactly once and confirmed, and the workers exit when the queue drains; a failing generation is not retried inside its 400 ms poll-interval backoff, is retried once after it, is not retried inside the doubled window, and a changed hook command is retried immediately and confirms; with the bound set to 1, a re-signing of a zone whose first generation is still queued drops the queued entry, the zone is deployed exactly once and exactly its newest generation is confirmed while the running zone is confirmed by its own completion; shutdown with two queued jobs returns from the bounded wait within its timeout and the queued deployments still drain once the blocking hook releases; a coalesced follow-up batch excludes the zone whose batch is still running and names only the re-signed one. The existing wait-group-tracked hook tests (R-026) and the CLI hook-contract tests (RA6X-043) pass unchanged. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-025 — Dashboard validation caches are not scoped to a daemon generation — FIXED
- **Change (signer):** Both dashboard caches are keyed by the daemon generation from RDAYBLUEX-005. `validationCache.get(gen, …)`: a lookup under a new generation drops the previous result and detaches any in-flight computation (its completion stores a result and clears the in-flight slot only if its generation is still current, and a waiter returns a result only for its own generation); `zoneValidationCache.get(gen, domain, …)`: a new generation evicts every per-zone entry (removed zones included), an in-flight old computation stores only into the entry of the generation and map that started it, and expired entries with no computation in flight are swept on every lookup so remove/re-add churn within a generation stays bounded. The handlers receive `d.Generation()` through `NewWebServer`'s closures (`dashboardHandler`, `apiValidateHandler`, `apiValidateZoneHandler` gained a `gen` argument; `cachedValidateAll`/`cachedValidateZone` likewise). Within a generation the 30-second cache, single-flight and stale/nil-on-timeout behaviour, endpoint schemas and per-request resolution of the current cfg/state are unchanged.
- **Files:** `web.go`, `web_generation_rdaybluex025_test.go` (new), `web_test.go`, `web_zonecache_r018_test.go`, `validate_fixes_test.go` (call sites gained the generation argument)
- **Verification:** Unit tests on both caches: generation 1 primes and serves a cached result within the TTL; the first generation-2 lookup computes afresh and never returns generation 1's zones; a generation-1 computation released after generation 2 became active does not replace generation 2's aggregate result nor its per-zone entry; a reload evicts a per-zone entry for a zone the new generation no longer has and recomputes the surviving zone; 200 add/remove-style lookups with a 1 ms TTL leave at most a handful of entries. The existing R-043/R-018 single-flight, bounded-wait and panic-isolation tests pass with the generation argument. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-004 — `data_dir` hot reload moves state without moving the lifetime lock — FIXED
- **Change (signer):** `data_dir` is restart-required. `Daemon.Reload` now returns an error and rejects, before touching anything, a configuration whose `DataDir` is not the active one (`ErrDataDirChanged`): identity is decided by `sameDirectory` — absolute + symlink-resolved path equality, or `os.SameFile` on the two directories — so symlink-equivalent, relative/absolute-equivalent and trailing-separator spellings are accepted while a different or unresolvable directory is refused. A rejected reload applies nothing (no zones, paths, listeners, hooks, heartbeat or state; the generation is not bumped) and logs a precise restart-required error naming both directories (no secrets). The SIGHUP branch in `runServe` performs the same check *before* `loadStateLocked(newCfg)`, so the other directory is neither locked nor read, and logs and continues on rejection. The config schema, CLI flags, on-disk state schema and startup locking are unchanged; hot migration was not implemented (the review allows it only with a full lock-transfer transaction, which is not needed here). Note: the review's suggested fix order omits this finding; it was taken here because it lives in the same `Reload` path as RDAYBLUEX-005/025.
- **Files:** `daemon.go`, `main.go`, `reload_datadir_rdaybluex004_test.go` (new)
- **Verification:** Through the real daemon: a reload changing only `data_dir` fails with `ErrDataDirChanged`, the generation stays 1, `current()` returns the previous config/state pointers, a later signing cycle writes state only under the startup directory while the rejected directory receives neither a state file nor a lock file, and a second instance on the active directory is still refused by the lifetime lock. A symlink to the data directory, a `dir/../base` spelling and a trailing-separator spelling are all accepted (each publishing a new generation with the new config active). A non-existent `data_dir` is rejected with the previous state active. Existing reload tests (R-006, R-057, RA6X-040) pass unchanged. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

## Group 2 — Correct DS lifecycle gates before further registrar automation

### RDAYBLUEX-003 — Rollover and publication gates can operate on a partial delegation — FIXED
- **Change (signer):** `internal/validate/parentds.go` was rewritten around nameserver identity. `discoverNS` asks the recursive resolver for the zone's NS RRset (Answer-section NS owned by the zone, or the Authority-section one for a referral-shaped answer), canonicalizes and de-duplicates the names, accepts Additional-section A/AAAA only as in-bailiwick, owner-matched glue for the family it supplies, resolves every other family of every name through the resolver accepting only records owned by the queried name (an NS name must not be an alias), and fails with an error naming the nameserver when a lookup fails (transport error or non-NOERROR rcode) or a name ends up with no address in either family; addresses are de-duplicated without losing the NS identities they represent (`nsTarget`, `flattenTargets`). Every resolver and direct exchange goes through `exchangeTC`, which retries over TCP on truncation and requires the response to echo exactly the question asked (the client library already rejects a mismatched ID); resolver answers must be NOERROR. `queryDirect` (shared with the validator's DS/DNSKEY/SOA checks) now also requires an authoritative answer. `ProbeParentDS` and `ProbePublishedSerial` count only DS/SOA records owned by the queried name and report the nameserver map in the new additive `ParentDSObservation.ByNameserver`; `resolveAllNS`, the configured resolver and the custom authoritative port are preserved. Two older fixtures were corrected to model authoritative servers and a resolver that answers A/AAAA (they relied on out-of-bailiwick glue and non-AA answers).
- **Files:** `internal/validate/parentds.go` (rewritten), `internal/validate/validate.go` (`queryDirect`), `internal/signer/rollover.go` (`ByNameserver`), `internal/validate/parentds_rdaybluex003_test.go` (new), `internal/validate/parentds_test.go`, `internal/validate/validate_test.go` (fixtures)
- **Verification:** A programmable lab (UDP+TCP resolver mock on one port, two authoritative parent servers on 127.0.0.1 and ::1) drives the real probes: NS, A and AAAA answers truncated on UDP are completed over TCP and both parent servers are probed with both nameserver identities recorded; a third NS name that returns NXDOMAIN fails discovery naming `ns-gone.parent.test.` with "coverage incomplete" and `ProbePublishedSerial` refuses too; one-family in-bailiwick glue is used for that family while the other family is resolved (the glued family is never re-resolved) and both addresses are probed; an out-of-bailiwick Additional A pointing at an unroutable address and an unrelated Additional record are ignored in favour of resolution; one address shared by two NS names is queried once while both identities are recorded; a non-authoritative answer from one parent fails the probe; an answer to a different question fails both the direct probe and resolver discovery; DS and SOA records owned by another name count as absent / "no SOA"; a SERVFAIL on one nameserver's addresses fails the probe without asserting absence. The pre-existing all-servers-must-agree and publication-probe tests pass on the corrected fixtures, as do the validator's DS/DNSKEY/SOA checks (their mocks now answer authoritatively). `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-007 — Registrar automation never constructs the phase-correct final DS set — FIXED
- **Change (signer):** Desired-DS construction is phase-aware. `registrar.IncludesOldDS` encodes the phase table — KSK: old+new only in `ds_add_wait`, new-only from `ds_propagation_wait` (the new DS was observed at every parent server; the workflow authorizes old-DS removal) through `retiring`; algorithm: old+new through `algo_ds_add_wait` and `algo_ds_propagation_wait` (RFC 6781 §4.1.4), new-only in `algo_old_ds_removal_wait` and `algo_retiring`; no rollover: active KSK only — and `BuildDSSet` uses it, so `registrar push`/`verify` can never resurrect the old DS in a removal or retirement phase. The CLI auto-publish logic was refactored into `autoPublishDS` (with `MaybeAutoPublishDS` as its interactive wrapper): `rollover_complete` now installs the phase-correct set — `ReplaceDS` (new-only) for a KSK rollover, additive for an algorithm rollover whose set still holds both, so no destructive clear happens there — and a new operation `signerpkg.OpRolloverOldDSRemoval` replaces with the new-only set. The rollover manager gained a `DSPublisher` seam (`SetDSPublisher`, injected by `newRolloverManager` for both daemon and CLI `sign`); `CheckAlgorithmRollover` invokes it at the automatic transition into the old-DS-removal phase and on every later check until it succeeds, idempotently across restarts through the new persisted `RolloverState.DSRemovalPushedAt` (additive, `omitempty`; included in the merge's rollover comparison). A failed registrar update is recorded as a visible zone warning and never advances the phase (the old DS stays served, so the every-parent-server absence probe cannot pass); a successful one is never repeated. Registrar interfaces, CLI command names/output, config keys and manual operation for zones without auto-publish are unchanged.
- **Files:** `internal/registrar/registrar.go`, `internal/signer/rollover.go`, `internal/state/state.go`, `registrar_cli.go`, `daemon.go`, `internal/registrar/dsset_rdaybluex007_test.go` (new), `registrar_phase_rdaybluex007_test.go` (new)
- **Verification:** Phase table test for `IncludesOldDS` (all nine type/phase pairs) and `BuildDSSet` through real staged keys asserting the exact DS identities in ordinary, every KSK and every algorithm phase. End to end against a Dynadot-shaped fake registrar loaded from a real config file: `add` and `rollover_start` publish old+new additively; KSK completion leaves only the new DS; an explicit `registrar push` during `ds_propagation_wait` and `retiring` removes an old DS that crept back and never re-adds it; algorithm completion retains both with zero DELETEs; the automatic transition into old-DS-removal replaces with new-only exactly once, persists the marker, and three further checks plus a restart over the persisted state do not repeat it while no key is retired; with PUTs failing, the transition stands, nothing is pushed, a `registrar auto-publish failed` warning is recorded, the prior DS set is intact (PUT-first), the live KSK and force-resign flag are unchanged, a second failing check still does not push, and after healing the next check replaces exactly once. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-008 — Old-DS absence ignores records using an unexpected digest type — FIXED
- **Change (signer):** `ProbeParentDS` now computes two different predicates. `PresentOnAll` (new-key presence) still requires an exact supported digest (SHA-256 or SHA-384) derived from the DNSKEY on every parent server. `AbsentOnAll` (old-key absence, the retirement gate) is true only when no DS with the old key's key tag AND algorithm exists on any server, irrespective of digest type or digest value — a SHA-1, unknown-digest, wrong-digest or colliding-tag record keeps the key "present", because waiting is safer than retiring a key a resolver may still select. The `ParentDSObservation` API, the every-parent fail-closed requirement, the supported positive digest policy and the TTL behaviour are unchanged; the distinction is documented on the function.
- **Files:** `internal/validate/parentds.go`, `internal/validate/parentds_rdaybluex008_test.go` (new)
- **Verification:** With the two-server parent lab, a residual record on one server that is a SHA-1 DS for the old key, an unknown digest type (200) with the old tag/algorithm, a wrong SHA-256 digest with the old tag/algorithm, and another key's digest under the old tag/algorithm each yield `AbsentOnAll=false` and `PresentOnAll=false`; an unrelated key's DS on both servers does not block absence; exact SHA-256 and exact SHA-384 on both servers give `PresentOnAll=true`; SHA-256 on one server and SHA-1 on the other is neither present-on-all nor absent. The algorithm-rollover consumer of `AbsentOnAll=false` (no retirement) is covered by the existing RA6X-003 test with a fake probe. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-024 — `parent_ds_ttl` can truncate to zero or wrap before a key-retirement gate — FIXED
- **Change (signer):** `config.ValidateParentDSTTL` (applied by `Config.Validate`) accepts zero (unset) or a whole number of seconds between `MinParentDSTTL` (1 s) and the documented operational maximum `MaxParentDSTTL` (7 days); sub-second, fractional-second, negative and above-maximum values are rejected with a field-specific error, which also rules out anything near `math.MaxUint32` seconds. `Config.ParentDSTTLFallback` returns the configured value only when it is in policy (else the 24 h default) and the new `ParentDSTTLFallbackSeconds` is the single checked conversion to the persisted `uint32` seconds; `runRolloverComplete`, `CompleteKSKRollover` and `CompleteAlgorithmRollover` use it and no longer perform their own narrowing cast. At the gates, `effectiveParentDSTTL` maps a persisted zero (legacy or unrecorded) to the conservative 24 h default in `CheckKSKRollover` and every `CheckAlgorithmRollover` wait, so zero never means "no wait". State validation refuses a persisted `parent_ds_ttl` above the 31-bit DNS TTL range (a wrapped or corrupt value) with a repair message, while zero and any real TTL still load. TOML key, state JSON field, observed-parent-TTL behaviour and the `--force` surface are unchanged.
- **Files:** `internal/config/config.go`, `internal/signer/rollover.go`, `internal/state/state.go`, `main.go`, `internal/config/parent_ds_ttl_rdaybluex024_test.go` (new), `internal/signer/parent_ds_ttl_rdaybluex024_test.go` (new), `internal/state/parent_ds_ttl_rdaybluex024_test.go` (new)
- **Verification:** Config table: unset, 1 s, 24 h and 7 d accepted; 500 ms and 999 ms rejected as below the minimum (they would truncate to zero), 1.5 s rejected as fractional, 7 d + 1 s, exactly `MaxUint32` s and `MaxUint32`+1 s rejected as above the maximum, negative rejected; `Config.Validate` applies the rule and, when bypassed, the fallback seconds are 86400 rather than a truncated or wrapped value, while in-policy values convert exactly. Rollover gates: a legacy `ds_propagation_wait` record with `ParentDSTTL` 0 and the DS observed 23 h ago does not retire, and does after 25 h; a 500 ms configured fallback entering propagation through `CompleteKSKRollover(…, 0)` records 86400 and the old KSK stays published; an algorithm record with zero TTL observed 2 h ago does not enter old-DS-removal. State: `parent_ds_ttl` 0, 86400 and 2147483647 load, 2147483648 and 4294967295 are refused with a repair message. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-031 — `resign` and `import` exit successfully after deployment-hook failure — FIXED
- **Change (signer):** One CLI contract for a committed signing/import followed by a failed deployment: `runPostSignHook` now returns `(served, deployErr)` where `deployErr` is the configured hook's failure (the rollover and `add` commands keep gating DS automation on `served` and ignore the second value); `resign` and `import` pass both to `reportDeployment`, which prints a "Deployment:" line distinguishing "confirmed served", "signed, not yet confirmed served (publication pending)" and "NOT confirmed served — the post-sign hook failed" with the retry path, and returns the distinct sentinel `ErrDeploymentNotConfirmed` (wrapped with the domain and hook error) so the exit status is non-zero. Nothing is rolled back: keys, config entry, state (pending publication, `deployment:` error) and signed output stay committed and the daemon retries the hook. `resign` no longer prints "re-signed successfully"; `import` runs the hook before its summary so the summary is truthful and no longer says "imported successfully". Hook environment, coalescing, on-disk transaction semantics, daemon retries and command arguments are unchanged.
- **Files:** `main.go`, `deploy_exit_rdaybluex031_test.go` (new), `publication_mode_rdaybluex013_test.go` (helper adapted to the two-value signature)
- **Verification:** `resign` with a failing hook returns an error satisfying `errors.Is(…, ErrDeploymentNotConfirmed)`, its stdout contains no "successfully" and does say "NOT confirmed served", the signed output remains, and the persisted state shows the new generation pending with a `deployment:` error; healing the hook and re-running succeeds with "confirmed served" and clears the error. `resign` with no hook configured (immediate mode) still exits zero. `import` with a failing hook returns the distinct error, prints no unqualified success, and leaves keys, output, pending state with the deployment error and the config entry committed. The existing CLI hook-contract and import-transaction tests pass unchanged. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

## Group 3 — Repair validator candidate authentication

### RDAYBLUEX-001 — An unauthenticated first parent response can determine the DS result — FIXED
- **Change (validator):** `queryDSFromParentWithValidation` selects candidates by authentication. Every parent server's answer is classified by `classifyParentDSAnswer`: a served child-owned DS RRset is authenticated under the parent's keys (`verifyDSRRSIGSetB`), and an empty (NODATA) or NXDOMAIN answer by its NSEC/NSEC3 proof of DS absence (`verifyDSAbsence`), whose outcome is kept on the new `parentDSResult.AbsenceProof`/`AbsenceProven` so `finalizeNoDSDelegation` (now taking the result) reuses it instead of spending the verification budget twice. The first candidate that authenticates is selected — quick mode stops querying there, extended mode queries every server — and every other answer is judged against the selection as a diagnostic (`judgeParentDSServer`, now a method, which also verifies a negative answer's absence proof when the selection is negative), so a bad earlier response never blocks a later good one and never disappears from the diagnostics. When nothing authenticates the first usable answer is returned with its failed validation so the existing verdict machinery reports the failure; when no server answers, an error. An NXDOMAIN or empty DS answer is never authoritative absence until its proof verified. The public JSON, quick/extended configuration, verification budget and disagreement diagnostics are unchanged.
- **Files:** `internal/validator/validator.go`, `internal/validator/candidate_selection_rdaybluex001_002_test.go` (new), `internal/validator/chain_enforce_test.go`, `internal/validator/verification_fixes_test.go` (finalizer call sites take the result)
- **Verification:** Through the hermetic two-server parent fixture (127.0.0.1 answers first, ::1 second), ordered response tables: unsigned DS then signed DS, attacker-signed DS then signed DS, unsigned NXDOMAIN then signed DS, and unsigned NODATA then signed DS are all **Secure** with exactly one parent disagreement naming 127.0.0.1 and the reason (RRSIG / error / different DS); an unsigned delegation NSEC then a signed one is **Insecure** with the selected candidate's verified absence proof recorded and the invalid responder reported; unsigned NXDOMAIN then an authenticated denial is **Insecure**; unsigned DS on both servers and unsigned NXDOMAIN on both are neither Secure nor Insecure, with the failed DS validation reported; quick mode continues past an unauthenticated first answer, is Secure, and stops after exactly two DS queries. The RA6X-018 per-server, RA6X-007 section and every other existing validator test pass. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-002 — Leaf selection treats unverified denial and DNAME paths as verified — FIXED
- **Change (validator):** `leafSignatureError` was replaced by `leafCandidateError`, a classifier that fully authenticates the path each candidate represents with the same primitives `verifyActualRecord` applies to the chosen answer: a denial (NXDOMAIN, or NOERROR with no answer for the type and no CNAME) must carry a verified NSEC or NSEC3 proof; YXDOMAIN must carry a verified Answer-section DNAME whose substitution genuinely exceeds the name-length limit; the queried type at the owner must verify including a wildcard expansion's no-closer-match proof; a CNAME below an Answer-section DNAME must be exactly the substitution of a verified DNAME RRset; an ordinary CNAME at the owner must verify; every other shape (no raw response, none of these records) is a failure, never an implicit success. `queryLeafAllServers` selects the first candidate whose complete path authenticates (quick mode stops there; extended mode queries every server), falls back to the first usable answer only when none authenticates so the failure is reported, keeps the content/signature disagreement diagnostics, and ends its loop with a labeled break when the context is cancelled. `discoverAliasTarget` continues past usable answers that contain no alias at the queried owner and accepts only a valid, unescaped target name. Result schema unchanged; disagreement alone still never changes the verdict.
- **Files:** `internal/validator/validator.go`, `internal/validator/candidate_selection_rdaybluex001_002_test.go` (new)
- **Verification:** Ordered two-server tests through `verifyActualRecord`: an unsigned NXDOMAIN denial first then a signed A → the positive answer is verified and the denial server reported; a signed NXDOMAIN first then an unsigned A → the authenticated denial is selected; an unsigned DNAME+synthesized CNAME first then a signed A → the A is verified with the DNAME server reported, and a signed DNAME path first then an unsigned A → the DNAME path is selected (`RecordType` DNAME, target from the substitution); an unsigned CNAME first then a signed CNAME → the signed CNAME is selected with the unsigned server reported; alias discovery returns the alias served by the second server when the first answers the name directly and rejects a target containing an escaped space; with a cancelled context no leaf query reaches the mock and no candidate is selected. The existing RA6X-018 leaf consensus test (unsigned second server flagged; expired first, valid second selected) passes unchanged. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-009 — SSE flush failures are invisible to cancellation and concurrency accounting — FIXED
- **Change (validator):** `SSEWriter` flushes through a new `flush` helper that calls `http.ResponseController.Flush` and returns its error wrapped (`failed to flush event: …`); only `http.ErrNotSupported` (a writer implementing plain `Flush` but not `FlushError`) falls back to the best-effort `http.Flusher.Flush`, as the Go HTTP abstraction offers no stronger signal there. Every SSE method (`WriteEvent`, `WriteRawEvent`, `WriteComment`, `WriteRetry`, `WriteID`) returns that error; the `http.Flusher` capability check, the per-event sliding deadline, headers, wire framing and stream ids are unchanged. The stream handler's existing cancel-on-error path therefore fires on a flush failure too, and its single deferred slot release keeps accounting exact. The handler gained an `sseValidateFn` test seam (nil in production) so a streaming validation can be blocked after its first event.
- **Files:** `sse.go`, `handlers.go`, `sse_flush_rdaybluex009_test.go` (new)
- **Verification:** A response-writer wrapper whose `Write` succeeds and whose `FlushError` returns a sentinel makes every SSE method return that sentinel while the event framing and stream id are still written; a plain `httptest.ResponseRecorder` (no `FlushError`) stays best-effort. In the stream handler, with the retry hint's flush allowed and the first validation event's flush failing, the validation context is cancelled (the seam observes `context.Canceled`), the handler returns well within a second, the global validation semaphore is empty afterwards and another request acquires the slot immediately. The existing SSE write-failure and reconnect-refusal tests pass. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

## Group 4 — Close credential and outbound-network boundaries

### RDAYBLUEX-014 — Registrar credentials can be sent over cleartext or across redirects — FIXED
- **Change (signer):** `config.ValidateRegistrarBaseURL` parses a `base_url` override as an absolute URL with a host and no userinfo, query or fragment, and requires `https`; `http` is accepted only for a loopback literal or `localhost` and only with the new `registrar.dynadot.allow_insecure_base_url = true` (off by default; safety is never inferred from a hostname that resolves locally). `Config.Validate` applies it, and `NewDynadotClient` applies it again so no construction path can bypass it. The adapter's HTTP client now carries a `CheckRedirect` that refuses every redirect (the API has none; following one would replay `Authorization`, `X-Signature` and a 307/308 body to a target the path-bound signature was never computed for), surfacing as a dispatched (uncertain) error for destructive callers; the error names only the redacted target. Default and sandbox endpoints, registrar interfaces, timeout/user-agent and valid HTTPS overrides are unchanged; the two root tests that point the adapter at loopback `httptest` doubles set the explicit setting.
- **Files:** `internal/config/config.go`, `internal/registrar/registrar_dynadot.go`, `internal/config/integrations_rdaybluex014_032_test.go` (new), `internal/registrar/transport_rdaybluex014_034_test.go` (new), `registrar_phase_rdaybluex007_test.go`, `registrar_recovery_ra6x031_test.go` (fixtures)
- **Verification:** Config table: empty, HTTPS (with/without path) accepted; cleartext public host refused with and without the override (the override never admits a public host); cleartext loopback refused without and accepted with the override (127.0.0.1, localhost, [::1]); relative, userinfo, query and non-HTTP schemes refused. Client construction enforces the same rule. A capture server behind 301/302/307/308 redirects from a loopback double never receives an `Authorization` or `X-Signature` header: `GetDS`, `AddDS` and `ReplaceDS` all fail with "redirect refused", `ReplaceDS` before any DELETE, and the error is a dispatched (uncertain) outcome. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-022 — The public-only DNS egress policy permits deprecated site-local IPv6 destinations — FIXED
- **Change (validator):** `NonPublic` now classifies from an explicit globally-reachable policy: an address is public only when it is global unicast per `net/netip` (not unspecified, loopback, multicast or link-local), not private, and not in a non-globally-reachable range of the IANA IPv4/IPv6 special-purpose registries. The IPv4 list gained the documentation networks (TEST-NET-1/2/3), the deprecated 6to4 relay anycast and the limited broadcast address; the IPv6 list gained `fec0::/10` (deprecated site-local), the local-use translation prefix, Teredo, benchmarking, ORCHID/ORCHIDv2, the `3fff::/20` documentation block and the SRv6 SID block. Embedded and translated forms are canonicalized at the dial boundary: IPv4-mapped IPv6 is judged as its IPv4 address (unmapped) and a 6to4 address by the IPv4 address it embeds; NAT64 and Teredo are refused as a whole because reachability cannot be decided from the address. The configured recursive resolver stays trusted and the explicit CIDR allowlist and `allow_private_destinations` are unchanged; ordinary global unicast (including the globally reachable AS112/AMT blocks) is not blocked. `PermitIP` was added for callers that already hold a parsed address (the RDAP dial boundary). One older vector that used a TEST-NET-2 address as "public" now uses a real global address.
- **Files:** `internal/dns/egress.go`, `internal/dns/egress_rdaybluex022_test.go` (new), `internal/validator/egress_rdaybluex022_test.go` (new), `internal/dns/egress_ra6x054_test.go` (vector)
- **Verification:** `fec0::1` and `feff:ffff::1` are refused by both `NonPublic` and `Permit`; one vector per non-global category in both families (this-network, private, CGNAT, loopback, link-local, protocol assignments, the three documentation networks, 6to4 relay, benchmarking, multicast, reserved, broadcast; unspecified, loopback, mapped private/loopback, NAT64, local-use translation, discard, Teredo, benchmarking, ORCHID/v2, documentation, SRv6, ULA, link-local, site-local, multicast, and 6to4 embedding private/loopback/documentation) is refused, while global controls (1.1.1.1, 9.9.9.9, Google's v6 resolver, AS112/AMT, 6to4 embedding a global address, mapped global) are permitted; an explicit `fec0::/10` allowlist admits it. At the dial boundary a query to `fec0::1` is refused instantly (no dial, no timeout) over both the authoritative and recursive paths and only an allowlist lifts the refusal. Through the real validator, a delegation whose second nameserver has only a site-local address shows that address refused by the egress policy while the reachable server still authenticates the zone as Secure. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-020 — RDAP endpoint and redirects create a blind HTTP egress path — FIXED
- **Change (validator):** `config.ValidateRDAPBaseURL` (applied by `Config.Validate`) requires an absolute HTTPS URL with a host and no userinfo, query, fragment or `..`/`.` path segments; `http` is accepted only for a loopback literal/localhost with the new TOML-only `rdap_allow_insecure_loopback = true`. The RDAP client never follows a redirect (`CheckRedirect` refuses every one) and, when an egress policy is installed, enforces it at its dial boundary through a custom transport dialer: the target host is resolved once, every resolved address is checked with the shared DNS egress classification (RDAYBLUEX-022), and only a permitted address — the very one that was checked — is dialed, so a DNS-rebinding answer cannot slip past; an environment proxy is never used. `NewHandlers` installs the same public-only policy (with the operator allowlist and `allow_private_destinations`) on the client. RDAP stays optional, the default URL/path contract, response-size/time limits and the validation result schema are unchanged, and an RDAP failure remains a visible diagnostic (`rdap_secure_dns.error`) rather than a DNSSEC failure.
- **Files:** `internal/config/config.go`, `internal/rdap/client.go`, `internal/dns/egress.go` (`PermitIP`), `handlers.go`, `internal/config/rdap_url_rdaybluex020_test.go` (new), `internal/rdap/client_rdaybluex020_034_test.go` (new)
- **Verification:** Config table: empty and HTTPS accepted; cleartext (with and without the override on a public host), cleartext loopback without the override, relative, schemeless, userinfo, query, fragment, dot-dot path and other schemes refused; cleartext loopback/localhost/[::1] accepted with the override; `Config.Validate` applies it. Client: 301/302/307/308 redirects to a capture server, a same-origin changed-path redirect and a self-redirect loop are all refused and the capture server sees zero requests; a loopback literal under the public-only policy is refused before any request; a public-looking hostname whose resolver answer is loopback, private (10.0.0.5) or site-local (rebinding-style) is refused at dial with zero requests; an allowlisted destination is dialed exactly once at the checked address, and `AllowPrivate` reaches the double. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-028 — RDAP DS integers and digests are narrowed without validation — FIXED
- **Change (validator):** `validateRDAPDS` checks every RDAP `dsData` object before it is converted: key tag 0..65535, algorithm and digest type 0..255, a supported digest type (1, 2 or 4), and a digest that is hexadecimal of exactly that type's length (20/32/48 bytes), normalized to upper case. `queryRDAPSecureDNS` excludes malformed records from `DSData` and matching and reports them in `RDAPSecureDNS.Error` ("N malformed RDAP DS record(s) ignored: record i: …"); the result schema, upper-case normalization and all valid comparison behaviour are unchanged, and the diagnostic never touches the core DNSSEC verdict.
- **Files:** `internal/validator/validator.go`, `internal/validator/rdap_ds_rdaybluex028_test.go` (new)
- **Verification:** Vectors: valid SHA-1/SHA-256/SHA-384 records accepted with the digest upper-cased; negative, above-maximum and wrapping (65537) key tags, wrapping (271) and negative algorithms, an above-maximum and an unsupported digest type, odd-length, non-hex and wrong-length digests rejected with the reason. Against a fake RDAP endpoint with the DNS DS `1/15/2/…`: RDAP records whose narrowed values would equal it (keyTag 65537, algorithm 271, keyTag −65535) plus a non-hex digest produce no match, an empty `DSData` and a diagnostic naming the four rejected records; a valid control beside a malformed one still yields a full match with the rejected sibling reported. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-034 — HTTP body limits do not reject responses larger than the advertised maximum — FIXED
- **Change (validator, signer):** Validator: `dns.ReadBodyLimited` reads at most `limit+1` bytes and fails with `ErrResponseTooLarge` when the body exceeds the limit (the body never enters the error); the anchor loader and the RDAP client use it in place of a bare `io.LimitReader`, so an oversized document is rejected instead of being parsed from its prefix. Signer: the Dynadot adapter's `readBodyLimited` does the same for its 1 MiB cap and the failure stays wrapped as a dispatched (uncertain) outcome, so destructive recovery logic treats it as after-dispatch. Limits, timeouts, valid parsing and the anchor pin requirement are unchanged.
- **Files:** validator `internal/dns/bodylimit.go` (new), `internal/dns/anchors.go`, `internal/rdap/client.go`, `internal/dns/bodylimit_rdaybluex034_test.go` (new), `internal/rdap/client_rdaybluex020_034_test.go` (new); signer `internal/registrar/registrar_dynadot.go`, `internal/registrar/transport_rdaybluex014_034_test.go` (new)
- **Verification:** Helper: exactly-at-limit and below-limit bodies are read whole, one byte over is `ErrResponseTooLarge`, a non-positive limit is invalid. Anchors: a valid document padded to end exactly at 1 MiB plus one byte is rejected as oversized, the at-limit document reaches the parser, large non-JSON is rejected as oversized. RDAP: at-limit and limit-minus-one bodies parse, limit-plus-one and large non-JSON are `ErrResponseTooLarge`, a body cut short by the server is an error. Registrar: a body one byte over the cap fails with "exceeds" as a dispatched error and an at-limit body is accepted. Both modules `go vet ./...` clean and `go test -race -count=1 ./...` green.

### RDAYBLUEX-032 — Enabled signer integrations are not fully validated at startup — FIXED
- **Change (signer):** `Config.Validate` now requires, for an enabled Dynadot registrar, a non-blank `api_key` and `api_secret` and a base URL that passes RDAYBLUEX-014's rule; and, for an enabled heartbeat, a non-blank `url`, `api_key` and `app` and an `interval_minutes` between `MinHeartbeatIntervalMinutes` (1) and `MaxHeartbeatIntervalMinutes` (1440), on top of the existing HTTPS rule. Values are trimmed once for the check and never echoed; disabled integrations stay permissive; credential storage, config keys, adapter interfaces and heartbeat payloads are unchanged. The heartbeat loop's defensive five-minute fallback remains but can no longer be reached by a validated configuration.
- **Files:** `internal/config/config.go`, `internal/config/integrations_rdaybluex014_032_test.go` (new)
- **Verification:** Matrix: complete registrar and heartbeat configurations validate; disabled-and-empty ones validate; a missing or whitespace-only registrar key/secret, a cleartext registrar base URL, a missing heartbeat URL/key/app, zero, negative, overflowed (2^40) and above-one-day intervals fail naming the field; one day is accepted; the cleartext heartbeat URL still fails; no failing case's error contains the configured key or secret values. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

## Group 5 — Bound inputs and startup resources

### RDAYBLUEX-017 — Zone-file input has no memory or record-count bound — FIXED
- **Change (signer):** Source input is bounded in both dimensions by package-level hard ceilings `maxZoneFileBytes` (256 MiB) and `maxZoneRecords` (2,000,000), applied by the shared `readSourceBytes` and `parseZoneRecords` helpers that every reading path now uses (`readSourceSnapshot` for daemon/CLI signing, `sourceDigest` for change detection, and `ValidateZoneFile` for `validate`, `add` and `import`). A descriptor whose size is already above the byte limit is refused before any read; the read itself goes through a `LimitReader(max+1)` so growth during the read or special filesystem behaviour is caught and refused; record accumulation stops at the record limit. Every refusal wraps the new sentinel `ErrSourceTooLarge`, which `NeedsSign` turns into a visible signing attempt rather than a silent "defer to next tick", so the daemon records a `signing:` error for that zone (`checkAndSignZone`), leaves its last-known-good output and recorded source identity untouched, keeps processing the other zones, and clears the error once a sane source is restored. Accepted syntax, the `$INCLUDE` prohibition, command surfaces and behaviour below the limits are unchanged.
- **Files:** `internal/signer/sign.go`, `internal/signer/source_bounds_rdaybluex016_017_test.go` (new), `source_bounds_daemon_rdaybluex017_test.go` (new)
- **Verification:** With the limits lowered through the package seams: files of `max-1`, `max` bytes sign and `max+1` is refused naming the limit; `max-1`, `max` records sign and `max+1` records are refused; a single 5,000-byte record is accepted within the byte limit; a sparse regular file above the limit is refused before reading (no allocation); a file that grows past the limit while being read is refused. In a two-zone daemon with the real 256 MiB limit, the oversized zone (a 256 MiB + 1 sparse file, no bytes read) records `signing: … source limit` while the other zone signs in the same cycle, the oversized zone's previous signed output is byte-for-byte unchanged and its recorded source identity is not updated; restoring a sane source clears the error and publishes. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-016 — Same-size source replacements can remain unsigned for days — FIXED
- **Change (signer):** `ZoneState` gained the optional `source_digest` (SHA-256 of the exact bytes parsed, hex), set by `recordSignedZone` only from the snapshot that was signed and published, carried through the state merge (`signingEqual`/`copySigning`), and absent from old state files. `NeedsSign` digests the source on every poll through the same bounded, consistency-checked descriptor read as signing (`sourceDigest`) and decides by content identity: an equal digest with changed metadata refreshes `SourceModTime`/`SourceSize` and does not re-sign; a different digest signs, even with an equal size and an equal or older mtime; a state without a digest and unchanged metadata records today's bytes as the baseline of the current output without re-signing (a legacy state with changed metadata signs as before); a read that cannot complete consistently (or fails) updates nothing and is retried on the next check. Expiry, rollover, path-change, output-presence and quiescence checks come first and are unchanged; schema compatibility is preserved by the `omitempty` field.
- **Files:** `internal/state/state.go`, `internal/signer/sign.go`, `internal/signer/source_bounds_rdaybluex016_017_test.go` (new)
- **Verification:** After signing, an atomic replacement with different equal-length content and the exact preserved mtime requests signing ("content changed (same size and modification time)"); the same with an older mtime requests signing; a byte-for-byte replacement with a newer mtime does not sign and refreshes the recorded metadata; a state with the digest cleared and unchanged metadata establishes the baseline silently and a later same-size content change is then detected; a source being rewritten in place during the digest read defers with the stored identity untouched, and a truncated in-place write does not update the stored digest. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-035 — Health endpoints perform unthrottled filesystem mutation — FIXED
- **Change (signer):** Directory write probes behind `/health` and `/healthz` go through a per-daemon `dirProbeCache`: one probe per directory is single-flighted (concurrent callers wait for the in-flight probe and share its outcome), the result is served for `healthProbeInterval` (10 s), and the cache is invalidated by `Daemon.invalidateHealthProbes` on a configuration reload, a failed state save and a failed zone signing, so a real write failure is re-observed promptly while a request storm cannot multiply filesystem writes. A probe that completes after an invalidation is discarded rather than cached. Remote health requests are bounded by a per-daemon token bucket (`healthRemoteRate` 20/s, burst 40) only when `health.allow_remote` is set; the loopback default is never limited. An over-limit request answers 429 with `Retry-After: 1`. Response bodies, the probed directories and the readiness semantics are unchanged.
- **Files:** `health.go`, `daemon.go`, `health_probe_rdaybluex035_test.go` (new)
- **Verification:** 300 concurrent `/health` and `/healthz` requests probe each of the output, data and keys directories exactly once and all receive the same healthy result, and 20 further requests inside the interval add no probes; with a 40 ms interval a directory that becomes unwritable is still reported healthy from the cache inside the interval and unhealthy (503) after expiry; a reload and a failed state save each cause the next request to re-probe; with `allow_remote` and a rate of 1/s burst 5, 50 requests yield 429s carrying `Retry-After` while the burst is admitted, and the loopback default never returns 429; at the cache level 50 concurrent callers share one in-flight probe and an invalidation during a probe discards its result. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-023 — Positive integer settings can overflow into invalid `time.Duration` values — FIXED
- **Change (validator):** `checkedDuration(field, n, unit, max)` performs every integer-to-duration conversion: non-positive, overflowing (`n > MaxInt64/unit`) and above-maximum values are field-specific errors, and the derived value is checked positive. Operational maxima: query timeout 5 minutes, total timeout 1 hour, shutdown 10 minutes, rate-limit cleanup 1 day, heartbeat interval 1 day. `applyNestedToFlat` now returns an error, which both `LoadFromFile` and `LoadFromEnv` propagate before `Validate`, so no server, goroutine or ticker exists when a bad value is rejected; `Validate` applies the same helper to every converted field (and to the heartbeat interval when enabled) and refuses a derived duration that disagrees with its integer. `NewRateLimiter` additionally clamps a non-positive cleanup interval to five minutes as defence in depth. The integer TOML/environment interfaces and the duration fields consumed elsewhere are unchanged.
- **Files:** `internal/config/config.go`, `ratelimit.go`, `internal/config/duration_overflow_rdaybluex023_test.go` (new), `startup_config_rdaybluex023_test.go` (new)
- **Verification:** Helper table: ordinary and at-maximum values convert exactly; maximum + 1, zero, negative, `MaxInt64/unit` (representable but above the maximum), `MaxInt64/unit + 1`, `MaxInt` and a minutes overflow are errors naming the field. Through the file and environment loaders, every converted field (`query_timeout_seconds`, `total_timeout_seconds`, `shutdown_timeout_seconds`, `rate_limit.cleanup_seconds`) at `MaxInt64/unit + 1`, `MaxInt`, maximum + 1, −1 and 0 is a load error naming the field, the maximum itself loads, and the heartbeat interval is checked only when enabled. Subprocess regression: the built binary run with `-check` and `cleanup_seconds = 999999999999999999` in a config file, and again through `RATE_LIMIT_CLEANUP_SECONDS` with no file, exits non-zero naming the field with no panic. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-010 — A delegation can create unbounded per-request DNS fan-out — FIXED
- **Change (validator):** `ResolveNS` canonicalizes nameserver names (lower case, fully qualified), drops duplicates and orders them by name. The new `Resolver.ResolveNSSet` applies explicit per-zone maxima before any work is scheduled — `MaxNameserversPerZone` (32) names looked up, `MaxAddressesPerNameserver` (16) distinct addresses kept per name, `MaxAddressesPerZone` (64) distinct addresses queried — and returns the records, a de-duplicated deterministically ordered query list (an IPv4-mapped address is its IPv4 address; one address shared by several names is listed under each and queried once) and a `Truncation` list naming every exceeded bound; `ResolveAddressesDetailed` de-duplicates and canonically orders its addresses, and `ResolveNSWithAddresses` is a wrapper over the set. Every validator path that flattened addresses (zone validation, leaf verification, alias discovery, the parent DS query) uses the set. `ValidateMultipleServers` de-duplicates its input and runs the authoritative queries through `runBounded`, a fixed-size worker pool of `max_concurrent` goroutines fed from the caller's loop: no goroutine per input, hand-off and each worker's next pick observe the context, and a server whose turn never comes is reported as "not queried". A truncated set is disclosed as zone warnings and makes the zone Indeterminate with the exceeded bounds in its error, every queried server marked "not judged", so no consensus is ever claimed from a partial set; leaf verification over a truncated set is reported as unqueryable and a truncated parent set is disclosed as a warning. Quick mode (first two servers), JSON field shapes, the global validation limit and configured resolver/timeouts are unchanged.
- **Files:** `internal/dns/resolver.go`, `internal/validator/validator.go`, `internal/dns/fanout_rdaybluex010_test.go` (new), `internal/validator/fanout_rdaybluex010_test.go` (new)
- **Verification:** Resolver: mixed-case and repeated NS names collapse to an ordered canonical list; repeated A/AAAA records and an IPv4-mapped duplicate collapse to three ordered addresses, and `UniqueAddresses` handles textual and mapped duplicates; a two-thousand-record delegation (a thousand names with case-varied duplicates, served over TCP) is bounded to 32 names looked up with the count disclosed, a name with 20 addresses keeps 16 with a note, a zone with more than 64 distinct addresses stops at 64 with a note, a shared address appears under each name but once in the query list, repeated resolution is identical, and a cancelled context ends resolution. Pool: 20 rounds of 5,000 jobs on 4 workers show goroutine growth of at most 6, peak concurrency 4 and every job run; after cancellation at most the running jobs complete and the pool returns; degenerate sizes are safe. Through the hermetic chain fixture: five case-varied NS names sharing one address yield three canonical names, one authoritative DNSKEY query and a Secure verdict shared by every name; a thousand-name delegation whose queried server serves a valid signed DNSKEY is Indeterminate with the bound and count in its error/warnings, 32 names disclosed, address lookups for exactly 32 names and no server judged; with one slow server and `max_concurrent = 1`, cancelling after the first query leaves the queued servers unqueried and reported as such. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module (the pool test repeats under the race detector).

### RDAYBLUEX-011 — Packaged validator startup has no offline last-known-good trust anchors — FIXED
- **Change (validator, packaging):** Runtime persistence was chosen. `dns.FetchAnchors` returns the accepted set together with the exact bytes it validated; `dns.WriteAnchorCache` persists those bytes only after they load again as a usable, pinned set and within `MaxAnchorDocumentBytes` (1 MiB), through a private temporary file in the same directory (mode 0600), `fsync`, `rename` over the cache and a directory `fsync`, removing the temporary file and leaving the previous cache untouched on any failure. `dns.LoadAnchorsWithCache` is the load order operator file → cache → URL, every source authenticated against the binary pins (a cached document is never trusted because it was cached), with an error naming every source when all fail. `AnchorsStore` gained `SetCachePath`, writes the cache after every usable fetch, keeps a cache write failure in `CacheError` without failing the load, and `Refresh` (used by the 24 h loop) re-reads the operator file or fetches the URL and keeps the current set on failure, never re-reading the cache. New setting `root_anchors_cache_path` (TOML and `ROOT_ANCHORS_CACHE_PATH`), default `/var/lib/sigillum-validator/root-anchors.json`, validated absolute and distinct from `root_anchors_path`; empty disables caching. The service unit declares `StateDirectory=sigillum-validator` (mode 0750) as its only writable path under `ProtectSystem=strict`; the packaged and example configurations set the key; the validator README, `CLAUDE.md` behaviour section and `docs/linux-packages.md` describe the cache. Operator-file precedence, the pins as the trust boundary, `-check` (which never touches the cache) and config compatibility are preserved.
- **Files:** `internal/dns/anchors.go`, `anchors_store.go`, `main.go`, `internal/config/config.go`, `config.toml.example`, `README.md`, `CLAUDE.md`, `../packaging/sigillum-validator.service`, `../packaging/sigillum-validator.toml`, `../docs/linux-packages.md`, `internal/dns/anchor_cache_rdaybluex011_test.go` (new), `internal/config/anchor_cache_rdaybluex011_test.go` (new), `anchors_cache_rdaybluex011_test.go` (new)
- **Verification:** Cache writer: a usable document is written with mode 0600 and the exact bytes and loads back as a usable set; truncated, non-JSON, unpinned, future-only, empty and oversized documents are refused with the previous cache intact and no temporary file left; an unwritable directory is an error with the previous cache intact. Load order: no file and no cache fetches the URL and returns its bytes; a written cache is then used without contacting the URL; a usable operator file wins over both; a corrupt cache falls through to the URL and a total failure names file, cache and URL; an empty cache path disables the stage. Store: a clean start offline fails naming every source, serves nothing, reports `root_anchors: unavailable` and creates no file; a successful fetch writes the cache and a restart with the mirror closed loads from the cache with one active pinned anchor and `root_anchors: available`, and a refresh offline keeps set and cache; truncated, non-JSON, unpinned, future-only, expired, empty and oversized refreshes each fail leaving the set, load time and cache unchanged with no temporary files, while a good refresh updates both; a read-only cache directory yields a successful load with the failure in `CacheError`, anchors served and readiness available; an operator file is used, never copied to the cache, and the URL is not contacted; a disabled cache is never written. Config: the path rule table, `Validate` applying it, and both loaders reading the key. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module. **Not run here:** the deb/rpm package-install tests in an isolated image (no such image is available in this environment); the unit and package files were inspected by hand instead.
- **Follow-up (2026-09-10, operator request):** the review's other option was adopted as well. The executable now carries the mirror's reviewed anchor document (`internal/dns/root-anchors.json`, embedded), loaded after the operator file and the cache and before the URL (`LoadEmbeddedAnchors`, `LoadedFrom` = `built-in`). Its unexpired anchors are exactly the pinned keys, so a fresh installation establishes root trust with no network access and the URL is reached only when the built-in set has no active pinned anchor — which, since the pins share the binary, means the binary is due for an update rather than that a download should be trusted. `SetEmbeddedAnchorsForTest` lets tests reach the cache and URL stages. Verification: the built-in document loads with two active pinned anchors (20326, 38696), every unexpired anchor is pinned and every pinned key is present with no expiry date; with no file and no cache it is used and the URL sees zero requests; an operator file still wins; only an unusable built-in document lets the URL be consulted; the earlier cache, refresh and readiness tests run with the built-in document disabled and still pass, and a total failure now names file, cache, built-in document and URL. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.
## Group 6 — Signature timing policy

### RDAYBLUEX-015 — Oversized signature lifetimes wrap DNSSEC time and pass self-verification — FIXED
- **Change (signer):** `config.ValidateSignatureTiming` (applied by `Config.Validate`) rejects a `signature_validity` whose whole window — validity plus the fixed `SignatureInceptionBackdate` (1 h) — is not strictly below 2^31 seconds (RFC 1982 serial arithmetic), and imposes the documented operational maximum `MaxSignatureValidity` (366 days) and minimum `MinSignatureValidity` (1 h). `signRecordsWithModel` takes one clock reading per pass so every RRSIG carries exactly backdate + validity. Post-sign verification (`verifySignedZoneWithModel`, which every signing and import path runs before output replacement) now calls `checkRRSIGWindow` on every RRSIG in addition to the cryptographic check: the signature must be currently valid under serial arithmetic (`ValidityPeriod`), its span must be backdate + validity, and its inception and expiration must each lie within `signingWindowTolerance` (10 min) of the values intended for this pass, so expired, not-yet-valid, wrapped, shifted and unexpectedly long or short windows are refused with the previous output kept. `prepareSignedZone` carries the earliest RRSIG expiration actually written (`earliestRRSIGExpiration`, placed on the timeline with `serialTime`) and `recordSignedZone` persists it as `signatures_expire`; the state field, TOML keys, defaults and CLI output are unchanged.
- **Files:** `internal/config/config.go`, `internal/signer/sign.go`, `internal/config/timing_rdaybluex015_021_030_test.go` (new), `internal/signer/rrsig_window_rdaybluex015_test.go` (new), `README.md`, `CLAUDE.md`, `../packaging/sigillum-signer.toml` (comments)
- **Verification:** Config table: defaults and the one-year maximum accepted; a year plus a day rejected as above the maximum; 100 years, exactly 2^31 s and a value that only crosses the boundary once the backdate is added rejected as not representable; a value just under the boundary rejected by the operational maximum; `Config.Validate` applies the rule. Signing: with every RRSIG re-signed (cryptographically valid) after its window was rewritten, verification refuses expired, not-yet-valid, wrapped (expiration before inception under serial arithmetic), unexpectedly long, unexpectedly short, future-shifted and past-shifted-within-validity windows, naming the reason, and the previous signed output is byte-for-byte kept each time; an unmodified pass verifies and the persisted `signatures_expire` equals the earliest RRSIG expiration parsed back from the published file and never exceeds now + validity. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-021 — The refresh window can be shorter than the daemon's polling interval — FIXED
- **Change (signer):** Configuration now requires `signature_refresh >= RefreshPollFactor (2) × poll_interval` — one interval to reach the next check after the threshold passes and one as the documented allowance for a full signing pass and ticker jitter — plus the granularity minima `MinSignatureRefresh` (1 min) and `MinSignatureValidity` (1 h), so sub-second pairs are refused; `refresh < validity` is unchanged. The daemon additionally schedules a wake-up at the earliest refresh threshold among the configured zones (`SignaturesExp − signature_refresh`, from the persisted actual expiration), re-planned after every pass, on a poll-interval change and on every reload, so an unchanged zone is re-signed at its threshold even under a long poll interval; a threshold already in the past is left to the pass that just ran and the poll ticker, never a hot loop. Each pass processes zones in expiration order (`zonesByUrgency`: never-signed first, then ascending expiration, ties by name), so a slow zone earlier in an arbitrary order cannot push a later zone past its expiration. Source-change polling, manual `sign`/`resign`, TOML keys, defaults and output are unchanged; `beforeSignZone` is a test seam.
- **Files:** `internal/config/config.go`, `daemon.go`, `internal/config/timing_rdaybluex015_021_030_test.go` (new), `expiry_wake_rdaybluex021_test.go` (new), `README.md`, `CLAUDE.md`, `../packaging/sigillum-signer.toml` (comments)
- **Verification:** Config boundaries: refresh one second below twice the poll interval rejected, exactly twice and one second above accepted, refresh equal to the poll interval rejected, a 1 h poll with a 90 min refresh rejected and with 2 h accepted (the multi-zone allowance), validity below 1 h, refresh below 1 min and a sub-second pair rejected. Daemon, real time: with a 1 h poll interval (the ticker never fires), validity 6 s and refresh 3 s, the zone is re-signed by the wake-up before its recorded expiration and not before the refresh threshold; with two zones, validity 8 s, refresh 4 s, the alphabetically earlier zone made slow (1.5 s) and the later zone made the most urgent, both are re-signed before their expirations and the most urgent one first; the ordering and next-due helpers place never-signed zones first, order by expiration, skip past thresholds and return zero when nothing is due. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-030 — Negative per-zone key lifetimes silently mean "inherit" — FIXED
- **Change (signer):** `Config.Validate` rejects a negative per-zone `ksk_lifetime` or `zsk_lifetime` with a zone-qualified error that names the inherit convention (omit or `0`); zero and omitted still inherit, positive overrides still apply, and the getters, TOML keys and global checks are unchanged. Because `LoadConfig`/`ParseConfig` validate, the SIGHUP reload path refuses such a file before `Daemon.Reload` is reached, and `AddZoneToConfigFile` continues to require the edited file to load.
- **Files:** `internal/config/config.go`, `internal/config/timing_rdaybluex015_021_030_test.go` (new)
- **Verification:** Through TOML loading for both roles: omitted and zero inherit the global values, positive values override, negative values fail loading naming the zone and the key; a config edit (`add`) on a file with a positive override keeps it loadable with the override intact and the new zone inheriting, and a file with a negative override is refused by the loader the reload path uses. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

## Group 7 — Boundary hygiene

### RDAYBLUEX-029 — Signer zone-name validation does not enforce DNS wire limits — FIXED
- **Change (signer):** `config.ValidateDomainName` now defines and enforces the managed-zone name grammar, documented at its declaration: ASCII letters, digits, hyphen and underscore in dot-separated labels (IDNA names as A-labels only; no escaped octets, no U-labels); labels of 1..63 octets and at most 253 characters of text (255 wire octets, `MaxZoneLabelLength`/`MaxZoneNameLength`); no empty label (no leading dot, no `..`, at most one trailing dot, which stays accepted and is ignored for identity); no leading or trailing hyphen in a label, with leading underscores (service-style labels) accepted; case accepted and canonicalized for identity; the root zone explicitly not managed. The path-separator/traversal defence is retained. The same validator already guards configuration loading, `add` and `import` before any key, output, config or state file changes, and everything it accepts is also accepted by `dns.IsDomainName`, which the persisted-state check and the parser apply. Output/key filename conventions, canonical-collision rejection, the config schema and already persisted valid names are unchanged.
- **Files:** `internal/config/config.go`, `internal/config/zone_name_rdaybluex029_test.go` (new), `zone_name_cli_rdaybluex029_test.go` (new)
- **Verification:** Config table: ordinary, trailing-dot, mixed-case, 63-octet label, 253-character (255 wire octets) with and without the trailing dot, inner hyphen, underscore service label, A-label and single label accepted, each also accepted by the DNS parser bare and qualified; 64-octet label, 254 characters, leading/trailing hyphen, empty labels (inner, leading, doubled trailing dot), U-label, escaped octet, space, path separator, traversal, root and empty rejected naming the rule; `Config.Validate` applies it per zone. CLI: `add` and `import` refuse every invalid spelling as an invalid domain name with the data directory (keys, output, state, config) byte-for-byte unchanged; `_acme-challenge.…`, an A-label and a trailing-dot spelling are added, signed and persisted, and the persisted state validates. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module. Observation outside this review's scope (not changed): a *mixed-case* zone spelling is accepted by the grammar as before, but signing it under NSEC3 fails post-sign verification because the generated NSEC3 owners keep the configured case while the verifier expects the canonical form; lower-case spellings are unaffected.

### RDAYBLUEX-033 — Boolean existence checks fail open in key rollback and backup naming — FIXED
- **Change (signer):** Every preservation or destruction decision now uses `(exists, error)` semantics or no-clobber creation, and `FileExists` (kept for its callers) is no longer used at a safety boundary. `uniqueBackupBase` claims both halves of a candidate with `O_CREATE|O_EXCL` (`reservePair`/`reserveFile`): any existing name — a file, an empty placeholder, a dangling or looping symlink — makes the candidate unavailable and any other error aborts; unused placeholders are released only while still empty (`releaseReservation`, also applied by `copyPairUnique` for a half absent at the source). `setAside` claims its destination with a hard link (atomic no-clobber), unlinks the original only once the link exists and keeps the material under both names if that unlink fails; where hard links are unavailable (`linkUnsupported`) it falls back to a fail-closed `statExists` followed by a rename, and every other link or stat error aborts with nothing moved. `keyPairAbsent` returns `(absent, error)` and `add` aborts on a stat failure before creating anything. `RecoverOrGenerateKeysReport` records the exact generations it minted (`KeyGeneration`, filled as soon as a pair is generated so it is accurate on a later failure); `unwindAdd` removes a live pair only when the record names it and `LivePairHoldsKeyTag` confirms the live public half still carries that key tag, leaving any other slot alone. Package-level `osStat`/`osLink`/`osRename`/`osRemove` seams exist for fault injection. No existing backup or key artifact is deleted or rewritten by the change.
- **Files:** `internal/signer/keys.go`, `internal/signer/keytx.go`, `main.go`, `internal/signer/existence_rdaybluex033_test.go` (new), `add_rollback_rdaybluex033_test.go` (new), `add_rollback_r011_test.go`, `verification_fixes1_test.go` (adapted to the new signatures)
- **Verification:** `setAside` skips an existing backup, a looping symlink and a dangling symlink and lands on the next free name with every earlier file untouched and no rename attempted; injected `EACCES`, `EIO`, `ELOOP` and a transient error on the link, and separately on the fallback stat with hard links reported unsupported, each return that error with the source and existing destination untouched and no rename or remove issued; the rename fallback still skips an occupied name and renames exactly once. `uniqueBackupBase` skips names occupied by an empty `.key` or a looping `.private` symlink, claims both halves as 0600 placeholders, never hands out a claimed name twice, returns `EACCES` from an unwritable directory instead of a "free" name; `statExists` reports `EACCES` from a directory without search permission; an injected `EIO` aborts `copyPairUnique` before any copy. Sixteen concurrent allocations yield sixteen distinct names, and sixteen concurrent set-asides into one namespace preserve every source at a distinct destination. Rollback: an uncommitted (recovered) pair survives an unwind; a minted generation is removed while the live slot holds it; a record naming a generation the slot no longer holds removes nothing; the orphan-preservation test now drives the real recovery report, which mints nothing on an orphan. `keyPairAbsent` answers absent/present on a readable directory and returns `EACCES` on one without search permission. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module on macOS (Linux and FreeBSD runs are not available in this environment; the code paths use only portable `os` primitives).

### RDAYBLUEX-012 — Malformed typed environment values silently activate defaults — FIXED
- **Change (validator):** `getEnvInt` and `getEnvBool` return `(value, error)`: an absent or empty variable yields the default (the existing contract); a present value that is not a whole number (duration suffix, decimal, letters, whitespace only, embedded space) or is outside the integer range, and a boolean outside the accepted spellings (`true/false`, `1/0`, `yes/no`, `on/off`, case-insensitively, surrounding whitespace ignored), is an error naming the variable and the expected syntax without echoing the value. `LoadFromEnv` collects every such error and returns them together (`errors.Join`) before conversion and validation, so an operator repairs a deployment in one pass and no malformed value ever reaches `Validate` as a default. Variable names, precedence, defaults and the `Config` fields are unchanged; the validator guide's environment note was corrected.
- **Files:** `internal/config/config.go`, `internal/config/config_test.go` (the old fall-back expectation), `CLAUDE.md`, `internal/config/env_typed_rdaybluex012_test.go` (new)
- **Verification:** For each of `MAX_CONCURRENT`, `MAX_CONCURRENT_VALIDATIONS`, `RATE_LIMIT_PER_SEC`, `RATE_LIMIT_BURST`, `RATE_LIMIT_CLEANUP_SECONDS`, `QUERY_TIMEOUT_SECONDS`, `TOTAL_TIMEOUT_SECONDS`, `SHUTDOWN_TIMEOUT_SECONDS` and `HEARTBEAT_INTERVAL_MINUTES`: `5s`, `1.5`, `ten`, whitespace only, an overflowing number, a lone sign and an embedded space fail startup naming the variable and not echoing the value, while a padded integer loads; empty variables keep every default; every accepted boolean spelling (including padded) sets the heartbeat flag as documented and `enabled`, `y`, `n`, `2`, `t`, `f`, a phrase and whitespace are refused; three malformed variables are reported together. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

## Group 8 — API contract consistency

### RDAYBLUEX-026 — A cold per-zone validation timeout returns `200 null` — FIXED
- **Change (signer):** `apiValidateZoneHandler` treats a nil result from the per-zone cache exactly like the aggregate handler does: `503 Service Unavailable` with `Retry-After: 5` and the same plain-text error body ("validation in progress, retry shortly"), the JSON content type set only once a result exists. The background computation still finishes and seeds the cache, a stale cached result is still served during a slow refresh, unknown zones remain 404, and successful JSON, cache timing and single-flight behaviour are unchanged. `validationMaxWait` became a variable and `zoneValidateFn` a nil-in-production seam so the cold path can be exercised without a 20-second wait.
- **Files:** `web.go`, `web_validate_zone_rdaybluex026_test.go` (new)
- **Verification:** Through the real web mux with the wait budget at 100 ms and the validation gated: a cold request answers 503 with `Retry-After: 5`, a non-JSON error body and never `null`; an unknown zone is 404; releasing the computation seeds the cache and the next request is 200 with the normal JSON result; a cached result aged past the TTL is still served (200) while the refresh outlasts the budget. Existing single-flight, generation and cache tests pass. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

### RDAYBLUEX-027 — Unknown validation modes silently select the more expensive mode — FIXED
- **Change (validator):** `parseValidationMode` is the one place both the SSE and the JSON endpoint parse the `mode` parameter, before any validation slot is taken: an empty value is the documented default (`extended`), `quick` and `extended` are accepted case-insensitively after trimming whitespace and normalized to their canonical spelling (`ModeQuick`/`ModeExtended`, also used at the `SetQuickMode` sites), and everything else is the existing 400 problem-details response naming the supported values. Successful quick/extended behaviour, metrics labels and response schemas are unchanged; the API guide documents the case-insensitive match and the 400.
- **Files:** `handlers.go`, `CLAUDE.md`, `mode_rdaybluex027_test.go` (new)
- **Verification:** Helper table: empty → extended; `quick`, `extended`, `QUICK`, `Extended`, padded `quick` → normalized; `quik`, `fast`, `quick,extended`, a 4,000-character token and a NUL-suffixed value rejected. Through both handlers with the validation seams installed: every invalid value is a 400 `application/problem+json` with status 400 and the supported values in `detail`, the seam is never called and the validation semaphore is empty afterwards; every valid value reaches the seam exactly once with the normalized mode and releases its slot. `go vet ./...` clean; `go test -race -count=1 ./...` green across the module.

## Verification notes

- Every finding above was verified with `go vet ./...` and `go test -race -count=1 ./...` in its module; both full suites were run again after the last group (validator green twice in a row; signer green for every package). Two pre-existing timing flakes, neither listed in the review and therefore left unchanged, were observed and reproduced independently of these changes:
  - signer `TestRA6X040_StartedHeartbeatIsTheOneShutdownStops` (root package) reports a data race between `Daemon.Run` starting the snapshot heartbeat client and a concurrently delivered `Daemon.Reload` when the reload arrives before `Run` has recorded that startup is in progress; it failed in two of the final full-suite runs, passes in isolation (18 of 18 targeted runs, one race in 60 iterations), and reproduces with the identical stack at the pre-group-6 commit `5b6f3b4` (one race in 60 iterations in a throwaway worktree, since removed).
  - validator `TestRA6X019_CancellationIsBounded` failed once while both module suites ran concurrently under the race detector and passed in every later run (5 of 5 targeted, then two full suites).
