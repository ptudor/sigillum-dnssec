# REVIEW_FABLE5_ULTRACODE — daemon/dnssec deep review

**Date:** 2026-07-10
**Reviewer:** Claude (Fable 5), multi-agent ultracode review
**Scope:** Full source of both projects — `signer` (dnssec-tudor) and `validator` — plus build system, docs, deployment artifacts.
**Mode:** Analysis only. No source files were modified.

## Method

1. **Ground truth:** `go build ./...`, `go vet ./...`, `gofmt -l .`, and `go test ./...` run clean in both projects (go1.26.4 darwin/arm64). Findings below are therefore invisible to the standard toolchain.
2. **Find phase:** 22 area-scoped reviewers (one per subsystem: signing pipeline, post-sign validation, key management, rollover/state, daemon lifecycle, config, registrar, web/ops, validator core/crypto/denial/DNS/HTTP/RDAP/frontend, build/tests, docs, plus dedicated concurrency, security, data-integrity, and RFC-compliance sweeps) each read their assigned files end-to-end.
3. **Gap-hunt phase:** 6 cross-cutting hunters (adversarial input, lifecycle, filesystem, time/serial arithmetic, cross-module contracts, resource limits) seeded with round-1 findings, filing only what round 1 missed.
4. **Completeness critic:** compared files-read against `git ls-files` and dispatched targeted finders for any uncovered area.
5. **Verify phase:** every finding independently and adversarially re-verified against the code before inclusion; severities re-judged. Findings that could not be fully confirmed are marked **Needs investigation** with the exact open question.

Prior reviews (`REVIEW_53.md`, `REVIEW_20260612.md`, `RFC_COMPLIANCE_REVIEW.md`, `docs/RFC_FIXES.md`) were consulted; resolved items are not re-reported unless a fix is incomplete or regressed.

## Status

> **IN PROGRESS** — signer subsystem reviewed and being verified; validator subsystem and cross-cutting sweeps in progress by direct read. Findings, summary table, and fix order land in subsequent commits.

## Findings

IDs are sequential and stable. Signer findings are R-001…R-078; validator findings continue from R-079; cross-cutting from R-090. Duplicate findings that multiple reviewers filed for the same root cause have been merged into a single ID (the merged sources are noted).

Every finding was re-verified against the current source during the review; line numbers are from the files as they exist at commit `7e27c0e`'s parent tree. Confidence is **confirmed** unless a finding is explicitly marked **Needs investigation**.

---

# Part 1 — `signer` (dnssec-tudor)

## Critical / High

### R-001 — No post-sign verification gate: a bogus signed zone ships to NSD unchecked
- **Severity:** High
- **Category:** correctness / data-integrity
- **Location:** `sign.go:132-142` (SignZone: signRecordsWithKeys → writeSignedZone), `sign.go:1000-1065` (writeSignedZone), `daemon.go:364-389` (sign then fire hook); contrast `validate.go` (live-DNS, post-publish, presence-only).
- **Problem:** `SignZone` goes straight from `signRecordsWithKeys` to `writeSignedZone` (atomic rename into the NSD include dir), and the daemon then fires the `post_sign` hook (`nsd-control reload`). Nothing verifies the produced records: no check that each RRSIG cryptographically verifies against the published DNSKEY RRset, no check that every authoritative RRset has a covering RRSIG, no NSEC/NSEC3 chain-closure check. The only pre-ship gates are `validateZone` (SOA count + NS-at-apex on the *unsigned input*) and `signingKeys.validateConsistency` (keytag-membership only, `sign.go:244`). The codebase itself documents a production outage from exactly this class (`sign.go:239-243`: a missing "append to dnskeys" line shipped a silent outage); `validateConsistency` narrowly guards that one mode. Any other signing bug — an RRset skipped by the occlusion logic, a broken NSEC3 chain, a wrong wildcard Labels count, a signature that doesn't verify — ships silently and SERVFAILs the zone at every validating resolver.
- **Evidence:** `sign.go:133-142` writes with no verification call between signing and `writeSignedZone`. The hand-rolled NSEC/NSEC3 chain builders (`generateNSECChain`, `generateNSEC3Chain`) are exactly the kind of code a self-verify would guard.
- **Fix specification:** Add a self-verification step in `SignZone` between `signRecordsWithKeys` and `writeSignedZone`, returning an error (which keeps the previous signed output in place, matching the project philosophy) when the produced zone is internally inconsistent. Minimum checks, all in-memory on `signedRecords` via miekg/dns: (1) for every RRSIG, find the DNSKEY in `keys.dnskeys` with the matching keytag/algorithm and call `rrsig.Verify(dnskey, rrset)` — catches wrong Labels, wrong OrigTtl, key mixups, corrupt signatures; (2) group authoritative records by `(name,type)` exactly as `signRecordsWithKeys` does and assert every non-occluded, non-delegation RRset (plus DS/NSEC at delegation points) has ≥1 covering RRSIG; (3) walk the NSEC/NSEC3 records and assert the Next pointers form a single closed cycle over every emitted owner name. Reuse `findDelegationPoints`/`isOccluded` so verifier and signer share one authoritative-data model (no differential parsing). **Must not change:** output path/format, atomic tmp+rename, state fields, TOML keys, keep-old-zone-on-error semantics. Default ON; a skip knob is optional.
- **Verification:** Unit test — break the signer in a test double (drop one RRset's RRSIG, or corrupt one NSEC NextDomain) and assert `SignZone` returns an error and the prior `.zone.signed` is untouched. Integration — sign testdata zones and confirm `ldns-verify-zone` agrees with the in-process verifier.

### R-002 — Shared deterministic `.tmp` path lets a concurrent daemon+CLI sign publish a corrupted signed zone (and corrupt state.json)
- **Severity:** High
- **Category:** concurrency / data-integrity
- **Location:** `sign.go:1013-1014` (`tempPath := path + ".tmp"`, `O_TRUNC`), `sign.go:1060` (rename); same pattern in `state.go:201-206` (`state.json.tmp`).
- **Problem:** `writeSignedZone` writes to a fixed temp path and renames it. The daemon's signing loop and the CLI (`sign`, `resign`, `add`, rollover commands) run in separate processes, and running the CLI while the daemon is up is an explicitly supported scenario (`State.ReloadFromDisk` exists precisely to merge "CLI resign/sign ran while daemon was running", `state.go:172`). If both processes sign the same zone concurrently, both open `<domain>.zone.signed.tmp` with `O_TRUNC`: process B's truncate zeroes process A's partial content while A's fd offset stays high, so A's later writes leave a NUL hole; whichever renames first publishes interleaved garbage. The post-sign hook then reloads the corrupt file. `State.Save` has the identical fixed-`.tmp` collision, where concurrent Saves corrupt or lose state.
- **Evidence:** `sign.go:1013-1014`; `state.go:201`. No flock or unique temp naming anywhere (grep for `flock`/`CreateTemp` is empty).
- **Fix specification:** In `writeSignedZone`, replace the fixed temp with `os.CreateTemp(filepath.Dir(path), "."+filepath.Base(path)+".*.tmp")`, keep the existing Chmod(0644)/header/records/Sync/Close/rename, and `os.Remove(f.Name())` on any error. Apply the same unique-temp pattern to `State.Save` (preserving `writeFileOwned`/`renameOwned` ownership semantics). **Must not change:** final output path `<output_dir>/<domain>.zone.signed`, the 0644 mode NSD depends on, atomic-rename publish semantics, or state.json format. Unique temp names are required regardless of any lock; an advisory flock (see R-007) is complementary hardening.
- **Verification:** Two goroutines call `writeSignedZone` for the same path with different record sets in a loop; after each call, parse the file with `dns.ZoneParser` and assert zero parse errors and content matching exactly one writer. Fails within a few iterations before the fix; never after. `ls` the output dir for leaked temp files.

### R-003 — Rollover signing branches warn-and-continue when the old key can't load, publishing a DNSKEY RRset validators still require (silent bogus zone)
- **Severity:** High
- **Category:** error-handling / correctness
- **Location:** `sign.go:289-299` (KSK ds_add_wait), `sign.go:328-338` (ZSK signing phase), `sign.go:340-359` (algorithm rollover); contrast fatal `sign.go:317-320` (ZSK pre-publish). Merges the two independently-filed reports (sign + rollover-state).
- **Problem:** During KSK `ds_add_wait` the parent DS still references (often *only* references) the old KSK. If `loadKeyPairByID` for the old KSK fails (backup deleted/unreadable, or the `.bak` fallback at `keys.go:209-214` fired), the code logs `slog.Warn` and signs anyway, publishing a DNSKEY RRset *without* the old KSK → every resolver validating via the old DS gets bogus/SERVFAIL. `validateConsistency` can't catch it (the old KSK is in neither the signing set nor `dnskeys`). Same warn-and-continue in the ZSK signing phase (drops old ZSK while caches hold its RRSIGs) and the algorithm branch (dropping old-algorithm keys additionally violates RFC 4035 §2.2 / RFC 6840 §5.11). The ZSK pre-publish branch was deliberately made fatal after a production outage with a comment explaining this exact class; the other three were left inconsistent.
- **Evidence:** `sign.go:290-292` `slog.Warn("[SIGN] Failed to load old KSK for rollover signing", ...)` then `else { … }`; parallel at 331-333 and 343-344/353-354. Contrast the deliberate `return nil, fmt.Errorf("loading old ZSK %d for pre-publish: %w", …)` at 318-319.
- **Fix specification:** Make all three branches fatal (return a wrapped error), matching pre-publish. `SignZone`'s caller already `AddError`s and keeps the previous signed output serving — the correct failure mode. Name `rollover complete` in the error text for operators whose old keys are legitimately gone. The ZSK signing-phase branch only needs the public half, so it may use `LoadPublicKeyByID` — but a failure must still be fatal. Additionally make `backupKey` failures at rollover *start* (`rollover.go:46-48, 163-165, 304-309`) abort the start (they are what these branches depend on). **Must not change:** rollover state strings, backup naming `<domain>.<type>.<tag>.{key,private}`, key-set semantics, the already-fatal pre-publish branch.
- **Verification:** Start a KSK rollover (and an algorithm rollover), delete the keytag-named backup files, call `SignZone`; assert it errors and the prior signed file is byte-identical. Repeat for the ZSK signing phase.

### R-004 — `ReplaceDS` can strand a zone with zero DS at the parent when the restore PUT fails after the DELETE
- **Severity:** High
- **Category:** correctness / data-integrity
- **Location:** `registrar_dynadot.go:521-537`, `registrar_cli.go:19-21` (60s shared ctx), `registrar_cli.go:262-274` (stderr-only warning).
- **Problem:** `ReplaceDS` = PUT (verify publish works) → DELETE (wipe) → PUT (restore). PUT-first (commit f549fa9) protects against systematic failures, but nothing protects the DELETE→restore-PUT window against transient failures: a 5xx, reset, context deadline, or process kill after the DELETE succeeds leaves the registrar holding zero DS — the "silent DNSSEC outage" the comment itself describes (parent downgrades to insecure delegation). Three aggravators: (1) the restore reuses the caller's context — `registrarContext()` is a flat 60s deadline shared across the whole sequence and the rate-limiter's `gate()` sleeps burn it without observing cancellation; (2) no retry of the restore PUT; (3) in `maybeAutoPublishDS` the error surfaces only as `warning: registrar auto-publish failed` on stderr, indistinguishable from the harmless "nothing happened" case, and nothing is persisted, so `status` keeps reporting the zone healthy while it has no DS.
- **Evidence:** `registrar_dynadot.go:528-536`; `registrar_cli.go:20` `context.WithTimeout(context.Background(), 60*time.Second)`; `registrar_cli.go:271-273` stderr-only.
- **Fix specification:** After a successful DELETE, make the restore maximally failure-proof: (a) run restore `AddDS` on a context detached from the caller — `context.WithoutCancel(ctx)` + a fresh generous timeout (~2m); (b) retry the restore with short backoff (3 attempts); (c) on final failure return a distinguishable sentinel error (`ErrRegistrarDSEmpty`); (d) in `maybeAutoPublishDS`/`runRegistrarPush`/`runRegistrarClear`, on that sentinel log at Error with explicit remediation and persist a zone warning (see R-032). Stop using one flat 60s deadline for a multi-call sequence. **Must not change:** PUT-first order (pinned by `TestDynadotReplaceDS_CallOrder`), `Registrar` interface, one-PUT-per-record wire behavior, empty-records==clear semantics, TOML keys.
- **Verification:** httptest tests — (1) restore PUT 500s twice then succeeds → `ReplaceDS` returns nil, final server set == `records`; (2) restore fails permanently → error unwraps to the sentinel and CLI writes the zone warning; (3) cancel parent ctx right after DELETE → restore PUT is still issued.

### R-005 — `serve --web` bypasses the R53-001 loopback guard, exposing the unauthenticated dashboard on all interfaces
- **Severity:** High
- **Category:** security
- **Location:** `main.go:401-406` (flag override after Validate), `config.go:404-408` (guard only in Validate), `dnssec_signer.rc.d`, `apache.conf.example`, `README.md`/`CLAUDE.md` examples. Merges web-ops + config reports.
- **Problem:** R53-001 added `Web.AllowRemote` + `validateLoopbackAddr` in `Config.Validate()`. But `runServe` applies the `--web` flag *after* `loadConfigAndState()` has already run Validate (`main.go:403-406` sets `cfg.Web.Enabled = true; cfg.Web.Listen = webAddr` with no re-check), and there is no bind-time guard. There is no auth at all, so `dnssec-tudor serve --web :8053` binds `0.0.0.0` with zero authentication — and every shipped doc recommends exactly this form (rc.d `dnssec_signer_web=":8053"`, apache.conf.example "start with `--web :8053`", README `--web :8080`). The claimed R53-001 fix is incomplete.
- **Evidence:** `main.go:403-406`; the only enforcement is `config.go:404-413` inside `Validate`, not reached by the flag path.
- **Fix specification:** In `runServe`, after applying the override, re-check: `if cfg.Web.Enabled && !cfg.Web.AllowRemote { if err := validateLoopbackAddr(cfg.Web.Listen, "--web"); err != nil { return err } }` (or re-call `cfg.Validate()`). Keep `allow_remote = true` as the explicit opt-out (don't change TOML keys or the flag). Update the three docs to loopback forms (`127.0.0.1:8053`). **Must not change:** config semantics; the flag must still enable the UI on a loopback address with no config change.
- **Verification:** `./dnssec-tudor serve --config testdata/config.toml --web :8053` with `allow_remote` unset must exit non-zero; with `allow_remote = true` it starts; `--web 127.0.0.1:8053` works without the flag.

### R-006 — After SIGHUP reload, web UI and health endpoints serve frozen pre-reload state; `/health` goes falsely 503 within `signature_validity`
- **Severity:** High
- **Category:** correctness / error-handling
- **Location:** `daemon.go:113-128` (Reload swaps `d.cfg`/`d.state`), `daemon.go:402-404` (`NewWebServer(d.cfg, d.state)` at startup), `daemon.go:427` (`RegisterHealthHandlersWithDaemon(mux, d.state, d.cfg, d)`), `web.go:46-67` (handler closures capture cfg/state), `health.go:47-160`. Merges daemon + web-ops reports.
- **Problem:** `Reload` replaces `d.cfg`/`d.state` with freshly loaded objects (`main.go:439` loads a new State), but both HTTP servers captured the *original* pointers at startup and the handler closures hold them forever. The signing loop is reload-safe (`takeSnapshot`), so it re-signs only the *new* state; the old state's `SignaturesExp` values then age out. Consequences after any SIGHUP (a documented routine flow — `add` prints "picks up the new zone after SIGHUP"): (1) dashboard/`/api/status` show pre-reload zones/serials and never show newly added zones; (2) new signing errors are invisible to `/health`; (3) roughly `signature_validity` (default 14d) after the SIGHUP, `now.After(zone.SignaturesExp)` becomes true for every frozen zone and `/health`+`/healthz` return **permanent 503 UNHEALTHY** with false `expired_signatures` until restart. `hardening_test.go` has no reload test at all.
- **Evidence:** `daemon.go:403` `d.server = NewWebServer(d.cfg, d.state)`; `web.go:50-64` closures over `cfg,state`; `daemon.go:427`.
- **Fix specification:** Make handlers resolve cfg/state per request via the Daemon (add an accessor under `d.mu.RLock` or reuse `takeSnapshot`; pass `*Daemon` into `NewWebServer` and `RegisterHealthHandlersWithDaemon`; each handler calls the accessor at the top). Also fix `daemon.go:429`'s unlocked read of `d.cfg.Health.DebugVars`. **Must not change:** endpoint paths, JSON schemas, listen addresses, the "listen change requires restart" rule, security headers/method restrictions, the deep-copy discipline.
- **Verification:** Test — build a Daemon, `Reload()` with a state whose zones have fresh `SignaturesExp`, invoke `healthHandler` via httptest and assert it reflects the *new* state; assert `/api/status` shows only new-state zones. Manual — SIGHUP after `add`, confirm the dashboard lists the new zone without restart.

### R-007 — No cross-process coordination on state.json: a mid-cycle CLI write is clobbered by the daemon's end-of-cycle Save (lost update); rollover state lost while key files are already rotated
- **Severity:** High
- **Category:** concurrency / data-integrity
- **Location:** `state.go:150-185` (ReloadFromDisk merge), `state.go:188-212` (Save whole-map overwrite), `daemon.go:265` (ReloadFromDisk), `daemon.go:296` (Save), `main.go:786-836` (rollover start). Merges rollover-state High + daemon Medium.
- **Problem:** The daemon does read-modify-write on state.json with no inter-process lock: `ReloadFromDisk` at cycle start, whole-map `Save` at cycle end. The merge only *adds* disk zones or adopts fresher-`LastSigned` ones; it never removes, and Save serializes the entire in-memory map. Any CLI write landing after a cycle's `ReloadFromDisk` and before its `Save` is clobbered. Most dangerous: a `rollover start` in that window writes the new-KSK + Rollover state and re-signs, then the cycle's Save reverts state.json to the pre-rollover snapshot — but the key *files* on disk already hold the new KSK (old one moved to the tag-suffixed backup). The next natural re-sign loads the new KSK with no rollover state → the old KSK vanishes from the DNSKEY RRset while the parent DS still references it → SERVFAIL; `rollover complete` then fails with "no rollover in progress". (`remove`, R-008, is the deterministic version of this same lost-update.)
- **Evidence:** `state.go:179` merge condition `diskZone.LastSigned.After(memZone.LastSigned)`; `daemon.go:296` unconditional `snap.state.Save()`. No flock anywhere.
- **Fix specification:** Introduce an advisory file lock (`syscall.Flock` on `<data_dir>/state.json.lock`, blocking with timeout) held around (a) the daemon's whole `signAllZones` load-mutate-save span and (b) every mutating CLI command's load→Save span (rollover start/complete/algorithm, add, import, remove, resign, sign). Defense-in-depth: re-run `ReloadFromDisk` immediately before the daemon's Save, and extend the merge to adopt a disk zone whose `Rollover` differs when disk `LastSigned >= mem LastSigned`. Lock-acquisition failure in the daemon must log and skip the cycle (never crash the loop); the CLI waits briefly then fails clearly. Read-only commands (status/ds/dnskey) don't need the exclusive lock. **Must not change:** state.json schema/format, the atomic tmp+rename, ownership behavior, the fresher-LastSigned merge rule.
- **Verification:** Stress test — daemon with a 1s poll over ~50 zones while repeatedly running `rollover start`/`complete` on one zone; after each CLI command, assert state.json retains the expected Rollover on the next two cycles. Fails intermittently before the fix.

### R-008 — `remove` is non-transactional with the config file; a running daemon re-adds the zone next cycle (and `RemoveZoneFromConfig` is dead code)
- **Severity:** High
- **Category:** correctness
- **Location:** `main.go:760-783` (runRemove), `config.go:510-514` (dead `RemoveZoneFromConfig`), `daemon.go:278` + `daemon.go:344-360` (re-adoption). Merges config High + daemon Medium + rollover-state Low.
- **Problem:** `dnssec-tudor remove` only deletes the zone from state.json and prints "You should manually remove the zone from config.toml." It never touches the config file. The daemon's loop iterates `snap.cfg.Zones` (which still contains the domain); for a config zone with no state entry it calls `recoverOrGenerateKeys`, recovers the still-on-disk keys, recreates the ZoneState, re-signs, and saves — so within one poll interval (default 5m) the removed zone is fully re-managed. `RemoveZoneFromConfig` (which even comments "This does NOT modify the config file") has zero callers (grep confirms). README/CLAUDE.md describe `remove` as "stops managing", which is false while a daemon runs. REVIEW_53 R53-008 claimed this "transactional" — incomplete.
- **Evidence:** `main.go:773` `state.RemoveZone(domain)`; `daemon.go:348-360` re-adoption; dead `config.go:512`.
- **Fix specification:** Implement a config-file removal counterpart to `AddZoneToConfigFile` that deletes the `[zones."<domain>"]` table (preserve unrelated content/comments; atomic tmp+rename + ownership helpers), call it from `runRemove` before state removal (preflight writability, then config, then state, unwind on failure), and update the printed note to say a running daemon needs SIGHUP. Delete or wire up `RemoveZoneFromConfig`. If config editing is deliberately out of scope, `remove` must instead *fail* when the domain is present in config, telling the operator to delete the block — silent success that reverts in 5 minutes is unacceptable. **Must not change:** keys are never deleted; `add` after `remove` still works. Update README/CLAUDE.md.
- **Verification:** With `serve` running, `remove` a zone; assert it stays gone from state.json across two cycles and (after config edit + SIGHUP) from status; regression test that the config rewrite preserves other zones.

### R-009 — Key pair files written non-atomically and never cross-checked: a crash or concurrent read can pair a new `.key` with an old `.private` and silently sign bogus
- **Severity:** High
- **Category:** data-integrity
- **Location:** `keys.go:156-191` (saveKeyFiles), `keys.go:177`/`185` (two independent `writeFileOwned`), `ownership.go:80-86` (`writeFileOwned` = `os.WriteFile`), `keys.go:441-468` (load without cross-check), `sign.go:701-728` (ECDSA derives pubkey from priv, so mismatch "succeeds").
- **Problem:** `saveKeyFiles` writes `.key` then `.private` as two independent `os.WriteFile` calls (truncate-in-place, no temp+rename, no fsync) — while `state.go` deliberately uses temp+rename for state.json, the *most* safety-critical files do not. During rollover, `backupExistingKeyFiles` skips its rename (backup already exists from `rollover.backupKey`'s copy), so both live files are overwritten in place. A crash between the two writes, or the daemon's signing loop (separate process, 5m poll) reading the pair mid-write, yields a new public DNSKEY with the old private key. Nothing verifies the halves correspond: `loadKeyPairFromPath` just parses both, `validateConsistency` compares only key tags, and the ECDSA signing path derives the public point from the private scalar so signing "succeeds" — producing RRSIGs whose keytag references a published DNSKEY they don't verify against → SERVFAIL with no local signal.
- **Evidence:** `ownership.go:81` `os.WriteFile(path, data, perm)`; only fsync in the project is `sign.go:1053` (signed zone).
- **Fix specification:** Two layers, both needed. (1) Make each key-file write atomic: write `<path>.tmp` in the same dir (fresh file → 0600/0644 actually applied), fsync, rename over the destination (reuse `writeFileOwned`+`renameOwned` for chown); write `.private` first, then `.key`. (2) Add a pub/priv correspondence check in `loadKeyPairFromPath` before returning: ED25519 — require len 64 and bytes 32..64 == decoded `DNSKEY.PublicKey` (or `ed25519.NewKeyFromSeed` for a 32-byte seed per R-010); ECDSA — require len(D) 32/48 and `ScalarBaseMult(D) == (X,Y)`; error on mismatch so `SignZone` fails and the prior zone keeps serving. **Must not change:** file names/format, modes, `LoadKeyPair`/`loadKeyPairFromPath` signatures; the check must accept all currently-generated keys (64-byte ED25519, 32/48-byte ECDSA).
- **Verification:** Overwrite only the `.key` with a second key's `.key`; assert `LoadKeyPair` errors (not a mismatched pair) and `SignZone` fails leaving prior output intact. Grep that no code path calls `os.WriteFile` on a final key path. Existing key/sign tests must pass.

### R-010 — Importing a standard BIND/ldns ED25519 key (32-byte seed, RFC 8080) panics the signer
- **Severity:** High
- **Category:** correctness (crash)
- **Location:** `sign.go:703-704` (`rrsig.Sign(ed25519.PrivateKey(privateKey), rrset)`), `keys.go:488-495` (`parsePrivateKeyFromFile`, no length check), `main.go:1157/1166/1219` (import → save → sign).
- **Problem:** BIND/ldns/RFC 8080 store the ED25519 private key as a 32-byte seed in the `PrivateKey:` field. `parsePrivateKeyFromFile` returns the raw decoded bytes with no validation; `runImport` saves them and calls `SignZone`; `signRRSIG` passes a 32-byte `ed25519.PrivateKey` to crypto/ed25519, whose `Sign` **panics** with "ed25519: bad private key length" when len ≠ 64. Internal keys avoid this only because `generateDNSSECKey` stores the full 64-byte value (which is itself non-standard vs BIND). So `dnssec-tudor import example.com zone.db --ksk Kexample.com.+015+NNNNN …` writes the key files and then panics at `main.go:1219` — CLI crashes with a backtrace, state.json unwritten, half-imported files left. If the zone later reaches the daemon, `checkAndSignZoneSafe`'s recover turns it into a per-cycle panic and the zone never signs.
- **Evidence:** `keys.go:490-491` decodes verbatim; `sign.go:704`. Confirmed: generation stores 64 bytes (`keys.go:116-117` `privateKey = priv`), import yields 32.
- **Fix specification:** In `signRRSIG`'s ED25519 case: if `len==ed25519.SeedSize` (32) expand via `ed25519.NewKeyFromSeed`; if `len==64` use as-is; else return an error (never reach the panic). Add length validation to the ECDSA branches (32/48). In `runImport`, validate the loaded pair before `saveKeyFiles` (derive pubkey from priv and compare to the parsed DNSKEY). **Must not change:** on-disk format of already-generated keys (existing 64-byte ED25519 must keep loading/signing), import flags, `loadBindKeyPair` layout. Optionally (separate decision) also *write* new ED25519 `.private` as the 32-byte seed for true BIND compatibility — only alongside load-time acceptance of both lengths.
- **Verification:** Test that writes a 32-byte-seed BIND ED25519 `.private` + matching `.key`, runs import+sign, asserts no panic and the SOA RRSIG verifies. Test that a garbage-length key yields an error from `signRRSIG`, not a panic.

### R-011 — ZSK rollover phase transitions gated on wall-time-since-start only; phases collapse after downtime or signing failure, dropping/switching keys faster than cache TTLs allow
- **Severity:** High
- **Category:** correctness / rfc-compliance
- **Location:** `rollover.go:193-253` (`handleZSKRolloverState`), specifically `rollover.go:196` (`daysSinceStart := now.Sub(rollover.Started)`), `:205`, `:235`.
- **Problem:** Transitions advance on `daysSinceStart` measured from `Rollover.Started` only; nothing verifies the previous phase's DNSKEY set was actually published (via `LastSigned`) for at least the DNSKEY TTL. If the daemon is down or signing is failing (e.g. a zone-file syntax error keeps `SignZone` erroring while the state machine still advances on wall clock) across the switch window, a restart satisfies both conditions at once: the switch fires in one poll cycle and completion the next. Harms: (a) if the daemon died after `startZSKRollover` but before the next sign, the new ZSK was never pre-published and the collapsed switch signs with a key validators never cached → bogus for up to DNSKEY TTL; (b) even with the pre-publish zone served throughout, the collapsed signing phase removes the old ZSK from the RRset ~one poll after the RRSIG switch while resolvers hold cached old-ZSK RRSIGs → bogus. Violates the RFC 7583 pre-publish model the 7/14-day defaults exist to satisfy.
- **Evidence:** `rollover.go:205` `if daysSinceStart >= switchDays`; `:235` `if daysSinceStart >= prepublishDays`.
- **Fix specification:** Add an optional `phase_started` field to `RolloverState` (fall back to `Started` when zero, for old state files). Set it on the pre_publish→signing transition. Gate each transition on ALL of: (1) `now - phaseStart >= phase duration`; (2) `LastSigned > phaseStart` (the phase's key set was actually published); (3) `now - LastSigned >= DNSKEY TTL floor` (use `cfg.DNSSEC.DNSKEYTtl`, or a conservative 24h/2×TTL when 0). **Must not change:** existing JSON field names, the `pre_publish`/`signing` state strings, the meaning of `rollover_prepublish`/`rollover_switch`, `ForceResign` semantics.
- **Verification:** Unit test — zsk rollover in pre_publish with `Started` 30d ago and `LastSigned` older than `Started`; running `CheckZSKRollover` twice must not reach complete until the zone was signed in pre_publish and the TTL floor elapsed after entering signing. Round-trip an old-schema state.json and confirm transitions still occur.

### R-012 — `dnssec-tudor validate` treats mere RRSIG presence as pass: a zone serving fully-expired signatures reports overall "pass"
- **Severity:** High
- **Category:** correctness (false operational signal)
- **Location:** `validate.go:342-393` (`checkRRSIGPresent`), specifically `:357-362` and `:370-375`.
- **Problem:** The RRSIG check only tests that an RRSIG with the right `TypeCovered` exists; it never inspects `Expiration`/`Inception` and never verifies the signature. Expired signatures are the single most common DNSSEC outage, and catching them is this tool's stated purpose. Realistic scenario: the daemon re-signs on schedule but the `post_sign` hook silently fails (broken `nsd-control`), so NSD serves a stale signed zone whose RRSIGs expire two weeks later. Then DS check = pass, DNSKEY check = pass (tags unchanged), RRSIG check = pass (expired RRSIGs are still *present*), SOA check = pass under default `serial_policy="keep"` → overall "pass" while every validating resolver SERVFAILs. It also never checks that the RRSIG's KeyTag corresponds to a locally-known key, so signatures from a stale/foreign key count.
- **Evidence:** `validate.go:358` `if rrsig, ok := rr.(*dns.RRSIG); ok && rrsig.TypeCovered == dns.TypeSOA { result.SOASigned = true; break }` — no temporal check.
- **Fix specification:** For each matching RRSIG evaluate temporal validity with RFC 1982 arithmetic on the 32-bit timestamps (use `rrsig.ValidityPeriod(time.Now())`, not naive uint32 compare). An out-of-window RRSIG must not set `SOASigned`/`DNSKEYSigned`; when all are expired/not-yet-valid set `Status="fail"` naming the expiry time; consider "partial" when within `signature_refresh`. When local keys are loadable, prefer counting only RRSIGs whose KeyTag matches. **Must not change:** JSON field names or the `{pass,partial,fail,error}` vocabulary consumed by the web UI / `computeOverall`.
- **Verification:** Extend `validate_test.go` to serve an RRSIG with `Expiration` in the past → check reports fail; within-window → pass.

## Medium

### R-013 — `poll_interval` (and shutdown/query/registrar timeouts) never validated: zero/negative panics `time.NewTicker` at startup and crashes a running daemon on SIGHUP
- **Severity:** Medium
- **Category:** error-handling (crash)
- **Location:** `config.go:296-416` (Validate omits these), `daemon.go:209` (`time.NewTicker`), `daemon.go:222` (`ticker.Reset`).
- **Problem:** `Validate()` checks positivity only for the four `[dnssec]` durations. `poll_interval = "0s"` or `"-5m"` passes (Duration.UnmarshalText returns negatives). `NewTicker(≤0)` panics at startup; and on SIGHUP a bad value reaches `ticker.Reset` inside `runSigningLoop`, whose panic is *not* covered by `signAllZonesSafe`'s recover, killing the running daemon. `health.shutdown_timeout` ≤0 → instant non-graceful shutdown; `validate.timeout`/`registrar.dynadot.timeout` = 0 → http.Client "no timeout".
- **Fix specification:** In `Validate()` reject `PollInterval ≤ 0`, `Health.ShutdownTimeout ≤ 0`, `Validation.Timeout ≤ 0`, `Registrar.Dynadot.Timeout ≤ 0` (all default positive; LoadConfig unmarshals over DefaultConfig so absent keys keep defaults). Reject loudly; no clamping; no default/key changes.
- **Verification:** `poll_interval = "0s"` → `serve` exits with "poll_interval must be positive" (no stack trace); SIGHUP with the bad value → reload fails, daemon keeps running.

### R-014 — `rollover_prepublish`/`rollover_switch` accept negative/zero/inverted values, collapsing the ZSK pre-publish safety timeline
- **Severity:** Medium
- **Category:** error-handling / rfc-compliance
- **Location:** `config.go:296-348` (no check), `rollover.go:146`, `rollover.go:199-235`. Merges config + rollover-state.
- **Problem:** Neither duration is validated and there is no ordering check. `rollover_switch >= rollover_prepublish` (e.g. swapped 14d/7d) makes the switch fire at `daysSinceStart >= switchDays` while `daysSinceStart >= prepublishDays` is already true, so completion removes the old ZSK on the next poll (~5m later) while resolvers hold the cached DNSKEY RRset without the new ZSK → bogus for up to DNSKEY TTL. Zero/negative switch → immediate switch, no dwell. Negative prepublish → the auto-rollover only triggers after the ZSK is already expired. The pre-publish Action message also hardcodes "in 7 days" regardless of config (`rollover.go:182`).
- **Fix specification:** In `Validate()` require `rollover_prepublish > 0`, `rollover_switch > 0`, `rollover_switch < rollover_prepublish` (optionally `rollover_prepublish < zsk_lifetime`), with messages showing both values. Build the Action string from `RolloverSwitch` instead of the literal. No default/key changes.
- **Verification:** `rollover_switch="14d", rollover_prepublish="7d"` fails Validate naming both keys; `-1d` fails; defaults pass.

### R-015 — Unknown/misspelled TOML keys silently ignored (go-toml/v2 without `DisallowUnknownFields`), silently reverting crypto-timing/security settings to defaults
- **Severity:** Medium
- **Category:** error-handling
- **Location:** `config.go:284` (`toml.Unmarshal`).
- **Problem:** `toml.Unmarshal` ignores keys that don't map to a struct field. Empirically confirmed: `signture_validity = "14d"` parses with `err=nil` and the default is used. Any typo — `nsec3_iteration`, `serial_polic`, a mis-placed key in the wrong table, `[dnsssec]` — is accepted with no diagnostic; the operator believes the setting is active. For a signing daemon this silently changes signature validity, rollover timing, or serial policy. This is also the mechanism behind R-016 and R-054.
- **Fix specification:** Switch `LoadConfig` to strict decoding via `toml.NewDecoder(...).DisallowUnknownFields()` and surface `*toml.StrictMissingError`. Every key in testdata/README/QUICKSTART already maps to a struct tag (verified), so valid configs keep loading. SIGHUP reloads with stray keys will now be rejected (the reload path logs and keeps the old config — desired).
- **Verification:** `LoadConfig` on TOML with `signture_validity` returns an error naming the key; testdata/config.toml still succeeds.

### R-016 — `digest_type` documented under `[registrar.dynadot]` but parsed only at `[registrar]` — following the doc silently yields SHA-256 pushes when SHA-384 was intended
- **Severity:** Medium
- **Category:** docs / correctness
- **Location:** `CLAUDE.md:169` (signer), `config.go:34` (`DigestTypeVal` on `RegistrarConfig`), `config.go:40-47`.
- **Problem:** The only doc for `digest_type` places `# digest_type = 2` inside `[registrar.dynadot]`, but the struct declares it on `RegistrarConfig` (the `[registrar]` table); `RegistrarDynadotConfig` has no such field. Because unknown keys are ignored (R-015), an operator who sets `digest_type = 4` under `[registrar.dynadot]` gets no error and `DigestType()` falls back to 2 — DS pushed with SHA-256 while they believe SHA-384. README/QUICKSTART document no `[registrar]` at all.
- **Fix specification:** Fix the CLAUDE.md example to show `digest_type` under a `[registrar]` table, and add a `[registrar]`/`[registrar.dynadot]` section to README. Do **not** move the field (registrar-level is the deliberate registrar-agnostic design; moving it breaks configs). Adopting R-015 turns the wrong-table placement into a load-time error.
- **Verification:** Paste the corrected example (digest_type=4) → `cfg.Registrar.DigestType()` returns 4.

### R-017 — Relative zone paths written verbatim; one missing zone file is fatal for the whole daemon at startup; non-ENOENT stat errors silently ignored
- **Severity:** Medium
- **Category:** error-handling / correctness
- **Location:** `config.go:366-386` (Validate), `main.go:637/681` (add/import store the arg verbatim), `config.go:493-508` (AddZoneToConfigFile).
- **Problem:** Three gaps. (1) `add`/`import` record the zonePath as given; a cwd-relative path succeeds at add time but breaks when the daemon later starts from a different working directory (systemd with no `WorkingDirectory` → `/`) — `Validate`'s `os.Stat` fails and the daemon refuses to start; no tilde expansion either. (2) `Validate` returns an error for any single missing zone file, so one deleted/renamed/unmounted zone at restart prevents the daemon from starting at all — under `Restart=on-failure` this is a restart loop while every zone's signatures march toward expiry (total outage from one bad entry), the opposite of runtime behavior where per-zone errors don't stop others. (3) `os.IsNotExist` is the only stat error handled; a permission-denied stat passes and only fails later at sign time.
- **Fix specification:** (1) `filepath.Abs(zonePath)` in `runAdd`/`runImport` right after arg parsing (keep accepting relative paths at load, just stop creating them). (2) In `Validate`, demote missing-zone-file to a recorded warning for the `serve` path so the daemon starts and signs the other zones, surfacing the broken one via status/health. (3) Treat any non-ENOENT stat error as the same warning, naming it. Don't change AddZoneToConfigFile's append format or the `[zones."domain"]` quoting.
- **Verification:** `add` with a relative path → appended config line is absolute. Two zones, delete one file, `serve` → daemon starts, signs the survivor, reports the missing one.

### R-018 — Data race on `d.heartbeat` (read unlocked in the signing path, written under lock in Reload); Reload holds the mutex across synchronous heartbeat network I/O
- **Severity:** Medium
- **Category:** concurrency
- **Location:** `daemon.go:342/367/375` (unlocked reads), `daemon.go:118-127` (Reload under lock), `daemon.go:247-256` (`takeSnapshot` omits heartbeat), `daemon.go:406/429` (unlocked cfg reads). Merges daemon + web-ops.
- **Problem:** `takeSnapshot` deliberately captures cfg/state/signer/rollover under `d.mu`, but `d.heartbeat` was left out: `checkAndSignZone` dereferences it directly while `Reload` reassigns it under lock — a data race (`go test -race` would flag it). Same class for `runWebServer`/`runHealthServer` reading `d.cfg` unlocked. Also `Reload` holds `d.mu.Lock()` across `d.heartbeat.Stop()`, which synchronously sends a "stopping" heartbeat with a 10s HTTP timeout — a SIGHUP with an unreachable endpoint stalls `takeSnapshot` (and the signing loop) for up to ~20s.
- **Fix specification:** Add `heartbeat` to the snapshot struct and use `snap.heartbeat` in `checkAndSignZone`. In `Reload`, capture the old client under the lock but call `Stop()` after releasing `d.mu`. Copy needed cfg fields into locals inside the locked sections in the two server goroutines. **Must not change:** heartbeat wire actions, listen-change-requires-restart, tickerReset behavior.
- **Verification:** `go test -race` driving `signAllZonesSafe` in a loop on one goroutine and `Reload()` on another → silent. SIGHUP with a blackholed heartbeat no longer freezes the signing loop.

### R-019 — Graceful shutdown can wait unboundedly (no ctx check between zones) and ignores repeated signals; SIGKILL then loses the cycle's state updates
- **Severity:** Medium
- **Category:** concurrency / error-handling
- **Location:** `daemon.go:258-308` (signAllZones), `main.go:445-453` (signal loop blocks on errCh), `heartbeat.go:58-103` (synchronous send).
- **Problem:** SIGTERM cancels `d.ctx`, but the in-flight `signAllZones` iterates every zone (sign + per-zone heartbeat HTTP) without checking `d.ctx.Done()` between zones, and `Save` only happens at cycle end. With many zones or a blackholed heartbeat (each `Send` blocks up to 10s, twice per zone), shutdown can take minutes. Meanwhile `runServe`'s signal loop, after the first signal, blocks on `<-errCh` and never reads `sigCh` again — repeated Ctrl-C does nothing; `signal.Notify` keeps default terminate disabled. `TimeoutStopSec` expires → SIGKILL mid-cycle loses all of the cycle's `LastSigned`/`PublishedSerial`/`SignaturesExp`/rollover updates (nothing saved incrementally) and may skip the coalesced hook.
- **Fix specification:** (1) In `signAllZones`, check `d.ctx.Done()` at the top of each zone iteration (both loops) and break, then still run the final `Save` and coalesced hook for zones already signed. (2) In `runServe`, keep consuming `sigCh` while waiting for `errCh`; on a second signal log "forcing exit" and return/`os.Exit(1)`. **Must not change:** the save-before-coalesced-hook guarantee for an uninterrupted cycle, SIGHUP semantics, exit-code behavior.
- **Verification:** `serve` with `poll_interval=1s`, many zones, a sleeping hook; SIGTERM mid-cycle → prompt exit with state.json holding entries for zones signed before interruption; two SIGTERMs against a hung hook → second terminates.

### R-020 — Web server has no ctx-based shutdown path: SIGTERM in the startup window leaves `ListenAndServe` running forever and `wg.Wait()` hangs
- **Severity:** Medium
- **Category:** concurrency / lifecycle
- **Location:** `daemon.go:92-110` (Shutdown), `daemon.go:394-411` (runWebServer), contrast `daemon.go:443-450` (health server's ctx watcher).
- **Problem:** The health server watches `d.ctx.Done()` and shuts itself down; the web server's only shutdown trigger is `Shutdown()` reading `d.server` under `d.mu`. If a shutdown signal lands before `runWebServer` reaches line 403, `Shutdown()` sees `d.server == nil` and skips it; `runWebServer` then starts `ListenAndServe` on an already-cancelled daemon and nothing ever shuts it down → `d.wg` never drains, `Run()` blocks on `wg.Wait()`, `runServe` blocks on `<-errCh` with signals no longer read → hang until SIGKILL.
- **Fix specification:** In `runWebServer`, after storing `d.server`, check `d.ctx.Done()` non-blocking and return without serving if cancelled; add the same ctx-watcher goroutine the health server uses (`go func(){ <-d.ctx.Done(); srv.Shutdown(timeoutCtx) }()`) with `Health.ShutdownTimeout`. Double `http.Server.Shutdown` is safe. **Must not change:** listen semantics, `ErrServerClosed` handling, wg accounting.
- **Verification:** Test — create a Daemon, `Shutdown()` immediately, then `Run()`; assert it returns within the shutdown timeout. Manual — `serve --web … & kill %1` in a tight loop always exits.

### R-021 — No double-start protection; non-fatal listen-bind failures let a second instance sign headlessly and hide a misconfigured listen address
- **Severity:** Medium
- **Category:** correctness / concurrency
- **Location:** `daemon.go:408-410` (web bind error logged only), `daemon.go:453-455` (health bind error logged only), `daemon.go:53-89` (Run continues regardless).
- **Problem:** No PID file, flock, or single-instance guard anywhere. A second `serve` (operator runs it while the systemd unit is active) fails to bind web/health but both failures are only logged and the goroutines return with `wg.Done` — so `Run()` continues and two daemons concurrently sign the same zones, double-fire hooks, race state.json Saves (amplifying R-007), and under `serial_policy=epoch` can publish divergent serials. The same non-fatal handling means a typo'd `web.listen`/`health.listen` leaves the daemon running with no health endpoint / no metrics while believing itself healthy.
- **Fix specification:** Treat a failed *initial* bind as fatal: `net.Listen` explicitly before starting each server; if the always-on health listener fails, return the error from `Run()` (process exits non-zero — a dead health port is a startup failure); same for the web listener when enabled. This also gives implicit double-start protection (the second instance can't bind the health port). Optionally add a lifetime flock on `state.json.lock`. **Must not change:** default listen addresses, R53-001 loopback enforcement, graceful-shutdown; later-reload bind failures stay warnings.
- **Verification:** Start one `serve`, then a second with the same config → the second exits non-zero "address in use". Invalid `health.listen` → exits non-zero at startup.

### R-022 — Zone-change detection has no write-quiescence window and stamps `LastSigned` after signing: a mid-write file can be signed truncated, and a content-only edit during signing is never re-signed
- **Severity:** Medium
- **Category:** correctness / data-integrity
- **Location:** `sign.go:374-444` (NeedsSign), `sign.go:146-151` (LastSigned set after sign), `daemon.go:328-339`.
- **Problem:** Polling-based (no fsnotify): `NeedsSign` triggers on `info.ModTime().After(LastSigned)` and `SignZone` immediately parses. A non-atomically-written zone file (shell `>` from a generator, in-place editor save) caught mid-write — if the truncated content still ends on a record boundary — parses cleanly, passes `validateZone` (SOA+NS present), and is signed; the atomic signed-output rename then publishes a zone with records missing, which NSD serves (names vanish with valid NSEC/NSEC3 denial) until the next poll notices, up to `poll_interval`. Second defect: `LastSigned` is set *after* parse+sign, so an edit landing between parse and completion has mtime < LastSigned; if it didn't change the serial, the serial fallback also misses it and it's never signed until the refresh re-sign (up to `signature_validity - signature_refresh` ≈ 11 days later).
- **Fix specification:** (1) Quiescence — in `NeedsSign`, when `mtime > LastSigned` but the file changed within the last few seconds (re-stat after a short delay showing changed mtime/size), defer to the next tick with a debug log; expiry-driven signing must not be deferred. (2) Fidelity — capture the source mtime/size at parse time and store it as the change-detection reference (new `omitempty` state field), keeping `LastSigned` for display/merge. **Must not change:** state.json back-compat (new field omitempty/absent-tolerant), the expiry/rollover precedence in NeedsSign, the atomic signed-zone write.
- **Verification:** Write half a zone file, call NeedsSign/SignZone within the window → deferred; complete + advance past the window → signs. Sign a zone, touch the unsigned file (mtime between parse and completion, serial unchanged) → next NeedsSign returns true.

### R-023 — `import` lacks the transactional guards `add` received: no config-append preflight, no unwind; a config failure diverges state from config with retry blocked
- **Severity:** Medium
- **Category:** error-handling
- **Location:** `main.go:1121-1247` (runImport), contrast `main.go:676-678` (add's preflight) and `unwindAdd`.
- **Problem:** `runAdd` got `preflightConfigAppend` + `unwindAdd` on every post-side-effect failure; `runImport` has neither. Its sequence writes keys, SetZone, SignZone (rollback only removes the in-memory zone — files stay), `state.Save`, then `AddZoneToConfigFile` last. If the config append fails (the root-owned-config case the preflight was added for), state.json already holds the zone → state and config diverge: the daemon won't sign it (not in cfg.Zones) but status shows it managed, and re-running `import` fails at "already managed (in state.json)", requiring manual state surgery. REVIEW_53 R53-008 claimed both add and import were made transactional — incomplete for import.
- **Fix specification:** Mirror add: `preflightConfigAppend` before `saveKeyFiles`; on `AddZoneToConfigFile` failure remove the zone from state and re-Save (plus delete the signed output), leaving the converted key files in place with a printed note (never auto-delete key material). **Must not change:** BIND key validation (flags 257/256), the keys→sign→state→config ordering, `saveKeyFiles` backup behavior, import's read-only registrar stance.
- **Verification:** Read-only config → `import` fails before writing key files. Config writable but AddZoneToConfigFile forced to fail → state.json lacks the zone afterward and a retry proceeds past "already managed".

### R-024 — `dnssec-tudor sign` exits 0 even when zones fail to sign
- **Severity:** Medium
- **Category:** error-handling
- **Location:** `sign.go:34-69` (SignAll returns only `state.Save()`), `main.go:465-494` (runSign).
- **Problem:** `SignAll` logs each zone's failure, records it in the zone's Errors, and continues (correct for resilience) but its only return value is `state.Save()`. `runSign` prints the status JSON and exits 0 even when some or all zones failed (unparseable files, unreadable keys). The one-shot `sign` is exactly what cron/CI run and they detect failure by exit code; siblings (`resign`, `registrar verify`) do return errors. A nightly `sign` reports success while every zone's signatures march toward expiry.
- **Fix specification:** Have `SignAll` count failures (or join errors) and return a non-nil error like `"%d of %d zones failed to sign"` while still saving state and continuing all zones. `runSign` prints the status JSON, then returns the error. **Must not change:** the daemon path (`checkAndSignZoneSafe`), status JSON schema, hook firing on partial success.
- **Verification:** Point one zone at a garbage file, `dnssec-tudor sign; echo $?` → exit 1 with JSON still printed and healthy zones signed; all-healthy still exits 0.

### R-025 — `rollover algorithm` panics (nil deref) on a zone whose key init previously failed
- **Severity:** Medium
- **Category:** correctness (crash)
- **Location:** `rollover.go:293` (`zoneState.KSK.Algorithm`), `rollover.go:304/307` (`zoneState.KSK.ID`/`ZSK.ID`), `main.go:943-951` (no key check), `sign.go:47-49` (placeholder persisted). Merges daemon Medium + rollover-state Low.
- **Problem:** `SignAll`'s error path persists a placeholder `ZoneState{Path, Errors}` with nil KSK/ZSK (see R-033). `runRolloverAlgorithm` validates only `zoneState == nil` and calls `StartAlgorithmRollover`, which dereferences `zoneState.KSK.Algorithm` with no nil check → panic. Siblings guard this (`StartKSKRollover` checks `oldKSK == nil`; `runDS` checks `zoneState.KSK == nil`), so it's an oversight. Trigger: key files made unreadable, `sign` stores the placeholder, operator later attempts an algorithm rollover → Go stack trace instead of an error.
- **Fix specification:** In `StartAlgorithmRollover`, after the in-progress check, add `if zoneState.KSK == nil || zoneState.ZSK == nil { return fmt.Errorf("zone %s has no usable keys …", domain) }`. Optionally also guard in `runRolloverAlgorithm`. **Must not change:** the placeholder-persisting behavior (it surfaces the error in status), the state machine, the existing KSK-rollover guard.
- **Verification:** `SetZone` a `ZoneState{Path}` with nil keys, call `StartAlgorithmRollover` → descriptive error, no panic.

### R-026 — Async post-sign hook goroutines are untracked by the daemon WaitGroup; shutdown right after a cycle can skip the final `nsd-control reload`
- **Severity:** Medium
- **Category:** lifecycle / correctness
- **Location:** `hooks.go:48-100` (executeHook), `hooks.go:109-160` (executeBatchHook), `daemon.go:302-304`/`379-389` (fired then return). Merges daemon + web-ops.
- **Problem:** Hooks run in a bare `go func()` with no linkage to `Daemon.wg` or `d.ctx`. `signAllZones` fires the coalesced hook at cycle end and returns; a SIGTERM around then finds `wg.Wait()` with nothing to wait for, so the process exits and kills the hook subprocess before/mid-run. Signed files were written and state says signed, but `nsd-control reload` never ran; on restart `NeedsSign` sees valid fresh signatures and doesn't re-sign, so the hook isn't re-fired — NSD serves the stale previous zone (old serial, aging RRSIGs) until the next natural trigger, worst case ~11 days.
- **Fix specification:** Track hook goroutines with a dedicated `sync.WaitGroup` that `executeHook`/`executeBatchHook` Add/Done when invoked from the daemon, and have `Run` wait on it (bounded by the existing 30s timeout) after the signing loop exits. Pass a `*sync.WaitGroup` (nil from CLI paths; `executeHookSync` unchanged). **Must not change:** hooks stay async relative to per-zone signing, the 30s timeout, the env-var contract, log redaction.
- **Verification:** `post_sign = sh -c 'sleep 2; touch /tmp/hook-ran'`, coalesce on, short poll; SIGTERM right after a cycle signs → process doesn't exit until the marker exists (or 30s).

### R-027 — Key backup failures are warn-only: `saveKeyFiles` overwrites the live private key even when its backup failed; rollover starts proceed on backup failure
- **Severity:** Medium
- **Category:** data-integrity
- **Location:** `keys.go:156-244` (saveKeyFiles / backupExistingKeyFiles), `rollover.go:46/163/304` (backupKey warn-only).
- **Problem:** The comment at `keys.go:164-166` says the backup "is the only way to recover" an overwritten KSK, yet every backup failure continues to the overwrite. `backupExistingKeyFiles` returns void; on `os.Rename` failure it warns and returns, and `saveKeyFiles` then overwrites both live files — destroying the only copy of the old private key. In `StartKSKRollover`/`startZSKRollover`/`StartAlgorithmRollover`, `backupKey` errors are `slog.Warn`-only before generation overwrites the live files. If both backup attempts fail the old KSK private key is gone while the registrar's DS still references it, and the rollover-signing branch that needs it is itself warn-only (R-003) → SERVFAIL with recovery impossible. Two adjacent gaps: the unparseable-key `.bak` fallback clobbers any previous `.bak`; and the "backup already exists" skip assumes the same-named backup holds the same key.
- **Fix specification:** Make `backupExistingKeyFiles` return error and `saveKeyFiles` abort (leaving the live pair untouched) when a required backup fails. Make `backupKey` failure fatal for KSK/algorithm rollovers (and ZSK too — costs nothing). Use a unique suffix for the `.bak` fallback. Verify the existing backup's `.key` parses to the same `PublicKey` before skipping. **Must not change:** backup filename convention, never-delete-keys policy.
- **Verification:** Make the keys dir read-only (non-root) after generating a key, call `GenerateKSK` again → error and originals untouched/loadable. `StartKSKRollover` returns error (Rollover nil) when `backupKey` fails.

### R-028 — `RecoverKeyState` silently regenerates when exactly one half of the pair exists, breaking the DS chain the hardening was meant to protect
- **Severity:** Medium
- **Category:** data-integrity
- **Location:** `keys.go:283-288` (returns nil,nil when one file missing), `keys.go:360-390` (recoverOrGenerateKeys).
- **Problem:** `recoverOrGenerateKeys` was hardened so that *unreadable* existing key files refuse regeneration (silently minting a new KSK while the parent DS references the old one SERVFAILs the zone). But that only covers "both files exist and can't be parsed." `RecoverKeyState` returns `(nil, nil)` — "caller should generate" — when `.key` exists but `.private` is missing, and vice versa. A missing `.private` beside a live `.key` is exactly the artifact the non-atomic `saveKeyFiles` crash window (R-009) leaves, and a common restore/cleanup accident. In that state the daemon/`add` mints a brand-new KSK — a silent chain break for a zone whose parent DS still matches the on-disk `.key`.
- **Fix specification:** Return `(nil, nil)` only when *both* files are absent; when exactly one exists, return an error naming the orphan so `recoverOrGenerateKeys` refuses to regenerate. A non-IsNotExist stat error is also an error, not a generate. **Must not change:** the `(nil, nil)` contract for genuinely-new domains, the error-wrapping style, `runAdd`'s kskGenerated/zskGenerated detection.
- **Verification:** Generate a pair, delete only `.private` → `RecoverKeyState` returns a non-nil error and `recoverOrGenerateKeys` refuses; repeat deleting only `.key`; the surviving half stays on disk.

### R-029 — No key-tag collision handling at generation: a colliding tag corrupts rollover identity, clobbers backups, and makes Dynadot's upsert-by-key_tag replace the old DS
- **Severity:** Medium
- **Category:** correctness
- **Location:** `keys.go:77` (accepts whatever tag), `rollover.go:255-270` (backupKey overwrites), `registrar_dynadot.go:486-503` (upsert by key_tag), `sign.go:244-260` (tag-as-identity). Merges keys Medium + registrar Low.
- **Problem:** `generateKey` accepts whatever `KeyTag()` the fresh key gets with no check against the zone's other keys, the rollover's old key, or existing backups. Tags are 16-bit non-unique (RFC 4034 App. B); a collision is a when-not-if across many zones and 90-day ZSK rollovers (~1/65536 per generation). When a new key's tag equals the old key's: (a) `RolloverState.OldKeyID == NewKeyID`, so `loadKeyPairByID(...OldKeyID)` resolves to a backup file the new rollover's `backupKey` may have overwritten; (b) for a KSK rollover with registrar automation, `AddDS` PUTs the new DS with the colliding tag and Dynadot's upsert-by-key_tag *replaces* the old DS instead of adding — defeating the additive contract and removing the trusted DS while resolvers hold cached DNSKEYs; (c) `validateConsistency` uses tags as identity, so a signing key can "validate" against a different published key with the same tag. Mainstream signers regenerate on collision.
- **Fix specification:** In `generateKey`, after generation, regenerate (bounded loop, ~10 attempts) while the new tag collides with the zone's current KSK/ZSK tag, an in-progress rollover's OldKeyID/OldZSKID, or an existing backup `<domain>.<type>.<tag>.key`. Make `rollover.backupKey` refuse to overwrite an existing backup with differing content. **Must not change:** `KeyState.ID`==keytag (schema), backup naming, DS formats. Debug-log only.
- **Verification:** Refactor the collision predicate into a pure `tagInUse(...)` and unit-test it against live files/state/rollover; assert `backupKey` no longer truncates a differing backup.

### R-030 — Full Dynadot API key written to logs at debug via the HMAC string-to-sign, contradicting the documented "never logged" contract
- **Severity:** Medium
- **Category:** security
- **Location:** `registrar_dynadot.go:115-128` (`sign`), specifically the `slog.Debug(..., "string_to_sign", stringToSign)` at 121-125 where `stringToSign = c.apiKey + "\n" + …`.
- **Problem:** At debug level the full `api_key` is logged in cleartext (the secret is not — only its length). CLAUDE.md promises "Neither the key nor the secret is ever logged" (line 366) and "never printed in logs" (line 83). Operators enable debug precisely to troubleshoot signature errors, so the key will land in logs during troubleshooting, and logs may be shipped/retained.
- **Fix specification:** Remove `api_key` from the logged string (log the string-to-sign with the key redacted, e.g. `len` only, or omit the field). Keep the signature and lengths for debugging. Do not change the HMAC computation. **Must not change:** the wire signature.
- **Verification:** Run a registrar op at `LOG_LEVEL=debug` and grep the log for the configured `api_key` value → absent.

### R-031 — `registrar push` / auto-publish during rollover can drop the old KSK's DS *when the backed-up old key fails to load* (reframed after verification)
- **Severity:** Medium
- **Category:** correctness / data-integrity
- **Location:** `registrar.go:74-82` (BuildDSSet warn-and-continue), `registrar_cli.go:262-274`.
- **Problem:** `BuildDSSet` correctly includes *both* old and new DS during a KSK/algorithm rollover (verified: `registrar.go:74-81`), so the normal `registrar push`/auto-publish path keeps both. **However**, if `LoadPublicKeyByID` for the old KSK backup fails, it `slog.Warn`s and returns the new KSK's DS *only*; `ReplaceDS(new-only)` then DELETEs the old DS from the parent during `ds_add_wait` → resolvers validating via the old chain go bogus. This is the same warn-and-continue-on-missing-old-key theme as R-003. (The originally-filed "push always deletes old DS during rollover" is *not* correct — verification showed BuildDSSet includes both.)
- **Fix specification:** In `BuildDSSet`, make an old-KSK load failure during a KSK/algorithm rollover a hard error (or, at minimum, refuse to hand a rollover-time DS set to `ReplaceDS` when the old key is missing) so the caller aborts rather than shrinking the set. Tie this to fixing the missing-backup root cause in R-027. **Must not change:** the both-DS-during-rollover behavior, the CompareDSSets drift logic.
- **Verification:** Start a KSK rollover, delete the old KSK backup, run `registrar push` against an httptest Dynadot → the command errors and issues no DELETE, rather than replacing to new-only.

### R-032 — Registrar auto-publish failures are never recorded in the zone's status warnings, contradicting the documented contract
- **Severity:** Medium
- **Category:** error-handling / docs
- **Location:** `registrar_cli.go:228-280` (maybeAutoPublishDS), specifically the `fmt.Fprintf(os.Stderr, "warning: …")`-only paths at 263/272.
- **Problem:** CLAUDE.md (lines 388-391) states registrar failures are "logged with `[REGISTRAR]` prefix and added to the zone's warnings array in the status output." `maybeAutoPublishDS` only prints to stderr; it never calls `AddWarning`, so `dnssec-tudor status` and the dashboard show the zone healthy while its DS push (including the R-004 zero-DS strand) failed. A cron flow relying on status JSON never sees the failure.
- **Fix specification:** On any auto-publish failure, `state.Mutate` + `zoneState.AddWarning("registrar auto-publish failed: …")` + `state.Save` (under the R-007 lock), in addition to stderr. Escalate the R-004 zero-DS sentinel to an Error-level warning. **Must not change:** the "registrar failure never prevents signing" guarantee, the CLI's non-zero exit for explicit `registrar push`/`verify`.
- **Verification:** With an httptest Dynadot that fails, run `add` on a registrar-enabled zone → `status` shows a warning for that zone.

### R-033 — `SignAll` persists a placeholder `ZoneState` with nil KSK/ZSK on key-recovery failure, permanently disabling key generation and ZSK auto-rollover for that zone
- **Severity:** Medium
- **Category:** error-handling
- **Location:** `sign.go:42-58` (placeholder stored + `SetZone` + Save at `sign.go:68`).
- **Problem:** When `SignAll` hits a `recoverOrGenerateKeys` error for a not-yet-in-state zone, it stores `&ZoneState{Path: zoneCfg.Path}` (nil KSK/ZSK), records the error, and continues; `state.Save` persists it. Because the zone is now non-nil in state, the `zoneState == nil` initialization branch never runs again, so `recoverOrGenerateKeys` (the only generate path) is never retried — a transient first-sign failure (e.g. keys dir briefly unwritable) permanently prevents that zone from ever getting keys until state is hand-edited, and it also feeds the R-025 nil-deref panic.
- **Fix specification:** On recovery failure, do **not** persist a keyless placeholder into `Zones`; instead record the failure out-of-band (e.g. a transient errors map surfaced in status) so the next `SignAll`/cycle retries `recoverOrGenerateKeys`. If a status entry is desired, mark it clearly non-managed and still allow the init branch to re-run. **Must not change:** the resilience behavior (other zones keep signing), the status surfacing of the error.
- **Verification:** Make the keys dir unwritable, `sign`, then fix permissions and `sign` again → the zone now gets keys and signs (before the fix it stays keyless).

### R-034 — Input zone containing DNSSEC records (stale RRSIG/NSEC/NSEC3/NSEC3PARAM) is neither stripped nor rejected; re-signing produces a broken zone
- **Severity:** Medium
- **Category:** correctness / data-integrity
- **Location:** `sign.go:446-476` (parseZoneFile keeps every RR), `sign.go:606-670` (signRecordsWithKeys signs every RRset including stray RRSIG).
- **Problem:** `parseZoneFile` appends all parsed records, including any RRSIG/NSEC/NSEC3/NSEC3PARAM present in the input. `SignZone` then generates its own NSEC/NSEC3 chain and appends it (duplicating/conflicting with the input's), and `signRecordsWithKeys` groups by (name,type) and signs *every* RRset — including a stray RRSIG RRset, creating an RRSIG-covering-RRSIG. All stale records are also carried verbatim into the output. Feeding an already-signed file (wrong path in config, or re-processing) silently produces an invalid zone.
- **Fix specification:** In `parseZoneFile` (or just before signing), drop input records of type RRSIG, NSEC, NSEC3, NSEC3PARAM (and optionally DNSKEY at apex, since the signer manages keys) before chain generation and signing — or reject the zone with a clear error if such records are present. **Must not change:** legitimate DS records at delegations, occlusion handling, the generated chain.
- **Verification:** Add a testdata zone containing a stale RRSIG/NSEC → `SignZone` output parses cleanly, has exactly one NSEC/NSEC3 chain, and no RRSIG-over-RRSIG.

### R-035 — Out-of-zone owner names are accepted, signed, and inserted into the NSEC/NSEC3 chain
- **Severity:** Medium
- **Category:** correctness / rfc-compliance
- **Location:** `sign.go:479-513` (validateZone checks only SOA/NS), `sign.go:750-823`/`881-998` (chain builders include every non-occluded name).
- **Problem:** `validateZone` does not check that all records are in-zone. A record whose owner is outside the apex (`evil.com.` in `ptudor.net`'s file, or a typo) is added to the name set, signed, and inserted into the NSEC/NSEC3 chain — breaking canonical ordering and the denial-of-existence proofs (the chain now "covers" names outside the zone). Most operators feed clean files, so this is an operator-error/edge case, but the signer should reject or drop such records rather than emit a subtly broken zone.
- **Fix specification:** In `validateZone` (or before chain generation), verify every authoritative owner name is equal to or below the apex (`dns.IsSubDomain(apex, name)`); reject with a clear error naming the offending owner, or drop out-of-zone records with a warning. **Must not change:** occlusion/delegation handling, glue treatment.
- **Verification:** Testdata zone with an out-of-zone record → `SignZone` errors (or drops it) and the emitted chain contains only in-zone names.

### R-036 — Cron/one-shot operation never advances ZSK rollovers, while `checkRolloverWarnings` promises "will auto-rollover"
- **Severity:** Medium
- **Category:** correctness
- **Location:** `sign.go:1127-1142` (checkRolloverWarnings warning), `daemon.go:284-293` (only the daemon runs `CheckZSKRollover`).
- **Problem:** `CheckZSKRollover` (the automatic ZSK rollover engine) is invoked only from the daemon's `signAllZones`. `dnssec-tudor sign` (one-shot / cron) never calls it, yet `checkRolloverWarnings` emits "ZSK expires in N days (will auto-rollover)" on that same path. An operator running only cron `sign` (a legitimate deployment) gets a promise the tool never keeps — the ZSK is never rolled and eventually the warning becomes "expired."
- **Fix specification:** Either run `CheckZSKRollover` for each zone in the one-shot `SignAll` path (matching the daemon), or make the warning conditional/accurate for non-daemon operation (e.g. "run `dnssec-tudor serve` for automatic rollover, or roll manually"). **Must not change:** the daemon's rollover cadence.
- **Verification:** Run cron `sign` past a ZSK's prepublish window → either the rollover advances (if engine wired in) or the warning text no longer claims automatic rollover.

### R-037 — `CompleteKSKRollover`/`CompleteAlgorithmRollover` retire the old KSK without verifying the new DS exists at the parent
- **Severity:** Medium
- **Category:** correctness
- **Location:** `rollover.go:77-114` (CompleteKSKRollover), `rollover.go:347-380` (CompleteAlgorithmRollover), `main.go:866-921` (runRolloverComplete).
- **Problem:** `Complete*Rollover` only checks the rollover is in `ds_add_wait`, then clears the rollover so the next sign drops the old KSK. Nothing verifies the new DS is actually live at the parent. If the operator runs `rollover complete` before the new DS is published/propagated (easy to do — the tool tells them to run it "once the new DS is published"), the old KSK is dropped while the parent still points only at it → bogus. The tool has the machinery to check (registrar `GetDS`, or a live DS query).
- **Fix specification:** Before retiring the old KSK in `runRolloverComplete`, query the parent (or the configured registrar via `GetDS`) and require the new KSK's DS to be present; if absent, refuse with a clear message and an explicit `--force` escape hatch. **Must not change:** the manual-DS workflow for zones without registrar automation (offer a warning + confirmation there), the state-machine strings.
- **Verification:** With the parent still holding only the old DS, `rollover complete` refuses (without `--force`); with the new DS present, it proceeds.

### R-038 — Rollover start rotates key files on disk before persisting state, with no rollback on Save failure or crash
- **Severity:** Medium
- **Category:** data-integrity
- **Location:** `rollover.go:42-73` (StartKSKRollover: backup → GenerateKSK → Mutate → Save).
- **Problem:** `StartKSKRollover` backs up the old key, generates the new KSK (which renames the old `.ksk.key` to the backup and writes the new one), *then* mutates state and `Save`s. If `Save` fails (disk full) or the process crashes between generation and Save, the key files are already rotated (the live `.ksk.key` is the new key) but state.json still says old KSK / no rollover → the next sign loads the new KSK, publishes only it, and the parent DS still references the old → bogus. Same ordering risk in `StartAlgorithmRollover`. Compounds with the R-007 clobber.
- **Fix specification:** Persist the rollover intent (state) *before* rotating key files, or make the whole start atomic under the R-007 lock with rollback: if `Save` fails, restore the old key files from backup and clear the in-memory rollover. **Must not change:** the both-keys-during-rollover signing, backup naming.
- **Verification:** Inject a `Save` failure in `StartKSKRollover` → the live `.ksk.key` is restored to the old key and state has no rollover (no divergence).

### R-039 — Automatic ZSK rollover mints the new ZSK with the current config algorithm, not the zone's existing key algorithm
- **Severity:** Medium
- **Category:** correctness / rfc-compliance
- **Location:** `rollover.go:167-169` (`keyGen.GenerateZSK(domain)` → `GetZoneAlgorithm`), `rollover.go:218-226` (state records the actual algorithm).
- **Problem:** `startZSKRollover` generates the new ZSK via `GenerateZSK` → `GetZoneAlgorithm`, which returns the zone override or the *current global* `[dnssec].algorithm`. If the operator changed the global algorithm after the zone was signed, the auto ZSK rollover mints a new ZSK in the *new* algorithm while the KSK stays the old algorithm — an unintended algorithm mismatch without a proper algorithm rollover (RFC 6840 §5.11: every algorithm signaled in DNSKEY/DS must sign all RRsets). The code even acknowledges the algorithm "may have changed" at `rollover.go:222`.
- **Fix specification:** Generate the new ZSK with the *existing* ZSK's algorithm (read from the current key/state), not the config default; algorithm changes must go through the dedicated `rollover algorithm` flow. **Must not change:** the algorithm-rollover path, config key names.
- **Verification:** Sign a zone with ED25519, change `[dnssec].algorithm` to ECDSAP256SHA256, trigger a ZSK rollover → the new ZSK is still ED25519 (matching the KSK).

### R-040 — `State.Save` renames without fsync: a crash/power loss can leave an empty or truncated state.json that then hard-fails startup
- **Severity:** Medium
- **Category:** data-integrity
- **Location:** `state.go:188-212` (`writeFileOwned` = `os.WriteFile`, no Sync; rename with no dir fsync).
- **Problem:** `Save` writes the temp file via `os.WriteFile` (no fsync) and renames (no directory fsync). After a crash/power loss the rename may be durable while the data is not, leaving a zero-length or truncated state.json. On restart `LoadState` → `json.Unmarshal` errors, and the daemon returns that error and refuses to start (crash loop) — while the signing engine can't run and every zone's signatures march toward expiry. Rollover state in particular is unrecoverable.
- **Fix specification:** In `Save`, `f.Sync()` the temp file before rename and fsync the containing directory after rename (as `writeSignedZone` does for the file). Consider tolerating a corrupt state.json at load by falling back to an empty state + `recoverOrGenerateKeys` (with a loud warning) rather than refusing to start. **Must not change:** the atomic-rename + ownership behavior, state.json schema.
- **Verification:** Simulate a truncated state.json → the daemon logs and recovers (or, minimally, the write path fsyncs — assert via an injected sync counter/test seam).

### R-041 — A pending KSK/algorithm rollover silently blocks automatic ZSK rollover indefinitely
- **Severity:** Medium
- **Category:** correctness
- **Location:** `rollover.go:137-157` (CheckZSKRollover only handles `Type=="zsk"`; `startZSKRollover` returns nil if any `Rollover != nil`).
- **Problem:** `CheckZSKRollover` only advances an existing rollover if its `Type=="zsk"`, and `startZSKRollover` returns early when any rollover is in progress. So a KSK rollover stuck in `ds_add_wait` (operator hasn't updated the parent DS) blocks the ZSK from rolling — the ZSK ages past its intended lifetime with no signal beyond the generic warnings. A long-pending KSK rollover (common — DS updates are manual) silently stalls ZSK hygiene.
- **Fix specification:** Allow a ZSK rollover to proceed concurrently with a KSK/algorithm rollover (they are independent key roles), or at minimum surface an explicit warning/status that the ZSK rollover is blocked by the pending KSK rollover. **Must not change:** the KSK/algorithm rollover flow, the single-`Rollover`-field schema may need a second slot (design decision) or a documented limitation.
- **Verification:** Start a KSK rollover, leave it in `ds_add_wait`, advance time past the ZSK prepublish window → either the ZSK rolls (concurrent) or status clearly reports the ZSK rollover is blocked.

### R-042 — Per-domain Prometheus series are never deleted: removed zones keep stale expiry gauges (guaranteed future false alerts)
- **Severity:** Medium
- **Category:** correctness / observability
- **Location:** `metrics.go:115-168` (`UpdateZoneMetrics` sets per-domain gauges, never deletes).
- **Problem:** `UpdateZoneMetrics` sets per-domain gauges (e.g. signature-expiry timestamp) but never calls `DeleteLabelValues` for zones no longer in state. A removed or renamed zone keeps its last gauge value forever; an expiry gauge frozen in the past becomes a permanent false "signatures expired" alert, and the metric cardinality only grows.
- **Fix specification:** Track the set of domains currently exported and `Delete`/`DeleteLabelValues` for any domain absent from the new state on each `UpdateZoneMetrics` (or use a `prometheus.NewGaugeVec` reset pattern). **Must not change:** metric names/labels other tooling scrapes.
- **Verification:** Add a zone, scrape metrics, remove it, scrape again → the per-domain series is gone.

### R-043 — Dashboard `?validate=true` and `/api/validate` run sequential live-DNS validation of every zone inside the request (DoS / amplification)
- **Severity:** Medium
- **Category:** performance / security
- **Location:** `web.go:100-106`/`184-193` (ValidateAll in-handler), `web.go:69-76` (30s WriteTimeout), `validate.go:98-128` (sequential per-zone live queries).
- **Problem:** `?validate=true` and `/api/validate` call `ValidateAll`, which loops every zone doing multiple live DNS queries each (NS resolution + DS/DNSKEY/SOA with a 5s timeout apiece), sequentially, inside the HTTP handler. For more than a few zones — or slow/timing-out nameservers — this blows past the 30s WriteTimeout and ties up the handler, and every request triggers a burst of outbound DNS. The web surface has no auth (loopback by default, but R-005 exposes it), so a client can trigger repeated many-second, many-query validation runs.
- **Fix specification:** Bound and protect the validate path: run it off the request (cache the last result with a short TTL, or a background refresh), cap total time to well under WriteTimeout, parallelize with a concurrency cap, and require it be explicitly enabled. At minimum, add a per-endpoint rate limit and a hard overall deadline. **Must not change:** the JSON schema, the `{secure,…}` semantics.
- **Verification:** With 50 configured zones and a blackholed NS, `GET /api/validate` returns within a bounded time and does not hold the handler for minutes.

### R-044 — Heartbeat sends are synchronous inside the signing loop: an unreachable endpoint adds ~20-30s per cycle
- **Severity:** Medium
- **Category:** performance / lifecycle
- **Location:** `heartbeat.go:58-103` (synchronous `Send`), `daemon.go:342/367/375` (called inline per zone).
- **Problem:** `checkAndSignZone` calls `SigningStart` and `SigningComplete`/`SigningError` synchronously; each `Send` blocks up to the 10s HTTP timeout. With a blackholed heartbeat endpoint, every signed zone adds up to ~20s to the cycle (and to shutdown, per R-019). At scale this serializes signing behind a dead monitoring endpoint.
- **Fix specification:** Make per-event heartbeat sends fire-and-forget (async, bounded queue, drop-oldest on backpressure) so signing never blocks on the monitoring endpoint; keep the periodic background heartbeat as-is. **Must not change:** the heartbeat wire actions/format, the "starting/stopping/idle" lifecycle.
- **Verification:** Blackhole the heartbeat URL and sign 20 zones → cycle time is unaffected by the dead endpoint.

## Low

The following are maintainability, minor-correctness, doc, and test-gap items. Each is real but low-impact; fix specs are terse.

- **R-045 (Medium→listed here for grouping; treat as Medium) — validate.go DS match hardcodes SHA-256:** `validate.go:237` computes `expectedDS = localKSK.ToDS(dns.SHA256)` and the comparison loop (`:258-266`) only compares against it, so a zone whose parent holds a `digest_type=4` (SHA-384) DS reports a permanent false "fail." *Fix:* compute both SHA-256 and SHA-384 expected DS and match if either matches the parent's (respecting the configured `digest_type`). *Verify:* mock a SHA-384 DS at parent → status pass.
- **R-046 (Medium) — DS check false "fail" during normal rollover:** `checkDSAtParent` (`validate.go:205-285`) compares parent DS only against the current KSK; during KSK/algorithm `ds_add_wait` the parent legitimately holds old+new, and if only the old is still present the check says "fail" though the zone is fine. *Fix:* during a rollover, accept a match against either the old or new KSK's DS (mirror `BuildDSSet`). *Verify:* simulate ds_add_wait with only the old DS → not a hard fail.
- **R-047 (Medium) — fail-open DS check when local KSK unloadable:** `validate.go:274-282` — if `localKSK` is nil (load failed) but *some* DS exists at the parent, status is "pass" ("no local KSK to compare"). A stale/foreign DS then reads as healthy. *Fix:* when the local KSK can't be loaded, report "error"/"indeterminate", not "pass." *Verify:* make the KSK unreadable → status is not "pass."
- **R-048 (Medium) — computeOverall reports "partial" for a bogus zone:** `computeOverall` (`validate.go:565-598`) returns "partial" when DS is present but DNSKEY/RRSIG fail — a SERVFAIL condition that should read as "fail." *Fix:* treat DS-present-with-DNSKEY/RRSIG-fail as overall "fail" (bogus). *Verify:* mock present DS + failing DNSKEY → overall "fail."
- **R-049 (Low) — validate all-error override can't distinguish auth-NS outage from local resolver failure:** `validate.go:192-199` marks the zone "fail" when all sub-checks error, but a down *local* resolver produces the same all-error state, mislabeling a validator-side problem as a zone problem. *Fix:* separate resolver-unreachable from authoritative-unreachable (probe the resolver first). *Verify:* point at a dead resolver → status distinguishes the two.
- **R-050 (Low) — queryDirect never retries over TCP on truncation:** `validate.go:543-562` sends UDP with DO and never retries on TC=1, so large DNSKEY answers silently lose records and the checks misreport. *Fix:* on `r.Truncated`, retry with `c.Net = "tcp"`. *Verify:* a zone with a large DNSKEY set validates correctly.
- **R-051 (Low) — resolveNS glue matching is case-sensitive:** `validate.go:485-496` compares additional-section names with `dns.Fqdn(...)` equality, missing the fast path when the server preserves original case. *Fix:* compare with `strings.EqualFold`/`dns.CanonicalName`. *Verify:* mixed-case glue still matched.
- **R-052 (Low→Medium security) — documented 0640 config permission is never enforced:** `config.go:277 LoadConfig` reads the secrets-bearing config with no mode check; CLAUDE.md requires "mode 0640 or stricter." A world-readable config with Dynadot key/secret and heartbeat key loads silently. *Fix:* stat the config and warn (or refuse) when it is group/other-readable beyond 0640, at least when registrar/heartbeat secrets are set. *Verify:* chmod 0644 a config with secrets → warning/refusal.
- **R-053 (Low) — Duration.UnmarshalText accepts trailing garbage:** `config.go:167` uses `fmt.Sscanf("%f")`, so `"1x2d"` parses as `1` days silently. *Fix:* parse the full numeric prefix strictly and error on leftover characters. *Verify:* `"1x2d"` → parse error.
- **R-054 (Medium) — CLAUDE.md's unquoted `[zones.ptudor.net]` examples register zero zones:** every zone example in the signer CLAUDE.md config (`CLAUDE.md:173-183`) uses the unquoted dotted form, which go-toml/v2 parses as nested tables and silently drops (empirically confirmed: `err=nil`, zero usable zones), while testdata and `add` use the working quoted `[zones."ptudor.net"]` form. An operator copying the documented config gets a daemon that signs nothing with no error. *Fix:* change all CLAUDE.md zone examples to the quoted form; adopting R-015 (strict decoding) makes the unquoted form a load-time error. *Verify:* paste the corrected example → zones load.
- **R-055 (Low) — doc drift: BurntSushi vs go-toml:** both `~/Git/CLAUDE.md`/`daemons/dnssec/CLAUDE.md` and the signer CLAUDE.md claim this project uses `BurntSushi/toml` ("the only project on BurntSushi"), but `go.mod`/`config.go:12` use `pelletier/go-toml/v2 v2.1.1`. This misdirects any fix for R-015 (the two libraries have different strict-mode APIs). *Fix:* correct all three docs. *Verify:* grep the docs for BurntSushi → none, or matches reality.
- **R-056 (Low) — log file never reopened on SIGHUP:** `setupLogging` (`main.go:241-297`) runs once; SIGHUP does not re-run it, so the "close previous log file on reload" branch is dead and log rotation (logrotate) leaves the daemon writing to the unlinked fd. *Fix:* re-run/reopen logging on SIGHUP (or document copytruncate). *Verify:* rotate the log, SIGHUP, confirm writes go to the new file.
- **R-057 (Low) — tickerReset drops the newest interval on double-SIGHUP:** `daemon.go:130-137` does a non-blocking send to a size-1 channel; two SIGHUPs within one loop iteration keep the *first* new interval and drop the second. *Fix:* replace the value in the channel (drain-then-send) so the latest wins. *Verify:* two rapid poll-interval changes → the last one takes effect.
- **R-058 (Low) — `serve --web` override lost on SIGHUP:** `main.go:401-406` sets Web from the flag, but the SIGHUP reload rebuilds config from file only, flipping `Web.Enabled`/`Listen` back to file values (default disabled). The web server keeps running (Reload warns "restart required"), so state is inconsistent. *Fix:* re-apply the `--web` override in the reload path, or document that `--web` requires the config `[web]` block. *Verify:* start with `--web`, SIGHUP, confirm the intended web config persists.
- **R-059 (Low) — runtime command failures print full cobra usage:** `main.go:33-46` leaves `SilenceUsage` unset, so an operational error (e.g. "zone not managed") dumps the whole usage text, burying the real error. *Fix:* set `SilenceUsage: true` (and `SilenceErrors` with manual error printing) on the root command. *Verify:* a failing command prints only the error.
- **R-060 (Low) — runAdd returns exit 1 after a fully successful add if the post-success LoadPublicKey fails:** `main.go:732-736` — the zone is already added/signed/config-written, but a DS-display load failure returns an error (exit 1), signaling failure for a completed operation. *Fix:* downgrade the DS-print failure to a warning after the add has committed. *Verify:* force LoadPublicKey to fail after commit → exit 0 with a warning.
- **R-061 (Low) — loaded key files not validated against domain/role/algorithm:** `parseDNSKEYFromFile`/`loadKeyPairFromPath` (`keys.go:471`, `keys.go:441`) accept whatever DNSKEY is in the file; a wrong file (mismatched owner, ZSK flags in a KSK slot, unexpected algorithm) is used silently. *Fix:* verify owner==domain, flags match the requested role (257/256), and the algorithm is supported, erroring otherwise. *Verify:* load a ZSK file as a KSK → error.
- **R-062 (Low) — AlgorithmFromName is dead and advertises unsupported RSA algorithms:** `keys.go:527` maps RSASHA256/RSASHA512 which `generateDNSSECKey`/`signRRSIG` can't produce/sign, and the function has no production caller. *Fix:* remove it or restrict it to the three supported algorithms. *Verify:* grep for callers; keep the supported-set consistent.
- **R-063 (Low) — sortZoneRecords compares lowercased names against a non-lowercased apex:** `sign.go:1078-1089` lowercases record names but compares to `apex := dns.Fqdn(domain)` (not lowercased), so a mixed-case zone key in config breaks the apex-first ordering. *Fix:* lowercase the apex for comparison. *Verify:* a `Example.COM` zone key still sorts apex-first.
- **R-064 (Low) — NeedsSign fully parses every zone file every poll cycle even when nothing changed:** `sign.go:432-441` parses for the serial check on every poll even when mtime and signatures are unchanged. At many zones this is wasteful I/O/CPU. *Fix:* skip the parse when mtime ≤ LastSigned and signatures aren't near expiry (mtime already gates this — reorder to avoid the parse). *Verify:* profile a large zone set across idle cycles.
- **R-065 (Low) — `$INCLUDE` directives are rejected and the limitation is undocumented:** `sign.go:456` uses `dns.NewZoneParser` without a `SetIncludeAllowed`, so zones using `$INCLUDE` fail to sign. *Fix:* either enable includes with a base dir, or document the limitation explicitly. *Verify:* a zone with `$INCLUDE` either signs or fails with a clear message.
- **R-066 (Low) — canonicalLess compares presentation-format labels:** `sign.go:849-879` splits/compares presentation strings, mis-ordering names with escaped octets relative to RFC 4034 §6.1 wire-format canonical order. Rare in practice. *Fix:* compare on wire-format (unescaped) label bytes. *Verify:* a zone with an escaped-octet owner name orders correctly.
- **R-067 (Low, security-adjacent) — full `*Config` (incl. Dynadot api_key/api_secret, heartbeat api_key) passed into the dashboard template:** `web.go:108-118` hands the whole `*Config` to the template. html/template escaping prevents injection, but any template edit that renders `.Config` fields risks leaking secrets to the dashboard (which R-005 can expose). *Fix:* pass a redacted view struct with only the fields the template needs. *Verify:* the template data contains no secret fields.
- **R-068 (Low) — dashboard uses absolute links `/` and `/?validate=true`:** `templates/index.html:128-130` — these escape a reverse-proxy path prefix (e.g. `/dnssec-status/` in the shipped Apache config), breaking the UI behind a subpath proxy. *Fix:* use relative links or a configurable base path. *Verify:* the dashboard works behind a subpath proxy.
- **R-069 (Low) — UpdateZoneMetrics has no "warning" bucket:** `metrics.go:123-131` — warning-status zones vanish from the summary gauges (total ≠ healthy+action_required+errors). *Fix:* add a warning gauge. *Verify:* a warning zone is counted.
- **R-070 (Low) — dead code and stale comment in ownership.go:** `cliRootAdvisory` (`ownership.go:105`) has no caller, and `InitOwnershipTarget`'s comment claims a parent-directory fallback that isn't implemented (`ownership.go:38-43` just returns). *Fix:* remove the dead function; fix or implement the comment's claim. *Verify:* grep for callers.
- **R-071 (Low) — health.checkDirWritable only tests the owner-write bit:** `health.go:179-193` checks the mode bit rather than actually attempting a write, falsely reporting 503 for a group-writable dir the daemon can write. *Fix:* attempt a temp-file create/remove (as `daemon.checkDirWritable` does) instead of inspecting mode bits. *Verify:* a group-writable, daemon-writable dir reports healthy.
- **R-072 (Low) — scripts/dynadot-probe.sh prints the API key and uses bash-4-only lowercasing:** `scripts/dynadot-probe.sh:151-153` echoes the key to stdout and uses `${var,,}` which fails on macOS's default bash 3.2. *Fix:* redact the key in output; use `tr` for lowercasing. *Verify:* run on bash 3.2 with no key leakage.
- **R-073 (Low) — rate limiter minted per RegistrarFor call and ignores context:** `registrar.go:36-44`/`registrar_ratelimit.go` — each `RegistrarFor` builds a fresh `DynadotClient` with a fresh limiter, so the sliding window never spans operations, and `gate()` sleeps without observing ctx cancellation (contributes to R-004's budget exhaustion). *Fix:* share one client/limiter per process (or persist limiter state) and make `gate()` honor `ctx.Done()`. *Verify:* burst several ops → the limiter throttles across them and cancels cleanly.
- **R-074 (Low) — dead rollover state constants imply an unimplemented `ds_remove_wait` phase:** `state.go:100-120` defines `KSKRolloverStateDSRemoveWait`/`AlgoRolloverStateDSRemoveWait` that no code transitions into, and the pre-publish Action text hardcodes "7 days" (see R-014). *Fix:* either implement the ds_remove_wait phase or remove the constants and align the docs. *Verify:* grep shows no orphan states.
- **R-075 (Low) — ValidateZoneFile duplicates Signer.validateZone rule-for-rule:** `sign.go:517-561` vs `sign.go:479-513` — two copies of the SOA/NS validation risk drifting; `add`/`import` use one and signing the other. *Fix:* have one call the other (or share a helper). *Verify:* a single source of truth for the rules.
- **R-076 (Low, test-gap) — validate.go check decision logic untested:** `validate_test.go` exercises only mock plumbing, not the pass/fail/partial decisions of the four check functions (the site of R-012/R-045..R-048). *Fix:* add table tests driving each check against mock responses (expired RRSIG, SHA-384 DS, missing KSK, present-DS/failing-DNSKEY). *Verify:* the new cases fail before R-012/R-045..R-048 fixes and pass after.
- **R-077 (Low, test-gap) — no coverage for ZSK rollover phase transitions or state round-trip:** `dnssec_test.go` doesn't test pre_publish→signing→complete timing (R-011/R-014) or state.json schema round-trip. *Fix:* add rollover state-machine tests with injected clocks and an old-schema round-trip. *Verify:* the tests exercise every transition and gate.
- **R-078 (Low, test-gap) — no reload test:** `hardening_test.go` has no `Reload`/SIGHUP test, which is why R-006 shipped. *Fix:* add a reload test asserting the web/health handlers reflect post-reload state. *Verify:* the test fails before R-006's fix.

---

# Part 2 — `validator`

The three headline findings (R-079, R-080, R-081) are soundness breaks in the chain-of-trust enforcement: the tool computes the right cryptographic checks but does not *require* them to pass for a `secure` verdict. Because the validator bootstraps all NS records, glue, and zone-cut structure from an untrusted recursive resolver (default in config: `127.0.0.1`; library default `8.8.8.8`) and only the authoritative-side crypto is meant to provide integrity, these enforcement gaps make the tool spoofable by a network attacker.

## Critical

### R-079 — DS RRSIG verification failure is only a warning: the parent→child chain link is not cryptographically enforced, so a forged DS/DNSKEY/answer is reported `secure`
- **Severity:** Critical
- **Category:** security / correctness
- **Location:** `internal/validator/validator.go:750-790` (validateZone), specifically `:758-763` (DS RRSIG result → warning only) and `:808` (`result.Status = StatusSecure` regardless); `internal/validator/validator.go:815-889` (queryDSFromParentWithValidation sets `RRSIGVerified` but it is never consumed).
- **Problem:** For every non-root zone, the DS RRset at the parent must be signed by the parent's keys (the inductive step of DNSSEC — RFC 4035 §5.2). `queryDSFromParentWithValidation` *does* verify the DS RRSIG against the parent's DNSKEY and records the result in `dsValidation.RRSIGVerified`/`dsValidation.Error`, but `validateZone` only turns a DS-RRSIG error into a **warning** (`result.Warnings = append(..., "DS RRSIG: %s", dsValidation.Error)`) and then unconditionally sets `StatusSecure` after `ValidateChainLink` (which only checks the DS *digest* matches a served DNSKEY). Grep confirms `RRSIGVerified` is never read to influence status, and no `StatusBogus` path derives from the DS RRSIG. So an on-path attacker (or a malicious authoritative server for the parent, or a compromised recursive resolver supplying attacker NS addresses per R-084) can present a forged DS pointing at an attacker-generated DNSKEY, a self-signed DNSKEY RRset, and a signed forged answer — all mutually consistent — and the tool reports `secure` even though the DS is not signed by the parent. This defeats the entire purpose of the validator (its own code comments call this "FULL cryptographic verification").
- **Evidence:** `validator.go:760-762`:
  ```go
  if dsValidation.Error != "" {
      result.Warnings = append(result.Warnings, fmt.Sprintf("DS RRSIG: %s", dsValidation.Error))
  }
  ```
  followed by `validator.go:774` `ValidateChainLink(...)` (digest-only) and `validator.go:808` `result.Status = StatusSecure`. `RRSIGVerified` is set at `validator.go:860` and read nowhere.
- **Fix specification:** In `validateZone`, when the zone has a DS (secure delegation) the DS RRset RRSIG MUST verify against the parent's (already-authenticated) DNSKEY for the zone to be `secure`. Concretely: require `dsValidation != nil && dsValidation.RRSIGVerified == true`; if the DS RRSIG is missing or fails to verify, set `result.Status = StatusBogus` with the DS-RRSIG error, not a warning. Edge cases: (a) the parent zone must itself be `secure` (its DNSKEY authenticated) before its DS signature can be trusted — thread the parent's *verified* DNSKEY, and if the parent is insecure the child is under an insecure delegation (handled by the no-DS path); (b) a DS answer with no RRSIG at all is bogus, not secure; (c) keep the existing digest-match (`ValidateChainLink`) and DNSKEY-self-signature checks. **Must not change:** the `secure`/`insecure`/`bogus`/`indeterminate` vocabulary, the JSON result schema (`DSValidation` already carries the fields), the root-anchor path.
- **Verification:** Add a test that serves a valid DNSKEY/DS digest match but a DS RRSIG that fails to verify (wrong signature bytes, or signed by a key not in the parent) → assert the zone result is `bogus`, not `secure` with a warning. A live test against `dnssec-failed.org` must report `bogus`; a healthy domain `secure`.

### R-080 — The DNSKEY RRset is not required to be signed by the DS-authenticated key: a rogue self-signed KSK added to the RRset validates as secure
- **Severity:** Critical
- **Category:** security / correctness
- **Location:** `internal/validator/dnssec.go:234-286` (VerifyDNSKEYRRSIG picks the signing key by the RRSIG's own keytag), `internal/validator/validator.go:721` (called with all DNSKEYs) vs `validator.go:774`/`dnssec.go:170-230` (ValidateChainLink independently finds the DS-matched key). No linkage between the two.
- **Problem:** RFC 4035 §5.2 requires authenticating the DNSKEY RRset *using the DS* — i.e. the DNSKEY RRset must be signed by (a key authenticated by) the parent's DS. `VerifyDNSKEYRRSIG` finds the signing key via `FindKSKByKeyTag(rrsigRecord.KeyTag, dnskeys)` (falling back to any key by tag) — the key named by the RRSIG itself, which the attacker controls — and never checks that this key is the one `ValidateChainLink` matched to a DS. An attacker serving the zone's DNSKEY RRset can include the genuine KSK-A (whose DS is at the parent, public information) alongside a rogue KSK-B and rogue ZSK-C, sign the DNSKEY RRset with KSK-B and the answer with ZSK-C. Then `ValidateChainLink` matches the real DS to KSK-A ✓, `VerifyDNSKEYRRSIG` verifies the RRSIG by KSK-B ✓, the leaf answer verifies by ZSK-C ✓ — `secure`, even though the private key of the DS-authenticated KSK-A was never used. This is a full chain break independent of R-079 (it does not even need the DS RRSIG to be unverified).
- **Evidence:** `dnssec.go:250-256` selects the key by `rrsigRecord.KeyTag`; the DS-matched `firstValidKSK` from `ValidateChainLink` (`dnssec.go:204-208`) is never passed to or cross-checked in `VerifyDNSKEYRRSIG`.
- **Fix specification:** Bind the two steps: the DNSKEY RRset must be verified by a KSK that is authenticated by a verified DS at the parent. Concretely — after `ValidateChainLink` yields the set of DS-matched DNSKEYs, require that the RRSIG over the DNSKEY RRset is made by one of *those* keys (match on keytag *and* that the key is a DS-matched KSK), and verify it. For the root, the equivalent binding already exists via `VerifyRootTrustAnchor` (digest match) — additionally require the DNSKEY RRSIG be by an anchor-matched key. Reorder so DS authentication happens before/with DNSKEY-RRSIG verification and they share the key identity. **Must not change:** the digest-based DS match, RFC 6840 §5.11 all-algorithms logic, the result schema.
- **Verification:** Test serving a DNSKEY RRset containing the genuine (DS-matched) KSK plus a rogue KSK, with the DNSKEY RRSIG made by the rogue KSK → assert `bogus`. The genuine case (RRSIG by the DS-matched KSK) stays `secure`.

## High

### R-081 — Insecure delegation is declared on an empty DS answer with no authenticated NSEC/NSEC3 proof of DS absence (downgrade attack)
- **Severity:** High
- **Category:** security / correctness
- **Location:** `internal/validator/validator.go:699-712` (no-DNSKEY → query DS → insecure), `internal/validator/validator.go:765-768` (`len(dsRecords)==0 → StatusInsecure`).
- **Problem:** When the parent returns no DS records, the validator declares the delegation `insecure` and stops validating — without requiring an authenticated denial (NSEC/NSEC3 from the parent proving the DS RRset genuinely does not exist, RFC 4035 §5.2/§4.3, RFC 5155). An on-path attacker (or malicious parent server) can simply strip the DS RRset from the response, and the tool downgrades a genuinely-secure zone to `insecure`, then treats the (attacker-supplied) child data as unvalidated. For a diagnostic tool this both misreports a secure zone and removes the protection the user is relying on.
- **Evidence:** `validator.go:704-707` returns `StatusInsecure` on `err != nil || len(dsResult) == 0` with no NSEC handling; `validator.go:765-768` likewise. `queryDSFromParentWithValidation` returns `nil` DS on an empty answer with no proof-of-absence check.
- **Fix specification:** Before declaring `insecure`, require an authenticated proof that the DS RRset is absent: an NSEC/NSEC3 record from the parent covering/matching the child name that does *not* have the DS bit set (and, for NSEC3, honoring opt-out only where RFC 5155 §6 allows). Verify that NSEC/NSEC3's RRSIG against the parent's DNSKEY (reuse `VerifyDenialRRSIGFromResponse`). If no such authenticated proof exists, the result is `indeterminate`/`bogus`, not `insecure`. **Must not change:** genuine opt-out insecure delegations must still resolve to `insecure` when the proof is present; the status vocabulary.
- **Verification:** Test — parent answer with zero DS and no NSEC → result is not `insecure` (indeterminate/bogus); parent answer with a valid signed NSEC/NSEC3 proving no DS → `insecure`. Live: a signed zone with DS stripped by a mock parent must not read `insecure`.

### R-082 — The leaf answer's RRSIG never affects the overall verdict: a secure chain with a bogus/forged answer reports `secure`
- **Severity:** High
- **Category:** correctness
- **Location:** `internal/validator/validator.go:256-276` (verifyActualRecord result stored on `leafZone.RecordValidation`), `internal/validator/validator.go:303` (`result.Result = lastStatus`, computed only from zone statuses).
- **Problem:** `verifyActualRecord` cryptographically verifies the queried record's RRSIG and records `RRSIGVerified`/`Error` on `RecordValidation`, but the top-level `result.Result` is set from `lastStatus`, which only ever changes on *zone* Bogus/Insecure/Indeterminate. The actual answer's signature outcome is never folded in. So a zone whose chain is secure but whose A/AAAA/CNAME answer has an expired, missing, or forged RRSIG (or a `RecordValidation.Error`) is still reported `secure` overall. The record error is surfaced only in the per-zone detail, not the verdict a caller/`/api/validate` consumer reads.
- **Evidence:** `verifyActualRecord` sets `validation.Error`/`validation.RRSIGVerified` (validator.go:507/512/534/539); grep confirms neither is read to change `result.Result`. `validator.go:303` `result.Result = lastStatus`.
- **Fix specification:** Fold the leaf `RecordValidation` into the verdict: if the chain is secure but the requested record's RRSIG is missing/expired/fails verification (and it is not a validated denial), downgrade `result.Result` to `bogus` (for a signature failure) or `indeterminate` (for an unqueryable answer), with the record error in `result.Errors`. Keep validated NXDOMAIN/NODATA/denial as their appropriate status. **Must not change:** the per-zone `RecordValidation` detail, the denial-proof handling, the status vocabulary.
- **Verification:** Test a secure chain serving an A answer with a forged/expired RRSIG → overall `bogus`; a correctly-signed answer → `secure`.

## Medium

### R-083 — Only type A is ever validated; the documented `type` parameter is ignored
- **Severity:** Medium
- **Category:** correctness / docs
- **Location:** `internal/validator/validator.go:90` (`QueryType: "A"` hardcoded), `validator.go:426` (`QueryRecordAuthoritative(..., dns.TypeA)`), `validator.go:350` (CNAME check queries A), `handlers.go` (never reads `type`).
- **Problem:** The API documents a `type` parameter (A, AAAA, MX, …), but the handler never reads it and the validator hardcodes `dns.TypeA` everywhere the leaf record is fetched/verified. A user validating `MX` or `AAAA` actually gets the A record validated; the reported `RecordValidation` is for the wrong type, and NODATA/denial reasoning uses `dns.TypeA`.
- **Fix specification:** Plumb the `type` query parameter through the handler → `Validate` → `verifyActualRecord`/`checkAndFollowCNAME` (default A), set `result.QueryType` accordingly, and use it in the denial `qtype`. Validate the type string against a supported set. **Must not change:** default behavior when `type` is omitted (A), the result schema field names.
- **Verification:** `GET /api/validate?domain=…&type=MX` validates the MX RRset (its RRSIG/denial), and `result.type` reflects MX.

### R-084 — NS records, glue addresses, and zone-cut structure come from an untrusted recursive resolver with no validation
- **Severity:** Medium (elevated in combination with R-079)
- **Category:** security
- **Location:** `internal/dns/resolver.go:30-125` (ResolveNS/ResolveAddresses/ResolveNSWithAddresses via `r.recursive`), `resolver.go:172-246` (CheckZoneCut/DiscoverZoneCuts via the recursive resolver).
- **Problem:** Every nameserver name, its A/AAAA addresses, and the set of zone cuts in the chain are obtained from a single recursive resolver (RD=1, DO unset, no DNSSEC checking) and trusted verbatim. The security of the tool therefore rests entirely on the authoritative-side crypto — which R-079/R-080/R-081 show is not enforced. Even once those are fixed, an attacker controlling the resolver can steer the validator to attacker nameservers and manipulate the zone-cut list (e.g. hide a zone cut, or via `CheckZoneCut` treating SERVFAIL/errors as "is a zone", `resolver.go:192-194`/`235-236`, inject spurious cuts). Root is safely bootstrapped from hardcoded root IPs (`GetRootServers`), which limits the damage to below the root once DS RRSIGs are enforced.
- **Fix specification:** Document and constrain the trust model: (a) after R-079, the DS-RRSIG chain makes resolver-supplied *addresses* non-load-bearing for the secure verdict — ensure a manipulated NS set can only yield `indeterminate`/`bogus`, never a false `secure`; (b) do not treat resolver errors/SERVFAIL as "confirmed zone cut" in a way that changes the verdict — mark such zones `indeterminate`; (c) consider validating the delegation NS/DS chain from the authoritative side rather than the recursive resolver. **Must not change:** the hardcoded root bootstrap.
- **Verification:** With a mock recursive resolver returning attacker NS addresses for a signed zone, the result is never `secure` (it is `bogus`/`indeterminate`) once R-079 is fixed.

### R-085 — NXDOMAIN/NODATA denial proofs are incomplete (single covering record; no wildcard-nonexistence; NSEC3 lacks the closest-encloser/next-closer/wildcard trio)
- **Severity:** Medium
- **Category:** correctness / rfc-compliance
- **Location:** `internal/validator/nsec.go:127-144` (verifyNSECNXDOMAIN), `:148-177` (verifyNSECNODATA), `:289-306` (verifyNSEC3NXDOMAIN), `:310-337` (verifyNSEC3NODATA).
- **Problem:** RFC 4035 §5.4 requires an NXDOMAIN proof to include *both* an NSEC covering the qname *and* an NSEC proving no wildcard could synthesize it; `verifyNSECNXDOMAIN` accepts a single covering NSEC. RFC 5155 §8.4 requires an NSEC3 NXDOMAIN proof to present the closest-encloser match, the next-closer cover, *and* the wildcard cover (three records); `verifyNSEC3NXDOMAIN` accepts a single covering NSEC3. The denial RRSIGs themselves are cryptographically verified (good, the 4ec8209 fix), but the *proof structure* is under-checked, so an incomplete denial is reported `verified`. (Blast radius is limited because denial results feed `RecordValidation`, which per R-082 doesn't currently drive the verdict — fixing R-082 raises this finding's importance.)
- **Fix specification:** Implement the full RFC 4035 §5.4 / RFC 5155 §8.4-8.9 proofs: NXDOMAIN requires the covering record *and* the wildcard-nonexistence record; NSEC3 NXDOMAIN requires closest-encloser + next-closer + wildcard; NODATA must also check the CNAME bit (RFC 6840 §4.3) and handle wildcard-NODATA. Only set `Verified=true` when the complete proof is present and every contributing RRSIG verifies. **Must not change:** the RRSIG cryptographic verification, the `NSECProof` schema.
- **Verification:** Tests with a single covering NSEC/NSEC3 for an NXDOMAIN → `Verified=false` (incomplete); with the full record set → `Verified=true`.

### R-086 — No enforced cap on concurrent validations: `active_validations` is a gauge only, enabling amplification/resource DoS
- **Severity:** Medium
- **Category:** performance / security
- **Location:** `handlers.go:95-96`/`254-255` (`IncrementActiveValidations`/`Decrement` are metrics only), `metrics.go:125-133`.
- **Problem:** `MaxConcurrent` bounds concurrent DNS queries *within* one validation, but nothing bounds the number of concurrent validations across requests — `IncrementActiveValidations` only moves a Prometheus gauge. Each validation in extended mode queries every authoritative NS of every zone in the chain, so a modest number of concurrent requests fans out into a large volume of outbound DNS to third parties (a DNS amplifier) and many goroutines. Per-IP rate limiting is the only guard and is bypassable behind a shared proxy or via distributed clients.
- **Fix specification:** Add a global concurrency limiter (buffered semaphore sized to a config value, e.g. `MAX_CONCURRENT_VALIDATIONS`) acquired at the top of `HandleValidateSSE`/`HandleValidateJSON`; when full, return 503/429 with `Retry-After` rather than starting work. Wire the gauge to the real in-flight count. **Must not change:** the per-validation `MaxConcurrent` semantics, the SSE contract.
- **Verification:** Fire N+1 concurrent validations with the cap at N → the extra request is rejected promptly; the gauge matches the live count.

### R-087 — Root trust anchor loaded with no signature verification; `LoadAnchorsFromURL` accepts `http://`
- **Severity:** Medium
- **Category:** security
- **Location:** `internal/dns/anchors.go:13-41` (LoadAnchors), `:47-97` (LoadAnchorsFromURL — HTTPS not required), no PKCS7/DNSSEC verification anywhere.
- **Problem:** The trust root is loaded from a JSON file or URL and trusted verbatim; there is no verification against IANA's signed `root-anchors.xml`/`.p7s`. `LoadAnchorsFromURL` does not require the URL to be HTTPS, so an operator (or the default being overridden) could fetch anchors over cleartext HTTP, MITM-able. This is partly mitigated because `VerifyRootTrustAnchor` recomputes the DS from the *real* root DNSKEY (served by hardcoded root IPs) and requires a digest match — so a tampered anchor alone fails closed unless the attacker also MITMs the root servers — but it is defense-in-depth worth closing.
- **Fix specification:** Require HTTPS for `ROOT_ANCHORS_URL` (reject `http://` at config validation). Optionally verify the IANA detached signature, or pin the well-known root KSK key tags (20326, 38696) as a sanity floor so a wholesale anchor swap is rejected. Set a size limit (already present, 1 MiB). **Must not change:** the file/URL fallback order, the digest-match verification.
- **Verification:** `ROOT_ANCHORS_URL=http://…` is rejected at startup; a pinned-tag check rejects an anchor file with no known root tag.

### R-088 — NSEC3 iteration count is not capped (RFC 9276): a hostile zone forces up to 65536 SHA-1 hashes per name
- **Severity:** Medium
- **Category:** performance / security
- **Location:** `internal/validator/nsec.go:342-362` (computeNSEC3Hash loops `iterations` times), callers at `:276`, `:551`; `warnings.go:66` only *warns*.
- **Problem:** `iterations` is a uint16 read from the (attacker-controlled) zone's NSEC3 records, up to 65535. `computeNSEC3Hash` performs `iterations+1` SHA-1 operations per name, and is called for the qname, the next-closer name, and per NSEC3 record. RFC 9276 recommends treating iterations > 100 as insecure and validators capping/refusing them. Here there is no cap — a hostile domain submitted to the public validator forces maximal hashing per request (a CPU amplifier), bounded only by the rate limiter.
- **Fix specification:** Enforce an iteration cap per RFC 9276: reject (treat the proof as insecure/failed) NSEC3 records with iterations > a small bound (e.g. 100), before computing hashes. Keep the existing warning for 1–100. **Must not change:** the SHA-1 hash computation for compliant zones.
- **Verification:** An NSEC3 record with iterations=1000 → the denial is rejected/flagged, and no 1000-iteration hashing is performed.

### R-089 — A failed initial trust-anchor load is only retried after 24 hours, causing up to 24h of 503s
- **Severity:** Medium
- **Category:** error-handling / availability
- **Location:** `main.go:39-49` (startup load, warn-and-continue), `main.go:86-104` (only refresh is the 24h ticker).
- **Problem:** If `anchorsStore.Load()` fails at startup (mirror briefly down, file missing), the server starts anyway and every `/validate`/`/api/validate` returns 503 "root trust anchors not available"; the log says "will retry" but the only retry is the 24-hour ticker. A transient boot-time failure disables the service for up to a day.
- **Fix specification:** On initial load failure, retry with backoff (e.g. every 30s–5m up to some ceiling) until anchors load, independent of the 24h steady-state refresh; expose readiness via `/health`. **Must not change:** the 24h steady refresh, the fallback file→URL order.
- **Verification:** Start with the anchor file/URL unavailable, then make it available → the service recovers within the backoff window, not 24h.

## Low

- **R-090 (Low) — SSE auto-reconnect re-runs the full server-side validation.** `handlers.go:37-177` — if the SSE stream drops without a terminal `complete`/fatal `error` event, the browser's `EventSource` auto-reconnects and the server starts a brand-new validation (`app.js:171-175` only re-enables the button on `CLOSED`). Amplifies R-086. *Fix:* always send a terminal event and set `Last-Event-ID`/a done marker so the client stops; consider refusing a reconnect that carries a completed id. *Verify:* drop the connection mid-stream → the server does not silently re-run.
- **R-091 (Low) — rate-limiter map eviction only every cleanup interval → IPv6 churn memory DoS.** `ratelimit.go:75-94` — buckets are evicted only when the cleanup ticker (default 5m) fires; an attacker rotating source addresses within a /64 can create millions of buckets within one window. *Fix:* cap the map size (LRU) or evict opportunistically on insert past a threshold. *Verify:* churn many unique IPs within one interval → memory stays bounded.
- **R-092 (Low) — RDAP client reads the response body with no size limit.** `internal/rdap/client.go:67` uses `io.ReadAll(resp.Body)` (contrast the 1 MiB `LimitReader` in `anchors.go:70`). A compromised/oversized RDAP response exhausts memory. *Fix:* wrap in `io.LimitReader` (e.g. 1 MiB). *Verify:* a large RDAP body is truncated, not fully buffered.
- **R-093 (Low) — go-toml/v2 without `DisallowUnknownFields` (silent typo'd config keys).** `config.go:170` — same class as signer R-015; a mistyped validator TOML key is silently ignored. *Fix:* strict decoding as in R-015. *Verify:* a typo'd key errors at load.
- **R-094 (Low, docs) — validator CLAUDE.md documents env-only `.env` config, but the code prefers TOML and `.env` is house-banned.** `CLAUDE.md:561-587` ("All configuration is via environment variables (not TOML)", "Create a `.env` file … based on `.env.example`") contradicts `config.go` (TOML file paths checked before env) and the user's global "never `.env`" policy. *Fix:* rewrite the config section to document TOML-first (matching `dnssec-validator.toml.example`) with env as fallback; drop `.env` references. *Verify:* docs match `Load()` behavior.
- **R-095 (Low) — `IsKSK`/`IsZSK` use strict flag equality (257/256), misclassifying REVOKE-bit and non-standard-flag keys.** `internal/dns/query.go:115-116` — a revoked key (RFC 5011, REVOKE bit set) or a key with extra flags is neither KSK nor ZSK, so `FindKSK/ZSKByKeyTag` miss it (though `FindDNSKEYByKeyTag` by tag still finds it). *Fix:* classify by the Zone-Key bit (bit 7) for signing capability and the SEP bit (bit 0) for KSK, rather than exact equality; handle REVOKE explicitly. *Verify:* a key with flags 257+REVOKE is still handled.
- **R-096 (Low) — CNAME RRSIG verification lacks the signer-name==zone check the A path has.** `internal/validator/validator.go:476-505` verifies a CNAME RRSIG by keytag but, unlike the A path (`validator.go:531`), never checks `rrsig.SignerName == zone`. Cryptographic verification still requires the signature to be by that key, so impact is low, but the defense-in-depth check is missing. *Fix:* add the signer-name check to the CNAME branch. *Verify:* a CNAME RRSIG with a foreign signer is rejected.
- **R-097 (Low) — NODATA NSEC proof doesn't check the CNAME/DNAME bit (RFC 6840 §4.3).** `internal/validator/nsec.go:148-177` — an NSEC NODATA proof should confirm the CNAME bit is not set (else a CNAME should have been returned). *Fix:* reject NODATA when the matching NSEC has the CNAME (or DNAME above) bit. *Verify:* a NODATA NSEC with the CNAME bit is not accepted.
- **R-098 (Low) — `/metrics` is unrestricted by default.** `server.go:71-80` — `MetricsAllowedCIDRs` defaults empty, so Prometheus metrics (including request/validation internals) are world-readable unless the operator sets a CIDR. *Fix:* default to loopback-only, or document prominently; the systemd unit should set the allowlist. *Verify:* default config restricts `/metrics` to loopback.
- **R-099 (Low, maintainability) — heartbeat code is duplicated between the two projects.** `signer/heartbeat.go` and `validator/internal/heartbeat/heartbeat.go` are near-duplicate AnyStatus clients that have already diverged (async vs sync sends, different lifecycle). *Fix:* extract a shared `libs/` heartbeat package (per the `~/Git` layout conventions) consumed by both. *Verify:* one implementation, both projects import it.
- **R-100 (Low) — the "query every NS, flag inconsistencies" headline feature is only partial for the leaf record.** `verifyActualRecord`/`checkAndFollowCNAME` (`validator.go:424-431`, `:348-355`) query authoritative servers only until the *first* success, so a per-server disagreement on the actual answer (the exact scenario the tool advertises) is not surfaced for the leaf record — only the DNSKEY step (`ValidateMultipleServers`) compares across servers, and even there disagreements are warning-only. *Fix:* query all servers for the leaf record and report per-server disagreement, consistent with the DNSKEY step and the documented feature. *Verify:* a zone where one NS returns a different/expired answer is flagged.

---

# Summary

**Coverage note:** The signer (`signer`) was reviewed by a fleet of area reviewers plus a full manual verification pass of every High/Medium finding against source. The validator (`validator`) was reviewed entirely by direct read (the review fleet was cut off by an account spend limit before it reached the validator), covering every `.go` file, the frontend, and config/build artifacts. Both projects' `go build`/`go vet`/`gofmt`/`go test` are clean — every finding here is invisible to that toolchain.

## Findings by severity

| Severity | Count | IDs |
|---|---|---|
| **Critical** | 2 | R-079, R-080 |
| **High** | 14 | R-001, R-002, R-003, R-004, R-005, R-006, R-007, R-008, R-009, R-010, R-011, R-012, R-081, R-082 |
| **Medium** | 40 | R-013…R-044 (signer: 32), R-045…R-048 (validate.go, listed under signer Low but Medium severity: 4), R-083, R-084, R-085, R-086, R-087, R-088, R-089 (validator: 7) |
| **Low** | 44 | R-049…R-078 (signer: 30), R-090…R-100 (validator: 11), plus R-055/R-094 doc items |

(Counts approximate where a few IDs carry a severity that differs from their listing group — notably R-045…R-048 are Medium-severity `validate.go` findings grouped under the signer Low block for locality, and R-052/R-054 are Medium-leaning. Treat the per-finding **Severity** line as authoritative.)

## The two things to fix first

1. **Validator chain enforcement (R-079 + R-080 + R-081 + R-082).** The validator currently reports forged/bogus zones as `secure` and secure zones as `insecure`. These four together are the difference between a working DNSSEC validator and one that provides false assurance. They are independent of each other and each must be closed. Fix as one coordinated change to `validateZone`/`VerifyDNSKEYRRSIG`, with the leaf-verdict fold-in (R-082) and DS-absence proof (R-081). Add adversarial tests (forged DS, rogue KSK, stripped DS, forged answer) that must all yield `bogus`/`indeterminate`.
2. **Signer bogus-zone and outage risks (R-003, R-002, R-009, R-010, R-001, R-006, R-007, R-008).** The signer can silently ship a bogus zone (rollover warn-and-continue, non-atomic keys, no post-sign verify), corrupt its output under concurrency, panic on a standard ED25519 import, and mismanage the CLI/daemon state split (remove/rollover lost updates, SIGHUP staleness).

## Suggested fix order (accounting for dependencies)

**Phase 1 — validator soundness (do together, ship behind tests):**
- R-079, R-080, R-081, R-082 — chain-of-trust enforcement. R-084 (resolver trust) becomes non-exploitable once these land; verify that.
- R-085 (complete denial proofs) — do alongside R-082 since fixing R-082 makes denial correctness verdict-affecting.

**Phase 2 — signer data-integrity and crash safety (mostly independent, high value):**
- R-010 (ED25519 import panic) — small, self-contained, crash fix.
- R-009 (atomic key writes + pub/priv cross-check) and R-027 (backup-failure fatal) and R-028 (orphan-half refusal) — same subsystem, do together.
- R-002 (unique temp names for signed zone + state.json) — prerequisite mindset for R-007.
- R-001 (post-sign verify gate) — independent safety net; catches regressions in R-003 and NSEC bugs.
- R-003 (rollover warn-and-continue → fatal) — depends conceptually on R-027 (backup reliability); do R-027 first.

**Phase 3 — signer CLI/daemon coordination (interrelated; R-007 is the anchor):**
- R-007 (state.json inter-process lock) — foundational; R-008, R-021, R-038, and the daemon side of R-018 build on it.
- R-008 (remove transactional) and R-023 (import transactional) — after R-007.
- R-006 (SIGHUP reload staleness) and R-018 (heartbeat/cfg race) — daemon lifecycle; R-018's snapshot change is small.
- R-019, R-020, R-021, R-026 (shutdown/lifecycle) — after R-006/R-018.

**Phase 4 — validator hardening and signer config validation:**
- R-086 (concurrent-validation cap), R-088 (NSEC3 iteration cap), R-087 (anchor HTTPS/verify), R-089 (anchor retry), R-091/R-092 (resource limits).
- R-013, R-014, R-015 (config validation + strict TOML) — signer; R-054/R-016 depend on R-015.
- R-005 (serve --web loopback re-check) — small, security-relevant, do early if the web UI is deployed.

**Phase 5 — correctness edges, RFC completeness, and the validator diagnostic verdicts:**
- R-045…R-048 (validate.go diagnostic accuracy), R-012 (expired-sig false pass) — the signer's `validate` command.
- R-011 (ZSK phase gating), R-037/R-038/R-039/R-041 (rollover correctness), R-034/R-035 (input hygiene), R-083 (validator record type), R-100 (leaf multi-server).

**Phase 6 — docs, dead code, tests, and maintainability:**
- R-054/R-055/R-094 (doc drift: quoted zone tables, BurntSushi vs go-toml, `.env` vs TOML), R-016/R-032 (registrar docs/contract), R-042/R-069 (metrics), R-070/R-062/R-074 (dead code), R-099 (shared heartbeat lib), R-076/R-077/R-078 (test gaps).

## Cross-cutting notes

- **Build/git hygiene:** `git ls-files` shows the compiled binaries `signer/dnssec-tudor` and `validator/dnssec-validator` are present on disk but the `.gitignore` (widened in commit `4d80794`/`990f6c5`) excludes them; confirm they are untracked before release. Both projects build/vet/test clean on go1.26.4.
- **Doc/reality inversions (both projects):** the root and `daemons/dnssec` `CLAUDE.md` claim the signer uses `BurntSushi/toml` "the only project on BurntSushi rather than go-toml" — both projects actually use `pelletier/go-toml/v2` (R-055). The signer's documented unquoted `[zones.ptudor.net]` config silently registers zero zones (R-054). The validator's CLAUDE.md documents env/`.env` config while the code prefers TOML (R-094). These matter because they misdirect fixes and can produce a non-functional deployment from copy-pasted docs.
- **What is genuinely solid (not findings, for reviewer confidence):** signer NSEC/NSEC3 chain generation's occlusion/ENT handling; validator DO-bit + TCP-fallback transport; validator denial-proof *RRSIG* cryptographic verification (the 4ec8209 fix); validator XFF rightmost-untrusted client-IP extraction (the 95a3976 fix); validator frontend XSS escaping; both projects' graceful-shutdown skeletons and structured logging. The signer's `hooks` are not command-injectable (operator-config commands, env-var data).
